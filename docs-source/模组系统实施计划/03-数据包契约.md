# 数据包契约

> [返回统筹说明](./README.md)

> 配套规范：[确定性哈希](./10-规范化与哈希规范.md) · [错误码](./11-错误码规范.md) · [资源限制](./12-资源限制默认值.md) · [注册表快照缓存](./15-注册表快照缓存.md)

## 6. 数据包契约

### 6.1 清单文件

每个数据包必须包含 `manifest.json`：

```json
{
  "id": "example_mod",
  "name": {
    "key": "example_mod.package.name",
    "fallback": "示例内容包"
  },
  "version": "1.0.0",
  "gameVersion": "2.4.0",
  "engineApiVersion": "1",
  "contentSchemaVersion": "1",
  "defaultLocale": "zh-CN",
  "locales": {
    "zh-CN": "locales/zh-CN.json",
    "en-US": "locales/en-US.json"
  },
  "authors": [
    { "name": "示例作者", "role": "developer" }
  ],
  "license": "MIT",
  "homepage": "https://example.com/mod",
  "issues": "https://example.com/mod/issues",
  "dependencies": [
    { "id": "example_library", "version": "1.3.2" }
  ],
  "entrypoints": {
    "taoyuan:tag": ["data/tags.json"],
    "taoyuan:item": ["data/items.json"],
    "taoyuan:crop": ["data/crops.json"],
    "taoyuan:tree": ["data/trees.json"],
    "taoyuan:fish": ["data/fish.json"],
    "taoyuan:pondable_fish": ["data/pondable-fish.json"],
    "taoyuan:pond_breed": ["data/pond-breeds.json"],
    "taoyuan:fish_pond_facility": ["data/fish-pond-facilities.json"],
    "taoyuan:building_upgrade": ["data/building-upgrades.json"],
    "taoyuan:forage": ["data/forage.json"],
    "taoyuan:animal": ["data/animals.json"],
    "taoyuan:animal_feed": ["data/animal-feeds.json"],
    "taoyuan:wallet_item": ["data/wallet-items.json"],
    "taoyuan:secret_note": ["data/secret-notes.json"],
    "taoyuan:tutorial": ["data/tutorials.json"],
    "taoyuan:museum_category": ["data/museum-categories.json"],
    "taoyuan:museum_item": ["data/museum-items.json"],
    "taoyuan:museum_milestone": ["data/museum-milestones.json"],
    "taoyuan:guild_goal": ["data/guild-goals.json"],
    "taoyuan:guild_donation": ["data/guild-donations.json"],
    "taoyuan:guild_level": ["data/guild-levels.json"],
    "taoyuan:npc": ["data/npcs.json"],
    "taoyuan:hidden_npc": ["data/hidden-npcs.json"],
    "taoyuan:story_quest": ["data/story-quests.json"],
    "taoyuan:heart_event": ["data/heart-events.json"],
    "taoyuan:season_event": ["data/season-events.json"],
    "taoyuan:quest_template": ["data/quest-templates.json"],
    "taoyuan:morning_event": ["data/morning-events.json"],
    "taoyuan:achievement": ["data/achievements.json"],
    "taoyuan:community_bundle": ["data/community-bundles.json"],
    "taoyuan:farm_map": ["data/farm-maps.json"],
    "taoyuan:animal_building": ["data/animal-buildings.json"],
    "taoyuan:animal_incubation": ["data/animal-incubations.json"],
    "taoyuan:breeding_hybrid": ["data/breeding-hybrids.json"],
    "taoyuan:tool_upgrade": ["data/tool-upgrades.json"],
    "taoyuan:processing_machine": ["data/processing-machines.json"],
    "taoyuan:processing_recipe": ["data/processing-recipes.json"],
    "taoyuan:equipment": ["data/equipment.json"],
    "taoyuan:equipment_set": ["data/equipment-sets.json"],
    "taoyuan:monster": ["data/monsters.json"],
    "taoyuan:monster_pool": ["data/monster-pools.json"],
    "taoyuan:enchantment": ["data/enchantments.json"],
    "taoyuan:drop_table": ["data/drop-tables.json"],
    "taoyuan:recipe": ["data/recipes.json"],
    "taoyuan:shop": ["data/shops.json"],
    "taoyuan:shop_offer": ["data/shop-offers.json"],
    "taoyuan:market_category": ["data/market-categories.json"],
    "taoyuan:hanhai_trade_exchange": ["data/hanhai-trade-exchanges.json"],
    "taoyuan:hanhai_trade_shop_upgrade": ["data/hanhai-trade-shop-upgrades.json"],
    "taoyuan:hanhai_treasure_reward": ["data/hanhai-treasure-rewards.json"]
  },
  "settings": "settings.schema.json",
  "attributions": "attributions.json",
  "assets": "assets"
}
```

`entrypoints` 的键必须是已经定义的 `RegistryTypeId`。普通数据包遇到未知注册表类型时应拒绝加载，不能根据文件内容临时创建注册表。

首版清单至少包含：

- 唯一数据包 ID。
- 可本地化的用户可见名称和确定版本。
- 制作和测试该模组时所针对的游戏版本。
- 所需引擎 API 版本和数据 Schema 版本。
- 必需依赖和版本范围。
- 各注册表的数据入口。
- 默认语言、语言包入口、作者、许可证和问题反馈地址。
- 可选配置 Schema 和资源署名清单。
- 可选资源目录。

`loadBefore`、`loadAfter` 和可选依赖可以后续增加。首版应优先使用显式依赖排序，不通过模糊优先级掩盖冲突。

### 6.2 注册表行为

注册表类型本身也使用命名空间 ID。官方首批注册表类型包括：

```text
taoyuan:tag
taoyuan:item
taoyuan:crop
taoyuan:tree
taoyuan:fish
taoyuan:pondable_fish
taoyuan:pond_breed
taoyuan:fish_pond_facility
taoyuan:building_upgrade
taoyuan:forage
taoyuan:animal
taoyuan:animal_feed
taoyuan:wallet_item
taoyuan:secret_note
taoyuan:tutorial
taoyuan:museum_category
taoyuan:museum_item
taoyuan:museum_milestone
taoyuan:guild_goal
taoyuan:guild_donation
taoyuan:guild_level
taoyuan:npc
taoyuan:hidden_npc
taoyuan:story_quest
taoyuan:heart_event
taoyuan:season_event
taoyuan:quest_template
taoyuan:morning_event
taoyuan:achievement
taoyuan:community_bundle
taoyuan:farm_map
taoyuan:animal_building
taoyuan:animal_incubation
taoyuan:breeding_hybrid
taoyuan:tool_upgrade
taoyuan:processing_machine
taoyuan:processing_recipe
taoyuan:equipment
taoyuan:equipment_set
taoyuan:monster
taoyuan:monster_pool
taoyuan:enchantment
taoyuan:drop_table
taoyuan:recipe
taoyuan:shop
taoyuan:shop_offer
taoyuan:market_category
taoyuan:hanhai_trade_exchange
taoyuan:hanhai_trade_shop_upgrade
taoyuan:hanhai_treasure_reward
```

内容的完整身份是“注册表类型 ID + 内容 ID”，而不是只有内容 ID：

```ts
type RegistryTypeId = NamespacedId

interface RegistryEntryKey {
  registryId: RegistryTypeId
  contentId: ContentId
}
```

因此 `taoyuan:item + taoyuan:seed` 与 `taoyuan:tag + taoyuan:seed` 可以同时存在。字段类型和目标注册表决定一个 ID 表示物品、标签还是配方，不在内容 ID 中重复写入 `tag::`、`item::` 等类别前缀。

通用注册表至少提供：

```ts
interface Registry<T> {
  register(owner: PackageId, entry: T): void
  get(id: ContentId): T | undefined
  require(id: ContentId): T
  has(id: ContentId): boolean
  values(): readonly T[]
  freeze(): void
}
```

`RegistrySet` 应作为按 `RegistryTypeId` 查询的注册表目录，不应将 `items`、`recipes` 等写成不可扩展的固定属性：

```ts
interface RegistrySet {
  defineRegistry<T>(definition: RegistryDefinition<T>): void
  get<T>(registryId: RegistryTypeId): Registry<T>
  freezeDefinitions(): void
  freezeEntries(): void
}
```

第一阶段只有引擎可以调用 `defineRegistry()`，数据包只能向引擎已公开的注册表注册条目。注册阶段保留内容来源、文件路径和原始本地 ID，以便错误报告能定位到数据包。冻结后不得暴露可修改数组和对象引用。

#### 6.2.1 怪物池契约

`taoyuan:monster_pool` 与 `taoyuan:monster` 相互独立。公开 TypeBox 契约的等价形状为：

```ts
interface MonsterPoolEntry {
  monsterId: ContentId
  weight?: number
}

interface MonsterPoolDef {
  id: ContentId
  entries: readonly [MonsterPoolEntry, ...MonsterPoolEntry[]]
}
```

- `weight` 必须是正整数，省略时按 `1` 处理。
- 单池 `entries` 原始条目数不超过 250,000，单条 `weight` 不超过 250,000，全部条目的有效权重总和不超过 250,000。
- 任一资源边界超限时产生 `PKG-LIMIT-001`，诊断包含 `poolId`、`actual`、`limit` 和 `fieldPath`；不得截断、归一化、清零或忽略超限权重。
- `entries` 必须非空，其顺序是公开且可观察的行为；注册、快照和解析均不得排序。
- 池只描述候选怪物和权重，不包含楼层生成、特殊层、属性缩放、战斗公式或随机数生成器。
- 每个 `monsterId` 必须引用 `taoyuan:monster` 中存在的条目，否则产生 `REG-REFERENCE-001`。
- 当前主矿洞 BOSS 池必须恰好包含一个等权条目，不满足时产生 `SCHEMA-VALIDATE-001`。
- 游戏运行所需的官方池缺失时产生 `REG-REQUIRED-001` `fatal` 诊断，不得在运行时静默退化为空池。
- 外部怪物池 JSON 必须先作为 `unknown` 处理，再通过由 `MonsterPoolDefSchema` 生成的 JSON Schema 和 Ajv 校验。

阶段 5 冻结以下 15 个官方池 ID：

```text
taoyuan:pool/mine/zone/shallow
taoyuan:pool/mine/zone/frost
taoyuan:pool/mine/zone/lava
taoyuan:pool/mine/zone/crystal
taoyuan:pool/mine/zone/shadow
taoyuan:pool/mine/zone/abyss
taoyuan:pool/mine/boss/20
taoyuan:pool/mine/boss/40
taoyuan:pool/mine/boss/60
taoyuan:pool/mine/boss/80
taoyuan:pool/mine/boss/100
taoyuan:pool/mine/boss/120
taoyuan:pool/skull-cavern/base
taoyuan:pool/skull-cavern/depth-11
taoyuan:pool/skull-cavern/boss
```

`depth-11` 只保存 `floor > 10` 时追加的候选；深度阈值、每 25 层出现 BOSS 和骷髅矿穴缩放仍由框架决定。当前官方池全部省略 `weight`，因此固定随机值继续映射到与旧数组下标完全相同的怪物。

#### 6.2.2 树木契约

`taoyuan:tree` 独立于 `taoyuan:crop` 和 `taoyuan:item`，使用 TypeBox 判别联合表达果树与野树：

```ts
type TreeDef =
  | {
      id: ContentId
      kind: 'fruit'
      name: LocalizedTextRef
      seedItemId: ContentId
      growthDays: number
      saplingPrice: number
      fruitItemId: ContentId
      fruitName: LocalizedTextRef
      fruitSeason: Season
      fruitSellPrice: number
    }
  | {
      id: ContentId
      kind: 'wild'
      name: LocalizedTextRef
      seedItemId: ContentId
      growthDays: number
      tapProductItemId: ContentId
      tapProductName: LocalizedTextRef
      tapCycleDays: number
    }
```

- `growthDays` 和 `tapCycleDays` 必须是正整数；`saplingPrice` 和 `fruitSellPrice` 必须是非负整数。
- 两个联合分支均使用 `additionalProperties: false`，果树和野树字段不得混合。
- `seedItemId`、`fruitItemId` 和 `tapProductItemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- 外部树木 JSON 在校验前保持为 `unknown`，通过 `TreeDefSchema` / `tree.schema.json` 后才允许注册。
- 阶段 6 首切片冻结 11 个本地 ID：`peach_tree`、`lychee_tree`、`mandarin_tree`、`plum_tree`、`apricot_tree`、`pomegranate_tree`、`persimmon_tree`、`hawthorn_tree`、`pine`、`camphor`、`mulberry`。
- 注册表 ID 使用 `taoyuan:*`，兼容对象和 `PlantedFruitTree.type` / `PlantedWildTree.type` 继续使用上述本地 ID，不改变存档 JSON。
- `treeDefinitions.ts` 是这 11 种树木旧格式数值的唯一静态来源；`items.ts` 为派生树苗/果实物品、`staticAdapters.ts` 为官方投影、等价测试为回归基线而读取该叶子模块。普通 Store、composable 和 Vue 页面不得直接读取旧数组。

#### 6.2.3 采集物契约

`taoyuan:forage` 描述竹林采集物本身，不包含天气倍率、技能加成、工具消耗、动物遭遇或战斗流程。公开 TypeBox 契约的等价形状为：

```ts
interface ForageDef {
  id: ContentId
  itemId: ContentId
  name: LocalizedTextRef
  season: Season[]
  chance: number
  expReward: number
}
```

- `id` 是采集条目 ID；当前官方条目与产物 `itemId` 保持一致，但运行时背包和存档仍只保存本地物品 ID。
- `itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `season` 必须非空且只使用四季枚举；`chance` 取值范围为 0 到 1；`expReward` 必须是非负整数。
- `WEATHER_FORAGE_MODIFIER`、`FOREST_ENCOUNTER_CHANCE`、动物/野兽遭遇随机、战败惩罚和战斗算法继续属于框架规则，不能随本采集物切片伪装成内容数据。
- 外部采集物 JSON 在校验前保持为 `unknown`，通过 `ForageDefSchema` / `forage.schema.json` 后才允许注册。

#### 6.2.4 动物定义契约

`taoyuan:animal` 描述可饲养动物的物种定义，不包含畜舍建造/升级、饲料效果、孵化器进度、放牧、疾病、死亡、宠物拾取或每日结算算法。公开 TypeBox 契约的等价形状为：

```ts
interface AnimalDef {
  id: ContentId
  name: LocalizedTextRef
  building: 'coop' | 'barn' | 'stable'
  cost: number
  productItemId?: ContentId
  productName?: LocalizedTextRef
  produceDays: number
  friendship: { min: number; max: number }
}
```

- `id` 是动物物种 ID；当前官方条目继续使用 `chicken`、`cow`、`horse` 等本地 ID 的 `taoyuan:*` 命名空间形式，运行时动物实例和旧存档仍保存本地 `type`。
- `building` 只表达物种归属建筑；建筑定义、容量升级、材料消耗和马厩建造仍由现有设施/框架规则处理。
- `productItemId` 仅对会产出的动物存在，必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`；马等无产出动物省略该字段且 `produceDays` 为 `0`。
- `cost`、`produceDays` 和 `friendship.min/max` 必须是非负整数；当前切片不改变购买价、产物、产出周期、好感范围、品质计算或出售退款。
- 外部动物 JSON 在校验前保持为 `unknown`，通过 `AnimalDefSchema` / `animal.schema.json` 后才允许注册。

#### 6.2.5 动物饲料定义契约

`taoyuan:animal_feed` 描述牧场 UI 可选择和购买的动物饲料定义，不包含喂食后的好感、心情、治病、产出天数或每日结算算法。公开 TypeBox 契约的等价形状为：

```ts
interface AnimalFeedDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  price: number
}
```

- `id` 是饲料物品 ID；当前官方条目继续使用 `hay`、`premium_feed`、`nourishing_feed` 和 `vitality_feed` 的 `taoyuan:*` 命名空间形式，运行时动物实例和旧存档仍保存本地 `fedWith`。
- `id` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `price` 必须是非负整数；当前切片不改变购买价格、库存扣除、饲料特殊效果、动物好感、心情、疾病治愈、产出节奏或存档结构。
- 外部动物饲料 JSON 在校验前保持为 `unknown`，通过 `AnimalFeedDefSchema` / `animal-feed.schema.json` 后才允许注册。

#### 6.2.6 动物建筑定义契约

`taoyuan:animal_building` 描述牧场动物建筑的建造定义和嵌套升级定义，不包含动物实例、孵化映射、饲料效果、放牧、疾病、死亡、宠物拾取或每日结算算法。公开 TypeBox 契约的等价形状为：

```ts
interface AnimalBuildingMaterial {
  itemId: ContentId
  quantity: number
}

interface AnimalBuildingUpgrade {
  level: number
  name: LocalizedTextRef
  capacity: number
  cost: number
  materialCost: AnimalBuildingMaterial[]
}

interface AnimalBuildingDef {
  id: ContentId
  building: 'coop' | 'barn' | 'stable'
  name: LocalizedTextRef
  description: LocalizedTextRef
  capacity: number
  cost: number
  materialCost: AnimalBuildingMaterial[]
  upgrades: AnimalBuildingUpgrade[]
}
```

- `id` 是建筑定义 ID；当前官方条目使用 `animal_building/coop`、`animal_building/barn` 和 `animal_building/stable` 的 `taoyuan:*` 命名空间形式，运行时建筑状态和旧存档仍保存本地 `type`。
- `building` 继续使用旧本地枚举 `coop`、`barn`、`stable`，以便动物物种归属、畜舍状态和升级查询保持兼容。
- `materialCost[].itemId` 与 `upgrades[].materialCost[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `capacity` 和材料 `quantity` 必须是正整数；`cost` 必须是非负整数；升级 `level` 当前要求大于等于 2。
- 当前切片不改变建造价格、升级价格、容量、材料消耗、扣款/扣料顺序、马厩行为或存档结构。
- 外部动物建筑 JSON 在校验前保持为 `unknown`，通过 `AnimalBuildingDefSchema` / `animal-building.schema.json` 后才允许注册。

#### 6.2.7 动物孵化定义契约

`taoyuan:animal_incubation` 描述蛋类物品孵化为动物的映射关系，不包含孵化器容量、鸡舍大师减半、物品扣除/退还、命名、建筑状态、疾病、放牧或每日结算算法。公开 TypeBox 契约的等价形状为：

```ts
interface AnimalIncubationDef {
  id: ContentId
  itemId: ContentId
  animalId: ContentId
  building: 'coop' | 'barn' | 'stable'
  days: number
}
```

- `id` 是孵化映射定义 ID；当前官方条目使用 `animal_incubation/<egg item local id>` 的 `taoyuan:*` 命名空间形式，运行时孵化器状态和旧存档仍保存本地 `itemId` 与 `animalType`。
- `itemId` 必须引用现有 `taoyuan:item`，`animalId` 必须引用现有 `taoyuan:animal`，缺失时产生 `REG-REFERENCE-001`。
- `building` 继续使用旧本地枚举 `coop`、`barn`、`stable`，并必须能匹配现有 `taoyuan:animal_building` 定义；缺失时产生 `REG-REFERENCE-001`。
- `days` 必须是正整数。
- 当前切片不改变鸡舍大师孵化时间减半、孵化器占用检查、物品扣除、孵化完成顺序、动物命名、日志文案或存档结构。
- 外部动物孵化 JSON 在校验前保持为 `unknown`，通过 `AnimalIncubationDefSchema` / `animal-incubation.schema.json` 后才允许注册。

#### 6.2.7.1 育种杂交定义契约

`taoyuan:breeding_hybrid` 描述育种台可发现的杂交品种定义，不包含育种台数量、种子箱容量、加工天数、变异随机、属性波动、图鉴状态、种子实例或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface BreedingHybridBaseGenetics {
  sweetness: number
  yield: number
  resistance: number
}

interface BreedingHybridDef {
  id: ContentId
  name: LocalizedTextRef
  parentCropA: ContentId
  parentCropB: ContentId
  minSweetness: number
  minYield: number
  resultCropId: ContentId
  baseGenetics: BreedingHybridBaseGenetics
  discoveryText: LocalizedTextRef
}
```

- `id` 当前按 `taoyuan:breeding_hybrid/{legacyHybridId}` 生成；运行时种子、图鉴和旧存档仍保存旧本地 `hybridId`，兼容查询接受本地 ID、`breeding_hybrid/{id}` 和完整命名空间 ID。
- `parentCropA`、`parentCropB` 和 `resultCropId` 必须引用现有 `taoyuan:crop`，缺失时产生 `REG-REFERENCE-001`。
- `minSweetness`、`minYield` 与 `baseGenetics.*` 均为 0 到 100 的整数；当前切片不改变任何杂交门槛、基础基因、发现文本或品种阶层。
- 杂交判定、同种育种、变异、图鉴发现、成就记录、育种台队列和种子箱容量仍由既有 Store/框架规则处理。
- 外部育种杂交 JSON 在校验前保持为 `unknown`，通过 `BreedingHybridDefSchema` / `breeding-hybrid.schema.json` 后才允许注册。
#### 6.2.8 工具升级定义契约

`taoyuan:tool_upgrade` 描述工具从一个等级升级到下一等级所需的金币和材料，不包含工具耐力消耗、批量操作范围、鱼竿判定、升级等待天数、铁匠铺 UI、扣款/扣料顺序或存档结构。公开 TypeBox 契约的等价形状为：

```ts
type ToolType =
  | 'wateringCan'
  | 'hoe'
  | 'pickaxe'
  | 'fishingRod'
  | 'scythe'
  | 'axe'
  | 'pan'

type ToolTier = 'basic' | 'iron' | 'steel' | 'iridium'

interface ToolUpgradeMaterial {
  itemId: ContentId
  quantity: number
}

interface ToolUpgradeDef {
  id: ContentId
  toolType: ToolType
  fromTier: ToolTier
  toTier: ToolTier
  money: number
  materials: ToolUpgradeMaterial[]
}
```

- `id` 当前按 `taoyuan:tool_upgrade/{snake tool type}/{from}_to_{to}` 生成，例如 `taoyuan:tool_upgrade/watering_can/basic_to_iron`；运行时工具状态和旧存档仍保存旧本地 `ToolType` 与 `ToolTier` 字符串。
- `toolType` 继续使用旧本地枚举值，包含 `wateringCan` 和 `fishingRod` 等 camelCase 值；内容 ID 路径使用 snake case 只是为了满足 `ContentId` 规则。
- `materials[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `money` 必须是非负整数；材料 `quantity` 必须是正整数。
- 当前切片只迁移 `TOOL_UPGRADE_COSTS`、`TOOL_NAMES`、`TIER_NAMES` 和 `getUpgradeCost(type, currentTier)` 查询入口；工具升级排队、两天等待、耐力/范围/批量操作、钓鱼小游戏和旧存档结构仍由现有框架规则处理。
- 外部工具升级 JSON 在校验前保持为 `unknown`，通过 `ToolUpgradeDefSchema` / `tool-upgrade.schema.json` 后才允许注册。

#### 6.2.9 可养殖鱼定义契约

`taoyuan:pondable_fish` 描述可放入鱼塘养殖的鱼种定义，不包含鱼塘建造/升级、容量、水质衰减、疾病、死亡、育苗塘、繁衍塘或基因随机算法。公开 TypeBox 契约的等价形状为：

```ts
interface PondFishGenetics {
  weight: number
  growthRate: number
  diseaseRes: number
  qualityGene: number
  mutationRate: number
}

interface PondableFishDef {
  id: ContentId
  fishItemId: ContentId
  name: LocalizedTextRef
  maturityDays: number
  baseProductionRate: number
  productItemId: ContentId
  defaultGenetics: PondFishGenetics
}
```

- `id` 是可养殖鱼定义 ID；当前官方条目与 `fishItemId` 保持一致，运行时鱼塘实例和旧存档仍保存本地 `fishId`。
- `fishItemId` 必须引用现有 `taoyuan:fish`，`productItemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `maturityDays` 必须是正整数；`baseProductionRate` 取值范围为 0 到 1；`weight`、`growthRate`、`diseaseRes` 和 `qualityGene` 为 0 到 100 的整数；`mutationRate` 为 1 到 50 的整数。
- 当前切片只迁移 13 种 `PONDABLE_FISH` 物种定义；鱼塘容量、鱼苗上限、水质、喂食、疾病、产出概率加成、品质计算、育苗和自然繁衍仍由现有 Store/框架规则处理。
- 外部可养殖鱼 JSON 在校验前保持为 `unknown`，通过 `PondableFishDefSchema` / `pondable-fish.schema.json` 后才允许注册。

#### 6.2.10 鱼塘品种定义契约

`taoyuan:pond_breed` 描述鱼塘图鉴中的品种定义和亲本链路，不包含鱼塘建造/升级、容量、水质、疾病、死亡、育苗塘、繁衍塘、遗传随机、基因变异或品质计算算法。公开 TypeBox 契约的等价形状为：

```ts
interface PondBreedDef {
  id: ContentId
  name: LocalizedTextRef
  generation: 1 | 2 | 3 | 4 | 5
  baseFishId: ContentId
  parentBreedA: ContentId | null
  parentBreedB: ContentId | null
}
```

- `id` 是品种图鉴 ID；当前官方条目继续使用 `g1_001` 至 `g5_020` 的本地 ID 兼容形状，运行时鱼个体和旧存档仍保存本地 `breedId`。
- `baseFishId` 必须引用现有 `taoyuan:pondable_fish`，缺失时产生 `REG-REFERENCE-001`。
- `parentBreedA` 和 `parentBreedB` 对一代品种为 `null`；二代及以上品种引用同一 `taoyuan:pond_breed` 中存在的亲本，缺失时产生 `REG-REFERENCE-001`。
- 品种顺序、亲本配对和每代数量是玩家可观察的图鉴/繁育行为，注册表投影不得排序或重写。
- 当前切片只迁移 400 条 `POND_BREEDS` 品种定义和旧查询入口；育苗塘任务、自然繁衍进度、遗传随机调用、基因上下限和鱼塘存档结构仍由现有 Store/框架规则处理。
- 外部鱼塘品种 JSON 在校验前保持为 `unknown`，通过 `PondBreedDefSchema` / `pond-breed.schema.json` 后才允许注册。

#### 6.2.11 鱼塘设施定义契约

`taoyuan:fish_pond_facility` 描述鱼塘设施的建造成本、升级成本、各等级静态容量和满级无限容量语义，不包含水质衰减、喂食、疾病、死亡、育苗塘任务、自然繁衍、遗传随机、品质计算或鱼塘存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface FishPondFacilityMaterial {
  itemId: ContentId
  quantity: number
}

interface FishPondFacilityCost {
  money: number
  materials: FishPondFacilityMaterial[]
}

interface FishPondFacilityCapacity {
  level: 1 | 2 | 3
  capacity: number
}

interface FishPondFacilityUpgrade {
  level: 2 | 3
  capacity: number
  cost: FishPondFacilityCost
}

interface FishPondFacilityDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  buildCost: FishPondFacilityCost
  capacities: FishPondFacilityCapacity[]
  upgrades: FishPondFacilityUpgrade[]
  unlimitedAtLevel: 1 | 2 | 3 | null
}
```

- `id` 是鱼塘设施定义 ID；当前官方条目使用 `taoyuan:fish_pond`，运行时鱼塘状态和旧存档仍保存 `built`、`level`、`fish`、`waterQuality` 等旧字段。
- `buildCost.materials[].itemId` 和 `upgrades[].cost.materials[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `money` 必须是非负整数；`quantity`、`capacity` 必须是正整数；升级等级当前限定为 `2` 或 `3`，设施等级当前限定为 `1`、`2` 或 `3`。
- `capacities` 保留旧 `POND_CAPACITY` 的静态表语义，当前官方等级 1/2/3 分别为 5/10/20；运行时满级无限容量由 `unlimitedAtLevel: 3` 表达，旧 `POND_CAPACITY[3]` 仍保持 20 作为兼容静态值。
- 当前切片只迁移 `FISH_POND_FACILITY`、旧 `POND_BUILD_COST` / `POND_UPGRADE_COSTS` / `POND_CAPACITY` 兼容导出和建造/升级/容量查询入口；水质、疾病、死亡、喂食、清理、育苗塘任务、自然繁衍、遗传随机和鱼塘存档结构仍由现有 Store/框架规则处理。
- 外部鱼塘设施 JSON 在校验前保持为 `unknown`，通过 `FishPondFacilityDefSchema` / `fish-pond-facility.schema.json` 后才允许注册。

#### 6.2.12 建筑升级定义契约

`taoyuan:building_upgrade` 描述农舍、山洞和酒窖升级定义，不包含温室解锁、仓库解锁、山洞品质阈值、山洞每日结算算法、酒窖陈酿计时或存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface BuildingUpgradeMaterial {
  itemId: ContentId
  quantity: number
}

interface FarmhouseUpgradeDef {
  id: ContentId
  kind: 'farmhouse'
  level: 1 | 2 | 3
  name: LocalizedTextRef
  description: LocalizedTextRef
  cost: number
  materialCost: BuildingUpgradeMaterial[]
  benefit: string
}

interface CaveUpgradeDef {
  id: ContentId
  kind: 'cave'
  level: 1 | 2 | 3
  name: LocalizedTextRef
  mushroomChance: number
  fruitBatChance: number
  doubleChance: number
  cost: number
  materialCost: BuildingUpgradeMaterial[]
  mushroomPool: { itemId: ContentId; weight: number }[]
  fruitPool: ContentId[]
}

interface CellarUpgradeDef {
  id: ContentId
  kind: 'cellar'
  level: 1 | 2 | 3 | 4 | 5
  name: LocalizedTextRef
  valuePerCycle: number
  maxSlots: number
  cost: number
  materialCost: BuildingUpgradeMaterial[]
}
```

- `id` 当前按 `taoyuan:building_upgrade/{farmhouse|cave|cellar}/{level}` 生成；运行时 `farmhouseLevel`、`caveLevel`、`cellarLevel` 和旧存档仍保存原数字等级。
- `materialCost[].itemId`、山洞 `mushroomPool[].itemId` 和 `fruitPool[]` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `cost`、`valuePerCycle` 必须是非负整数；`quantity`、`maxSlots` 和山洞 `weight` 必须是正整数；山洞概率字段取值范围为 0 到 1。
- 当前切片只迁移 `FARMHOUSE_UPGRADES`、`CAVE_UPGRADES`、`CELLAR_UPGRADES`、旧类型/数组导出和 `getFarmhouseUpgrade()`、`getCaveUpgrade()`、`getCellarUpgrade()` 查询入口；温室升级空数组、温室/仓库解锁、山洞品质阈值、酒窖增值周期和日更算法仍由现有框架规则处理。
- 外部建筑升级 JSON 在校验前保持为 `unknown`，通过 `BuildingUpgradeDefSchema` / `building-upgrade.schema.json` 后才允许注册。

#### 6.2.13 钱袋物品定义契约

`taoyuan:wallet_item` 描述钱袋页面展示的永久被动物品定义，不包含解锁判定算法、被动效果执行逻辑、商店折扣叠加规则、存档结构或发放来源。公开 TypeBox 契约的等价形状为：

```ts
type WalletItemEffectType =
  | 'shopDiscount'
  | 'forageQuality'
  | 'miningStamina'
  | 'fishingCalm'
  | 'cookingRestore'
  | 'cropGrowth'
  | 'tradeBonus'

interface WalletItemDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  effect: {
    type: WalletItemEffectType
    value: number
  }
  unlockCondition: LocalizedTextRef
}
```

- `id` 当前继续使用旧本地钱包物品 ID 的 `taoyuan:*` 命名空间形式，例如 `taoyuan:merchant_seal`；运行时 `wallet.unlockedItems` 和旧存档仍保存本地 ID。
- `effect` 是被动效果描述符，当前切片只迁移既有展示和值；实际 `getShopDiscount()`、`getForageQualityBoost()`、`getMiningStaminaReduction()` 等固定 ID 查询仍属于 Store 框架规则。
- 当前切片只迁移 `WALLET_ITEMS`、`getWalletItemById()` 和新增列表查询入口；钱袋自动解锁条件、瀚海兑换发放、被动效果叠加和存档 JSON 均保持不变。
- 外部钱袋物品 JSON 在校验前保持为 `unknown`，通过 `WalletItemDefSchema` / `wallet-item.schema.json` 后才允许注册。

#### 6.2.14 秘密纸条定义契约

`taoyuan:secret_note` 描述秘密纸条的展示文本、类型、可使用状态和宝藏奖励，不包含获得概率、钓鱼/挖矿触发入口、使用状态存档结构或奖励发放 UI。公开 TypeBox 契约的等价形状为：

```ts
type SecretNoteType = 'tip' | 'treasure' | 'npc' | 'story'

interface SecretNoteRewardItem {
  itemId: ContentId
  quantity: number
}

interface SecretNoteDef {
  id: ContentId
  noteId: number
  type: SecretNoteType
  title: LocalizedTextRef
  content: LocalizedTextRef
  usable: boolean
  reward?: {
    money?: number
    items?: SecretNoteRewardItem[]
  }
}
```

- `id` 当前按 `taoyuan:secret_note/{noteId}` 生成，例如 `taoyuan:secret_note/1`；运行时 `secretNote.collectedNotes`、`secretNote.usedNotes` 和旧存档仍保存数字 `noteId`。
- `reward.items[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`；`quantity` 必须是正整数，`money` 必须是非负整数。
- 当前切片只迁移 `SECRET_NOTES`、秘密纸条 Store 的随机收集池/使用奖励读取路径，以及成就页面秘密纸条列表展示；钓鱼/挖矿调用 `tryCollectNote()` 的时机、随机调用次数、奖励发放顺序和存档 JSON 均保持不变。
- 外部秘密纸条 JSON 在校验前保持为 `unknown`，通过 `SecretNoteDefSchema` / `secret-note.schema.json` 后才允许注册。

#### 6.2.15 教程提示定义契约

`taoyuan:tutorial` 描述晨间教程提示的显示文本、稳定优先级和引擎已实现的触发条件键，不包含教程总开关、已显示提示、已访问面板、临时 flag 存档结构或条件判断函数本身。公开 TypeBox 契约的等价形状为：

```ts
type TutorialConditionKey =
  | 'earlyFirstDay'
  | 'allWasteland'
  | 'tilledNoPlanted'
  | 'plantedUnwatered'
  | 'hasHarvestable'
  | 'harvestedNeverSold'
  | 'earlyGame'
  | 'staminaWasLow'
  | 'neverVisitedShop'
  | 'neverFished'
  | 'neverMined'
  | 'neverTalkedNpc'
  | 'neverCheckedQuests'
  | 'neverCooked'
  | 'firstRainyDay'
  | 'justChangedSeason'
  | 'hasCropNoSprinkler'
  | 'neverHadAnimal'

interface TutorialDef {
  id: ContentId
  tipId: string
  priority: number
  conditionKey: TutorialConditionKey
  message: LocalizedTextRef
}
```

- `id` 当前按 `taoyuan:tutorial/{tipId}` 生成，例如 `taoyuan:tutorial/tip_welcome`；运行时 `tutorial.shownTipIds` 和旧存档仍保存旧本地 `tipId` 字符串。
- `conditionKey` 只能取当前 `processMorningTutorialEndDay()` 已实现的条件键；数据包不能通过教程定义注入脚本、表达式或新的条件函数。
- `priority` 必须是正整数；官方内容保持旧 `MORNING_TIPS` 的顺序和优先级，晨间结算仍只显示首个满足条件且未显示过的提示。
- 当前切片只迁移 `MORNING_TIPS` 和 `useEndDay.ts` 的晨间提示读取路径；教程开关、面板访问标记、临时 flag 清理、条件判断、日志写入和存档 JSON 均保持不变。
- 外部教程提示 JSON 在校验前保持为 `unknown`，通过 `TutorialDefSchema` / `tutorial.schema.json` 后才允许注册。

#### 6.2.16 博物馆定义契约

`taoyuan:museum_category`、`taoyuan:museum_item` 和 `taoyuan:museum_milestone` 描述博物馆页面的分类、可捐赠条目和捐赠数量里程碑奖励，不包含捐赠动作、奖励领取状态、背包扣料流程、成就或存档结构。公开 TypeBox 契约的等价形状为：

```ts
type MuseumCategoryKey = 'ore' | 'gem' | 'bar' | 'fossil' | 'artifact' | 'spirit'

interface MuseumCategoryDef {
  id: ContentId
  key: MuseumCategoryKey
  label: LocalizedTextRef
}

interface MuseumItemDef {
  id: ContentId
  itemId: ContentId
  name: LocalizedTextRef
  category: MuseumCategoryKey
  sourceHint: LocalizedTextRef
}

interface MuseumMilestoneRewardItem {
  itemId: ContentId
  quantity: number
}

interface MuseumMilestoneDef {
  id: ContentId
  count: number
  name: LocalizedTextRef
  reward: {
    money?: number
    items?: MuseumMilestoneRewardItem[]
  }
}
```

- `museum_category` 的 `id` 当前按 `taoyuan:museum_category/{key}` 生成，例如 `taoyuan:museum_category/ore`；运行时分类 key 仍使用旧本地字符串。
- `museum_item.id` 与 `museum_item.itemId` 当前均按旧可捐赠物品 ID 的 `taoyuan:*` 命名空间形式生成，例如 `taoyuan:copper_ore`；运行时 `museum.donatedItems` 和旧存档仍保存旧本地物品 ID。
- `museum_milestone.id` 当前按 `taoyuan:museum_milestone/{count}` 生成，例如 `taoyuan:museum_milestone/5`；运行时 `museum.claimedMilestones` 和旧存档仍保存数字捐赠数量。
- `museum_item.itemId` 和 `museum_milestone.reward.items[].itemId` 必须引用现有 `taoyuan:item`，`museum_item.category` 必须引用现有 `taoyuan:museum_category/{category}`；缺失时产生 `REG-REFERENCE-001`。
- 当前切片只迁移 `MUSEUM_CATEGORIES`、`MUSEUM_ITEMS`、`MUSEUM_MILESTONES`、博物馆 Store 查询/奖励读取路径和博物馆页面展示读取路径；捐赠扣料、里程碑领取、奖励发放顺序、旧存档 JSON 和 UI 文案保持不变。
- 外部博物馆 JSON 在校验前保持为 `unknown`，分别通过 `MuseumCategoryDefSchema` / `museum-category.schema.json`、`MuseumItemDefSchema` / `museum-item.schema.json` 和 `MuseumMilestoneDefSchema` / `museum-milestone.schema.json` 后才允许注册。

#### 6.2.17 行会定义契约

`taoyuan:guild_goal`、`taoyuan:guild_donation` 和 `taoyuan:guild_level` 描述冒险家公会的怪物讨伐目标、可捐献物品点数和等级经验阈值，不包含公会商店购买流程、贡献点余额、讨伐领取状态、被动加成算法或存档结构。公开 TypeBox 契约的等价形状为：

```ts
type GuildGoalZone = 'shallow' | 'frost' | 'lava' | 'crystal' | 'shadow' | 'abyss' | 'boss' | 'skull'

interface GuildGoalRewardItem {
  itemId: ContentId
  quantity: number
}

interface GuildGoalDef {
  id: ContentId
  monsterId: ContentId
  monsterName: LocalizedTextRef
  zone: GuildGoalZone
  killTarget: number
  reward: {
    money?: number
    items?: GuildGoalRewardItem[]
  }
  description: LocalizedTextRef
}

interface GuildDonationDef {
  id: ContentId
  itemId: ContentId
  points: number
}

interface GuildLevelDef {
  id: ContentId
  level: number
  expRequired: number
}
```

- `guild_goal.id` 当前按 `taoyuan:guild_goal/{monsterId}` 生成，例如 `taoyuan:guild_goal/mud_worm`；运行时 `monsterKills`、`claimedGoals` 和旧存档仍保存旧本地怪物 ID。
- `guild_donation.id` 当前按 `taoyuan:guild_donation/{itemId}` 生成，例如 `taoyuan:guild_donation/copper_ore`；运行时捐献入口和背包仍保存旧本地物品 ID。
- `guild_level.id` 当前按 `taoyuan:guild_level/{level}` 生成，例如 `taoyuan:guild_level/1`；运行时等级、经验和贡献点仍保存数字字段。
- `guild_goal.monsterId` 必须引用现有 `taoyuan:monster`，`guild_goal.reward.items[].itemId` 和 `guild_donation.itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- 当前切片只迁移 `MONSTER_GOALS`、`GUILD_DONATIONS`、`GUILD_LEVELS`、公会 Store 查询入口和行会页面展示/捐献读取路径；`GUILD_SHOP_ITEMS` 继续由既有 `taoyuan:shop_offer` 投影负责，`GUILD_BONUS_PER_LEVEL` 仍是框架规则。
- 讨伐奖励领取、捐献扣料、公会升级循环、贡献点和经验结算、旧存档补点迁移、商店限购、UI 文案、Electron、打包配置和存档路径均保持不变。
- 外部行会 JSON 在校验前保持为 `unknown`，分别通过 `GuildGoalDefSchema` / `guild-goal.schema.json`、`GuildDonationDefSchema` / `guild-donation.schema.json` 和 `GuildLevelDefSchema` / `guild-level.schema.json` 后才允许注册。

#### 6.2.18 NPC 定义契约

`taoyuan:npc` 描述桃源村普通 NPC 的显示文本、性别、角色、性格、礼物喜好、日常对话、可婚标记、兼容心事件 ID 和生日。不包含隐藏 NPC、心事件场景、剧情任务、普通任务、每日事件、求婚/生育/雇工算法、好感状态或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type Gender = 'male' | 'female'
type FriendshipLevel = 'stranger' | 'acquaintance' | 'friendly' | 'bestFriend'

interface NpcDef {
  id: ContentId
  name: LocalizedTextRef
  gender: Gender
  role: LocalizedTextRef
  personality: LocalizedTextRef
  lovedItems: ContentId[]
  likedItems: ContentId[]
  hatedItems: ContentId[]
  dialogues: Record<FriendshipLevel, LocalizedTextRef[]>
  marriageable?: boolean
  heartEventIds?: ContentId[]
  datingDialogues?: LocalizedTextRef[]
  zhijiDialogues?: LocalizedTextRef[]
  zhijiHeartEventIds?: ContentId[]
  birthday?: { season: Season; day: number }
}
```

- `id` 当前按 `taoyuan:npc/{legacyId}` 生成，例如 `taoyuan:npc/chen_bo`；运行时 `npcStates[].npcId`、配偶、知己、雇工和旧存档仍保存旧本地 NPC ID。
- `lovedItems`、`likedItems` 和 `hatedItems` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `heartEventIds` 和 `zhijiHeartEventIds` 在注册表内部使用 `taoyuan:heart_event/{legacyId}` 内容 ID，必须引用现有 `taoyuan:heart_event`，缺失时产生 `REG-REFERENCE-001`；兼容门面继续向 Store 和 UI 返回旧本地事件 ID。
- 当前切片只迁移普通 `NPCS`、`getNpcs()`、`getNpcById()`、NPC Store 初始化/补新增状态、NPC 页面列表和农舍日历生日读取路径；对话随机、聊天/送礼好感、求婚、知己、雇工、孕育、子女和旧存档 JSON 保持不变。
- 外部 NPC JSON 在校验前保持为 `unknown`，通过 `NpcDefSchema` / `npc.schema.json` 后才允许注册。

#### 6.2.18.1 好感事件定义契约

`taoyuan:heart_event` 描述普通 NPC 与隐藏 NPC 的好感事件场景、标题、触发好感阈值、知己要求和选项好感变化。不包含 NPC 好感数值、已触发事件存档、触发顺序算法、求婚/结缘副作用、婚礼框架事件或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface HeartEventSceneChoice {
  text: LocalizedTextRef
  friendshipChange: number
  response: LocalizedTextRef
}

interface HeartEventScene {
  text: LocalizedTextRef
  choices?: HeartEventSceneChoice[]
}

interface HeartEventDef {
  id: ContentId
  npcId: ContentId
  requiredFriendship: number
  requiresZhiji?: boolean
  title: LocalizedTextRef
  scenes: HeartEventScene[]
}
```

- `id` 当前按 `taoyuan:heart_event/{legacyId}` 生成，例如 `taoyuan:heart_event/qiu_yue_heart_3`；运行时 `triggeredHeartEvents`、普通 NPC/隐藏 NPC Store 和旧存档仍保存旧本地事件 ID。
- `npcId` 必须引用现有 `taoyuan:npc` 或 `taoyuan:hidden_npc`；普通 NPC 事件使用 `taoyuan:npc/{legacyNpcId}`，隐藏 NPC 事件使用 `taoyuan:hidden_npc/{legacyNpcId}`。缺失引用继续输出 `REG-REFERENCE-001`。
- 普通 NPC 的 `heartEventIds` / `zhijiHeartEventIds` 和隐藏 NPC 的 `heartEventIds` 必须引用现有 `taoyuan:heart_event`；兼容门面继续返回旧本地事件 ID 数组。
- `wedding_ceremony` 仍是由框架在运行时替换对象的婚礼事件，不进入 `taoyuan:heart_event` 官方注册表；旧 `getHeartEventById('wedding_ceremony')` 继续返回旧框架事件。
- 当前切片只迁移旧 `HEART_EVENTS`、`HIDDEN_NPC_HEART_EVENTS`、`getHeartEventsForNpc()`、`getHeartEventById()`、`getHiddenNpcHeartEvents()` 和 `getHiddenNpcHeartEventById()` 查询入口，以及普通/隐藏 NPC Store 的触发检查读取路径；好感数值、聊天/送礼、选项结算副作用、求婚/结缘流程和旧存档 JSON 保持不变。
- 外部好感事件 JSON 在校验前保持为 `unknown`，通过 `HeartEventDefSchema` / `heart-event.schema.json` 后才允许注册。

#### 6.2.18.2 隐藏 NPC 定义契约

`taoyuan:hidden_npc` 描述隐藏仙灵 NPC 的显示文本、发现步骤、供奉喜好、亲密对话、求缘/结缘材料、结缘被动、能力和显现生日。不包含心事件场景、每日事件、被动效果执行算法、亲密状态存档、求缘/结缘流程副作用或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type HiddenNpcDiscoveryPhase = 'unknown' | 'rumor' | 'glimpse' | 'encounter' | 'revealed'
type HiddenNpcAffinityLevel = 'wary' | 'curious' | 'trusting' | 'devoted' | 'eternal'
type HiddenNpcInteractionType = 'meditation' | 'music' | 'ritual' | 'dreamwalk' | 'cultivation'
type HiddenNpcPassive = {
  type: 'quality_boost' | 'stamina_save' | 'exp_boost' | 'sell_bonus' | 'luck' | 'max_stamina' | 'max_hp'
  value: number
}
type HiddenNpcBondBonus =
  | { type: 'weather_control'; chance: number }
  | { type: 'crop_blessing'; chance: number }
  | { type: 'animal_blessing'; chance: number }
  | { type: 'stamina_restore'; amount: number }
  | { type: 'fish_attraction'; chance: number }
  | { type: 'spirit_shield'; staminaSave: number; hpBonus: number }
  | { type: 'sell_bonus'; percent: number }

type HiddenNpcDiscoveryCondition =
  | { type: 'season'; season: Season }
  | { type: 'weather'; weather: Weather }
  | { type: 'timeRange'; minHour: number; maxHour: number }
  | { type: 'location'; panel: string }
  | { type: 'item'; itemId: ContentId; quantity?: number }
  | { type: 'skill'; skillType: SkillType; minLevel: number }
  | { type: 'npcFriendship'; npcId: ContentId; minFriendship: number }
  | { type: 'questComplete'; questId: ContentId }
  | { type: 'mineFloor'; minFloor: number }
  | { type: 'fishCaught'; fishId: ContentId }
  | { type: 'money'; minAmount: number }
  | { type: 'yearMin'; year: number }
  | { type: 'day'; day: number }

interface HiddenNpcDef {
  id: ContentId
  name: LocalizedTextRef
  trueName: LocalizedTextRef
  gender: Gender
  title: LocalizedTextRef
  origin: LocalizedTextRef
  personality: LocalizedTextRef
  discoverySteps: {
    id: string
    phase: HiddenNpcDiscoveryPhase
    conditions: HiddenNpcDiscoveryCondition[]
    scenes: { text: LocalizedTextRef; choices?: { text: LocalizedTextRef; friendshipChange: number; response: LocalizedTextRef }[] }[]
    logMessage?: LocalizedTextRef
  }[]
  resonantOfferings: ContentId[]
  pleasedOfferings: ContentId[]
  repelledOfferings: ContentId[]
  dialogues: Record<HiddenNpcAffinityLevel, LocalizedTextRef[]>
  interactionType: HiddenNpcInteractionType
  bondable: boolean
  courtshipItemId: ContentId
  bondItemId: ContentId
  courtshipThreshold: number
  bondThreshold: number
  heartEventIds: ContentId[]
  courtshipDialogues: LocalizedTextRef[]
  bondBonuses: HiddenNpcBondBonus[]
  abilities: { id: string; affinityRequired: number; name: LocalizedTextRef; description: LocalizedTextRef; passive?: HiddenNpcPassive }[]
  courtshipCraftCost: { itemId: ContentId; quantity: number }[]
  bondCraftCost: { itemId: ContentId; quantity: number }[]
  manifestationDay: { season: Season; day: number }
}
```

- `id` 当前按 `taoyuan:hidden_npc/{legacyId}` 生成，例如 `taoyuan:hidden_npc/long_ling`；运行时 `hiddenNpcStates[].npcId`、已触发心事件、已解锁能力和旧存档仍保存旧本地隐藏 NPC ID。
- 供奉物品、求缘/结缘物品、制造材料和 `item` 发现条件必须引用现有 `taoyuan:item`；`npcFriendship.npcId` 必须引用现有 `taoyuan:npc`；`questComplete.questId` 必须引用现有 `taoyuan:story_quest`；`fishCaught.fishId` 必须引用现有 `taoyuan:fish`。缺失引用继续输出 `REG-REFERENCE-001`。
- `heartEventIds` 在注册表内部使用 `taoyuan:heart_event/{legacyId}` 内容 ID，必须引用现有 `taoyuan:heart_event`，缺失时产生 `REG-REFERENCE-001`；兼容门面继续向 Store 和 UI 返回旧本地事件 ID。
- 当前切片只迁移旧 `HIDDEN_NPCS`、`getHiddenNpcs()`、`getHiddenNpcById()`、隐藏 NPC Store 初始化/旧存档补新增状态、发现条件、供奉、能力解锁、每日结缘被动和心事件触发前的隐藏 NPC ID 判断读取路径；亲密数值、物品扣除、被动效果执行、求缘/结缘副作用和旧存档 JSON 保持不变。
- 外部隐藏 NPC JSON 在校验前保持为 `unknown`，通过 `HiddenNpcDefSchema` / `hidden-npc.schema.json` 后才允许注册。

#### 6.2.18.3 剧情任务定义契约

`taoyuan:story_quest` 描述主线剧情任务的章节、顺序、显示文本、发布 NPC、目标列表和奖励。不包含任务接取/提交副作用、目标进度存档、任务面板状态、普通任务、心事件、季节节日事件、NPC 好感算法或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type StoryQuestObjective =
  | { type: 'earnMoney'; label: LocalizedTextRef; target: number }
  | { type: 'reachMineFloor'; label: LocalizedTextRef; target: number }
  | { type: 'reachSkullFloor'; label: LocalizedTextRef; target: number }
  | { type: 'allSkillsLevel'; label: LocalizedTextRef; target: number }
  | { type: 'harvestCrops'; label: LocalizedTextRef; target: number }
  | { type: 'catchFish'; label: LocalizedTextRef; target: number }
  | { type: 'cookRecipes'; label: LocalizedTextRef; target: number }
  | { type: 'killMonsters'; label: LocalizedTextRef; target: number }
  | { type: 'discoverItems'; label: LocalizedTextRef; target: number }
  | { type: 'completeBundles'; label: LocalizedTextRef; target: number }
  | { type: 'completeQuests'; label: LocalizedTextRef; target: number }
  | { type: 'shipItems'; label: LocalizedTextRef; target: number }
  | { type: 'ownAnimals'; label: LocalizedTextRef; target: number }
  | { type: 'skillLevel'; label: LocalizedTextRef; skillType?: SkillType; target: number }
  | { type: 'npcFriendship'; label: LocalizedTextRef; npcId: ContentId | '_any'; friendshipLevel: FriendshipLevel }
  | { type: 'npcAllFriendly'; label: LocalizedTextRef; friendshipLevel: FriendshipLevel }
  | { type: 'deliverItem'; label: LocalizedTextRef; itemId: ContentId; itemQuantity: number }
  | { type: 'married'; label: LocalizedTextRef }
  | { type: 'hasChild'; label: LocalizedTextRef }

interface StoryQuestDef {
  id: ContentId
  chapter: number
  order: number
  title: LocalizedTextRef
  description: LocalizedTextRef
  npcId: ContentId
  objectives: StoryQuestObjective[]
  moneyReward: number
  friendshipReward?: { npcId: ContentId; amount: number }[]
  itemReward?: { itemId: ContentId; quantity: number }[]
}
```

- `id` 当前按 `taoyuan:story_quest/{legacyId}` 生成，例如 `taoyuan:story_quest/main_1_1`；运行时 `mainQuest.questId`、`completedMainQuests` 和旧存档仍保存旧本地任务 ID。
- `skillLevel.skillType` 可省略；省略时保持旧任务“任意技能达到目标等级”的语义，不得默认成农耕或其他单一技能。
- `npcId`、`npcFriendship.npcId` 和 `friendshipReward[].npcId` 必须引用现有 `taoyuan:npc`，但 `npcFriendship.npcId: "_any"` 保留旧任意 NPC 语义；`deliverItem.itemId` 和 `itemReward[].itemId` 必须引用现有 `taoyuan:item`。缺失引用继续输出 `REG-REFERENCE-001`。
- 当前切片只迁移 `STORY_QUESTS`、`getStoryQuests()`、`getStoryQuestCount()`、`getStoryQuestById()`、`getStoryQuestByOrder()`、`getNextStoryQuest()`、`getChapterQuests()`、`getFirstStoryQuest()` 和主线任务 Store 查询入口；接取、进度判断、提交奖励、下一任务推进、UI 文案、章节标题和旧存档 JSON 保持不变。
- 外部剧情任务 JSON 在校验前保持为 `unknown`，通过 `StoryQuestDefSchema` / `story-quest.schema.json` 后才允许注册。

#### 6.2.18.4 季节节日事件定义契约

`taoyuan:season_event` 描述固定季节与日期触发的节日事件定义，包括显示文本、奖励效果、旁白和互动节日类型。不包含隔夜触发流程、奖励发放副作用、食谱解锁、BGM/弹窗、晨间随机事件、普通任务、市场、瀚海或旅行商人逻辑。公开 TypeBox 契约的等价形状为：

```ts
type SeasonEventFestivalType =
  | 'fishing_contest'
  | 'harvest_fair'
  | 'dragon_boat'
  | 'lantern_riddle'
  | 'pot_throwing'
  | 'dumpling_making'
  | 'firework_show'
  | 'tea_contest'
  | 'kite_flying'

interface SeasonEventDef {
  id: ContentId
  eventId: string
  name: LocalizedTextRef
  season: Season
  day: number
  description: LocalizedTextRef
  effects: {
    friendshipBonus?: number
    moneyReward?: number
    itemReward?: { itemId: ContentId; quantity: number }[]
    staminaBonus?: number
  }
  narrative: LocalizedTextRef[]
  interactive?: boolean
  festivalType?: SeasonEventFestivalType
}
```

- `id` 当前按 `taoyuan:season_event/{legacyId}` 生成，例如 `taoyuan:season_event/summer_lantern`；运行时事件弹窗、节日小游戏入口和农舍日历继续使用旧本地 `eventId` / `id` 字符串。
- `effects.itemReward[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`；`friendshipBonus`、`moneyReward` 和 `staminaBonus` 仍由现有隔夜结算规则解释。
- `festivalType` 只能取当前 `showFestival()` 已支持的互动节日类型；数据包不能通过节日事件定义注入脚本、小游戏实现或新的副作用。
- 当前切片只迁移 `SEASON_EVENTS`、`getTodayEvent()`、隔夜节日事件读取和农舍日历节日展示读取路径；节日奖励结算、食谱解锁、BGM/弹窗、`{year}` 文案替换和旧存档 JSON 保持不变。
- 外部季节节日事件 JSON 在校验前保持为 `unknown`，通过 `SeasonEventDefSchema` / `season-event.schema.json` 后才允许注册。

#### 6.2.18.5 普通任务模板定义契约

`taoyuan:quest_template` 描述公告板普通委托模板和特殊订单模板。不包含任务实例生成计数器、任务接取/提交副作用、任务进度存档、任务面板状态、NPC 好感结算或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type QuestTemplateType = 'delivery' | 'fishing' | 'mining' | 'gathering'

interface BoardQuestTemplateTarget {
  itemId: ContentId
  name: LocalizedTextRef
  minQty: number
  maxQty: number
  seasons: Season[]
  unitPrice: number
}

interface BoardQuestTemplateDef {
  id: ContentId
  templateId: string
  kind: 'board'
  type: QuestTemplateType
  targets: BoardQuestTemplateTarget[]
  npcPool: ContentId[]
  rewardMultiplier: number
  friendshipReward: number
}

interface SpecialOrderTemplateDef {
  id: ContentId
  templateId: string
  kind: 'special_order'
  name: LocalizedTextRef
  targetItemId: ContentId
  targetItemName: LocalizedTextRef
  quantity: number
  days: number
  moneyReward: number
  itemReward: { itemId: ContentId; quantity: number }[]
  seasons: Season[]
  npcId: ContentId
  tier: number
}

type QuestTemplateDef = BoardQuestTemplateDef | SpecialOrderTemplateDef
```

- `id` 当前按 `taoyuan:quest_template/board/{type}` 或 `taoyuan:quest_template/special_order/tier_{tier}/{targetItemId}_{npcId}` 生成；运行时公告板任务和特殊订单实例继续保存旧本地 `targetItemId`、`npcId` 和任务实例 ID。
- `templateId` 是稳定的本地兼容模板 ID，用于注册表往返后重建旧 `QuestTemplateDef` / `SpecialOrderTemplate` 对象；`getOfficialQuestTemplateDef()` 接受完整内容 ID、`quest_template/...`、`board/...` 和已知公告板类型作为兼容查询输入。
- `targets[].itemId`、`targetItemId` 和 `itemReward[].itemId` 必须引用现有 `taoyuan:item`；`npcPool[]` 和 `npcId` 必须引用现有 `taoyuan:npc`。缺失引用继续输出 `REG-REFERENCE-001`。
- 当前切片只迁移 `QUEST_TEMPLATES`、`SPECIAL_ORDER_TEMPLATES`、`getQuestTemplates()`、`getSpecialOrderTemplates()`、`generateQuest()` 和 `generateSpecialOrder()` 的模板读取路径；随机调用顺序、奖励计算、任务描述、接取/提交副作用和旧存档 JSON 保持不变。
- 外部任务模板 JSON 在校验前保持为 `unknown`，通过 `QuestTemplateDefSchema` / `quest-template.schema.json` 后才允许注册。

#### 6.2.18.6 晨间随机事件定义契约

`taoyuan:morning_event` 描述隔夜结算后可能展示的晨间旁白、带选项事件和彩蛋事件定义。不包含触发概率、随机抽取顺序、作物破坏算法、选择弹窗流程、物品/金钱/好感发放副作用、日志写入或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type MorningEventEffect =
  | { type: 'loseCrop' }
  | { type: 'gainItem'; itemId: ContentId; qty: number }
  | { type: 'gainMoney'; amount: number }
  | { type: 'gainFriendship'; amount: number }

interface MorningEventChoice {
  label: LocalizedTextRef
  result: LocalizedTextRef
  effect?: MorningEventEffect
}

interface MorningNarrationEventDef {
  id: ContentId
  eventId: string
  kind: 'narration'
  message: LocalizedTextRef
  effect?: MorningEventEffect
}

interface MorningChoiceEventDef {
  id: ContentId
  eventId: string
  kind: 'choice'
  message: LocalizedTextRef
  choices: MorningEventChoice[]
}

interface MorningEasterEggEventDef {
  id: ContentId
  eventId: string
  kind: 'easter_egg'
  message: LocalizedTextRef
  effect?: MorningEventEffect
}

type MorningEventDef =
  | MorningNarrationEventDef
  | MorningChoiceEventDef
  | MorningEasterEggEventDef
```

- `id` 当前按 `taoyuan:morning_event/narration/{index}`、`taoyuan:morning_event/choice/{legacyId}` 或 `taoyuan:morning_event/easter_egg/{index}` 生成；隔夜结算和选择弹窗继续使用旧本地事件形状。
- `eventId` 是稳定的本地兼容事件 ID，用于注册表往返后重建旧 `MorningNarration`、`MorningChoiceEvent` 和 `MorningEasterEgg` 对象；兼容查询接受完整内容 ID 或 `morning_event/...` 形式。
- `gainItem.itemId` 必须引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`；`loseCrop`、`gainMoney` 和 `gainFriendship` 的实际副作用仍由现有晨间事件处理流程解释。
- 当前切片只迁移 `MORNING_NARRATIONS`、`NARRATIONS_NO_LOSS`、`MORNING_CHOICE_EVENTS`、`MORNING_EASTER_EGGS` 和 `useEndDay.ts` 的晨间事件输入读取路径；触发概率、随机调用顺序、作物/背包/金钱/NPC 状态修改和旧存档 JSON 保持不变。
- 外部晨间随机事件 JSON 在校验前保持为 `unknown`，通过 `MorningEventDefSchema` / `morning-event.schema.json` 后才允许注册。

#### 6.2.19 成就与祠堂任务定义契约

`taoyuan:achievement` 描述可完成成就的显示文本、稳定条件和奖励；`taoyuan:community_bundle` 描述祠堂任务的提交物品与奖励。不包含进度统计、奖励发放副作用、提交状态、完成状态、成就弹窗或旧存档结构。公开 TypeBox 契约的等价形状为：

```ts
type AchievementCondition =
  | { type: 'itemCount'; count: number }
  | { type: 'cropHarvest'; count: number }
  | { type: 'fishCaught'; count: number }
  | { type: 'moneyEarned'; amount: number }
  | { type: 'mineFloor'; floor: number }
  | { type: 'skullCavernFloor'; floor: number }
  | { type: 'recipesCooked'; count: number }
  | { type: 'npcFriendship'; level: string }
  | { type: 'skillLevel'; skillType: SkillType; level: number }
  | { type: 'questsCompleted'; count: number }
  | { type: 'npcBestFriend'; count: number }
  | { type: 'npcAllFriendly' }
  | { type: 'married' }
  | { type: 'hasChild' }
  | { type: 'monstersKilled'; count: number }
  | { type: 'shippedCount'; count: number }
  | { type: 'fullShipment' }
  | { type: 'animalCount'; count: number }
  | { type: 'allSkillsMax' }
  | { type: 'allBundlesComplete' }
  | { type: 'hybridsDiscovered'; count: number }
  | { type: 'breedingsDone'; count: number }
  | { type: 'hybridTier'; tier: number }
  | { type: 'hybridsShipped'; count: number }
  | { type: 'museumDonations'; count: number }
  | { type: 'guildGoalsCompleted'; count: number }
  | { type: 'hiddenNpcRevealed'; count: number }
  | { type: 'hiddenNpcBonded' }
  | { type: 'itemDiscovered'; itemId: ContentId }

interface AchievementRewardItem {
  itemId: ContentId
  quantity: number
}

interface AchievementDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  condition: AchievementCondition
  reward: {
    money?: number
    items?: AchievementRewardItem[]
  }
}

interface CommunityBundleDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  requiredItems: { itemId: ContentId; quantity: number }[]
  reward: {
    money?: number
    items?: AchievementRewardItem[]
    description: LocalizedTextRef
  }
}
```

- `id` 当前分别使用 `taoyuan:achievement/<legacyId>` 和 `taoyuan:community_bundle/<legacyId>`；运行时 `completedAchievements`、`completedBundles` 和 `bundleSubmissions` 仍保存旧本地 ID。
- `itemDiscovered.itemId`、成就奖励物品、祠堂任务提交物品和祠堂奖励物品必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `condition.type` 只能取当前 `useAchievementStore` 已实现的条件键；数据包不能通过成就定义注入脚本、表达式或新的统计函数。
- 当前切片只迁移 `ACHIEVEMENTS`、`COMMUNITY_BUNDLES`、成就 Store 查询入口和成就页面展示/祠堂提交读取路径；进度统计、奖励发放顺序、提交扣料、旧存档 JSON 和 UI 文案保持不变。
- 外部成就和祠堂任务 JSON 在校验前保持为 `unknown`，分别通过 `AchievementDefSchema` / `achievement.schema.json` 和 `CommunityBundleDefSchema` / `community-bundle.schema.json` 后才允许注册。

#### 6.2.20 农场地图定义契约

`taoyuan:farm_map` 描述开局田庄选择界面展示的地图定义，不包含地图加成算法、每日结算、初始赠送、农场地块生成规则或存档结构。公开 TypeBox 契约的等价形状为：

```ts
type FarmMapType =
  | 'standard'
  | 'riverland'
  | 'forest'
  | 'hilltop'
  | 'wilderness'
  | 'meadowlands'

interface FarmMapDef {
  id: ContentId
  type: FarmMapType
  name: LocalizedTextRef
  description: LocalizedTextRef
  bonus: LocalizedTextRef
}
```

- `id` 当前继续使用旧本地农场地图类型的 `taoyuan:*` 命名空间形式，例如 `taoyuan:standard`；运行时 `game.farmMapType` 和旧存档仍保存本地 `FarmMapType` 字符串。
- `type` 只能取当前框架已实现的 6 种官方地图类型；新增地图类型需要先实现对应地图规则和存档兼容，不能只注册显示文本。
- 当前切片只迁移 `FARM_MAP_DEFS`、新增 `getFarmMapDefs()` 和 `getFarmMapByType()` 查询入口以及开局农场选择 UI 读取路径；溪流鱼获、竹林拾遗、山丘矿脉、荒野遭遇、草甸鸡舍赠送和标准田庄肥沃土壤等算法仍由现有 Store/框架规则处理。
- 外部农场地图 JSON 在校验前保持为 `unknown`，通过 `FarmMapDefSchema` / `farm-map.schema.json` 后才允许注册。

#### 6.2.21 加工机器制造定义契约

`taoyuan:processing_machine` 描述可制造并放置的加工机器定义，不包含加工配方、机器运行队列、每日结算、自动收取算法或存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface ProcessingMachineMaterial {
  itemId: ContentId
  quantity: number
}

interface ProcessingMachineDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  craftCost: ProcessingMachineMaterial[]
  craftMoney: number
  autoCollect?: boolean
}
```

- `id` 当前继续使用旧本地加工机器 ID 的 `taoyuan:*` 命名空间形式，例如 `taoyuan:furnace`；运行时加工槽位和旧存档仍保存本地 `machineType`。
- `craftCost[].itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`；`quantity` 必须是正整数，`craftMoney` 必须是非负整数。
- `autoCollect` 只表达旧定义中的完成后自动收取标记；自动收取时机、产物投递、虚空箱回退和日更流程仍由现有 Store/框架规则处理。
- 当前切片只迁移 `PROCESSING_MACHINES` 到 `processingMachineDefinitions.ts` 唯一叶子来源、`getMachineById()` / `getProcessingMachines()` 查询入口，以及加工 Store 和加工页面的机器定义读取路径；加工配方定义已由后续 `taoyuan:processing_recipe` 切片单独迁移，酒坊/种子制造机多任务逻辑、加工队列、制造消耗顺序、退款、配方解锁和存档 JSON 仍保持不变。
- 外部加工机器 JSON 在校验前保持为 `unknown`，通过 `ProcessingMachineDefSchema` / `processing-machine.schema.json` 后才允许注册。

#### 6.2.22 加工配方定义契约

`taoyuan:processing_recipe` 描述某台加工机器可执行的配方定义，不包含加工槽位、运行队列、自动收取时机、虚空箱回退、多任务酒坊/种子制造机算法、配方解锁或存档结构。公开 TypeBox 契约的等价形状为：

```ts
type ProcessingRecipeDef =
  | {
      id: ContentId
      machineId: ContentId
      name: LocalizedTextRef
      inputItemId: ContentId
      inputQuantity: number
      outputItemId: ContentId
      outputQuantity: number
      processingDays: number
      description: LocalizedTextRef
    }
  | {
      id: ContentId
      machineId: ContentId
      name: LocalizedTextRef
      inputItemId: null
      inputQuantity: 0
      outputItemId: ContentId
      outputQuantity: number
      processingDays: number
      description: LocalizedTextRef
    }
```

- `id` 当前继续使用旧本地加工配方 ID 的 `taoyuan:*` 命名空间形式，例如 `taoyuan:smelt_copper`、`taoyuan:honey`；运行时加工槽位和旧存档仍保存本地 `recipeId`。
- `machineId` 必须引用现有 `taoyuan:processing_machine`，缺失时产生 `REG-REFERENCE-001`；兼容层继续把它还原为旧 `machineType` 字段。
- `inputItemId` 为 `ContentId` 时必须引用现有 `taoyuan:item`，且 `inputQuantity` 必须是正整数；`inputItemId: null` 只允许搭配 `inputQuantity: 0`，用于蜂箱、蚯蚓箱等旧无输入配方。
- `outputItemId` 必须引用现有 `taoyuan:item`，`outputQuantity` 与 `processingDays` 必须是正整数。
- 当前切片迁移 `PROCESSING_RECIPES` 到 `processingRecipeDefinitions.ts` 唯一叶子来源、`getProcessingRecipeById()` / `getRecipesForMachine()` 查询入口和官方注册表投影；输入扣料、品质继承、自动收取、无输入配方自动重启、虚空箱、酒坊/种子制造机多任务、配方解锁和旧存档结构保持不变。
- 外部加工配方 JSON 在校验前保持为 `unknown`，通过 `ProcessingRecipeDefSchema` / `processing-recipe.schema.json` 后才允许注册。

#### 6.2.23 单件装备定义契约

`taoyuan:equipment` 描述单件装备定义。当前阶段 6 已迁移戒指、帽子、鞋子和武器；戒指、帽子、鞋子使用可穿戴装备分支，武器使用同一注册表中的 `kind: "weapon"` 分支。公开 TypeBox 契约的等价形状为：

```ts
interface EquipmentRecipeIngredient {
  itemId: ContentId
  quantity: number
}

interface WearableEquipmentDef {
  id: ContentId
  kind: 'ring' | 'hat' | 'shoe'
  name: LocalizedTextRef
  description: LocalizedTextRef
  effects: EquipmentEffect[]
  shopPrice?: number | null
  recipe: EquipmentRecipeIngredient[] | null
  recipeMoney: number
  obtainSource: LocalizedTextRef
  sellPrice: number
}

interface WeaponEquipmentDef {
  id: ContentId
  kind: 'weapon'
  name: LocalizedTextRef
  description: LocalizedTextRef
  weaponType: 'sword' | 'dagger' | 'club'
  attack: number
  critRate: number
  shopPrice: number | null
  shopMaterials: EquipmentRecipeIngredient[]
  fixedEnchantment: ContentId | null
  sellPrice: number
}

type EquipmentDef = WearableEquipmentDef | WeaponEquipmentDef
```

- `id` 当前继续使用旧本地装备 ID 的 `taoyuan:*` 命名空间形式，例如 `taoyuan:jade_guard_ring`、`taoyuan:straw_hat`、`taoyuan:straw_sandals`、`taoyuan:copper_sword`；运行时拥有的武器/戒指/帽子/鞋子实例和旧存档仍保存本地 `defId`。
- `effects` 只能使用引擎已支持的 `EquipmentEffect` 类型；效果叠加、好感、掉落率、体力消耗和战斗算法仍由现有 Store/domain 规则处理。
- 可穿戴装备的 `shopPrice` 仅在存在旧购买价格的装备上写入；缺失或 `null` 表示不能直接购买，旧戒指定义不会因此新增购买价格。`recipe` 为 `null` 表示不可合成；存在时每个 `itemId` 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- 武器的 `shopPrice` 保留旧定义中的 `number | null` 形状；`shopMaterials` 中每个 `itemId` 必须引用现有 `taoyuan:item`，`fixedEnchantment` 非空时必须引用现有 `taoyuan:enchantment`，缺失时产生 `REG-REFERENCE-001`。
- 当前切片迁移 `RINGS` 到 `ringDefinitions.ts` 叶子来源、`HATS` 到 `hatDefinitions.ts` 叶子来源、`SHOES` 到 `shoeDefinitions.ts` 叶子来源、`WEAPONS` 到 `weaponDefinitions.ts` 叶子来源、`getRingById()` / `getHatById()` / `getShoeById()` / `getWeaponById()` 查询入口和官方注册表投影；`CRAFTABLE_*`、商店派生表、怪物/宝箱/BOSS 掉落执行、合成流程、附魔生成、装备方案和装备存档结构保持不变。
- 外部单件装备 JSON 在校验前保持为 `unknown`，通过 `EquipmentDefSchema` / `equipment.schema.json` 后才允许注册。

#### 6.2.24 装备套装定义契约

`taoyuan:equipment_set` 描述装备套装的件位组成与激活奖励，不包含单件武器、戒指、帽子、鞋子的定义、合成、掉落、附魔算法、装备方案或存档结构。公开 TypeBox 契约的等价形状为：

```ts
interface EquipmentSetPieces {
  weapon?: ContentId
  ring: ContentId
  hat: ContentId
  shoe: ContentId
}

interface EquipmentSetBonus {
  count: 2 | 3 | 4
  effects: EquipmentEffect[]
  description: LocalizedTextRef
}

interface EquipmentSetDef {
  id: ContentId
  name: LocalizedTextRef
  description: LocalizedTextRef
  pieces: EquipmentSetPieces
  bonuses: EquipmentSetBonus[]
}
```

- `id` 当前继续使用旧本地装备套装 ID 的 `taoyuan:*` 命名空间形式，例如 `taoyuan:miner_set`；运行时装备和旧存档仍保存本地装备定义 ID。
- `pieces.weapon` 可省略，`ring`、`hat` 和 `shoe` 为必填；所有件位 ID 必须引用现有 `taoyuan:item`，缺失时产生 `REG-REFERENCE-001`。
- `bonuses[].count` 当前限定为 2、3 或 4；`effects` 只能使用引擎已支持的 `EquipmentEffect` 类型，奖励叠加和装备基础属性计算仍由现有库存/装备规则处理。
- 当前切片只迁移 `EQUIPMENT_SETS` 到 `EQUIPMENT_SET_DEFINITIONS` 唯一叶子来源、`getEquipmentSets()` 和 `getSetByPieceId()` 查询入口，以及背包 Store 的套装激活计算读取路径；单件装备定义、装备获取、合成、怪物/宝箱掉落、附魔生成和装备存档结构均保持不变。
- 外部装备套装 JSON 在校验前保持为 `unknown`，通过 `EquipmentSetDefSchema` / `equipment-set.schema.json` 后才允许注册。

### 6.3 冲突策略

默认策略如下：

1. 同一数据包在同一注册表内重复 ID：拒绝加载该数据包。
2. 不同数据包在同一注册表内使用同一完整 ID：拒绝后加载的数据包。
3. 未声明依赖却引用其他数据包内容：拒绝加载。
4. 官方内容 ID：第三方数据包默认不得覆盖。
5. 将来如支持覆盖，必须由清单显式声明，并在模组管理界面警告。

冲突检查以 `(registryId, contentId)` 为键；不同注册表可以拥有相同 `contentId`。注册表类型 ID 自身必须全局唯一，第三方插件不得定义 `taoyuan:*` 注册表。

### 6.4 引用解析

完整生命周期采用“先定义注册表类型，再注册内容条目”的顺序：

1. 引擎定义官方注册表类型。
2. 后续获得授权的事件插件定义私有注册表类型，机制插件定义自有公开注册表类型。
3. 冻结注册表类型目录，不再接受新的注册表定义。
4. 官方内容包和第三方数据包注册原始条目，此时不要求被引用项已经出现。
5. 所有数据包注册完成后，统一检查物品、标签、配方材料、掉落表、附魔、加工机器、单件装备、装备套装、行会目标/捐献、成就/祠堂任务、季节节日事件奖励和商店商品引用。
6. 冻结所有注册表内容，再创建 Store、读取存档并挂载 Vue。

这可以支持跨文件和跨依赖包引用，同时避免加载顺序偶然影响校验结果。

### 6.5 内容编译器

当前内置数据存在自动生成内容的逻辑。迁移后由内容编译器负责：

- 根据作物定义生成种子物品和收获物品。
- 根据烹饪配方生成成品物品。
- 根据加工配方生成加工品目录项。
- 建立按 ID 查询的 Map 和分类索引。
- 生成只读、稳定排序的运行时目录。

派生项必须有确定的 ID 规则，并检查是否与显式内容冲突。相同输入应产生相同输出，不得在编译阶段使用随机数。

### 6.6 标签化材料与配方选择器

物品标签用于表达烹饪和加工语义，例如“素食”“肉类”“蛋白质”“水果类”。标签不替代现有 `category`：分类继续服务于背包分组、出售和 UI，一个物品只有一个主要分类；标签是可扩展的多值集合，一个物品可以同时属于多个标签。

官方标签使用命名空间 ID，例如：

```text
taoyuan:vegetarian
taoyuan:meat
taoyuan:protein
taoyuan:fruit
```

第三方物品若希望被官方标签配方识别，应显式声明对应官方标签。数据包也可以注册自有标签，但引用其他数据包标签时必须声明依赖。标签定义、显示名称和引用关系由 `TagRegistry` 管理。

物品定义通过可选标签列表保持对现有内容的向后兼容：

```ts
interface ItemDef {
  id: ContentId
  tags?: ContentTagId[]
}
```

配方材料采用可辨识联合类型，保留现有固定物品材料，并增加标签和多标签择一材料：

```ts
type RecipeIngredient =
  | {
      type: 'item'
      itemId: ContentId
      quantity: number
    }
  | {
      type: 'tag'
      tagId: ContentTagId
      quantity: number
    }
  | {
      type: 'anyOfTags'
      tagIds: ContentTagId[]
      quantity: number
    }
```

旧配方中没有 `type`、只有 `itemId + quantity` 的材料，在内容标准化阶段按 `type: 'item'` 读取，避免为现有配方制造一次性破坏性迁移。

“包子”可以表达为：

```json
{
  "id": "taoyuan:steamed_bun",
  "ingredients": [
    {
      "type": "item",
      "itemId": "taoyuan:wheat_flour",
      "quantity": 1
    },
    {
      "type": "anyOfTags",
      "tagIds": ["taoyuan:vegetarian", "taoyuan:meat"],
      "quantity": 1
    }
  ]
}
```

标签配方不能分别统计每个材料槽后直接相加。同一物品可能同时拥有多个标签，例如豆腐可同时属于“素食”和“蛋白质”；同一份豆腐不能被两个材料槽重复占用。应在 `domain/` 中建立纯材料分配器，统一生成原子扣料计划：

```text
配方选择器 + 当前库存 + 制作数量
                ↓
        IngredientAllocationPlan
                ↓
具体扣除的物品、品质和数量
```

`canCook()`、最大可制作数量、品质预览和实际扣料必须复用同一规划入口。规划失败时不得扣除部分材料；批量制作必须一次生成完整计划。默认自动选料顺序应稳定，并优先使用低品质、低价值材料；烹饪弹窗应展示标签槽实际选择的物品，并允许玩家在执行前更换。成品品质继续取实际消耗材料中的最低品质。

#### 制品组成标签与堆叠

标签可以声明是否传播到制品，以及是否影响堆叠：

```ts
interface TagDef {
  id: ContentTagId
  propagateToCraftedOutput?: boolean
  stackPolicy?: 'merge' | 'separate'
}

interface CraftedItemStack {
  itemId: ContentId
  quantity: number
  quality: Quality
  compositionTags?: ContentTagId[]
}
```

配方仍产出一个基础物品定义，例如 `taoyuan:steamed_bun`，但每批产物根据实际消耗材料生成带 `compositionTags` 的运行时变体。只有 `propagateToCraftedOutput: true` 的材料标签会传播，避免把商店分类、内部索引等技术标签带入成品。

堆叠签名由以下字段组成：

```text
基础 itemId + 品质 + 所有 stackPolicy=separate 的组成标签（稳定排序）
```

`stackPolicy` 默认为 `merge`。两个制品的普通组成标签不同仍可堆叠；合并后 `compositionTags` 取去重并集，直接丢失每份制品的具体材料来源和逐份标签分布。拆分该堆物品时，拆出的各部分都继承合并后的标签并集，不尝试恢复已经丢失的来源。

`stackPolicy: separate` 的特殊标签必须参与堆叠签名。任一特殊标签集合不同，两个制品就不得合堆。例如 `taoyuan:human_meat` 必须定义为 `separate`，因此含“人肉”的包子、料理或加工品不能与不含该标签的同类制品堆叠。

任何会改变食用效果、售价、NPC 反应、心情、合法性或其他逐件玩法行为的组成标签，都必须使用 `separate`；`merge` 标签只能承受合并后来源丢失和标签并集语义。内容校验器应拒绝将行为敏感标签配置为 `merge`。旧存档没有 `compositionTags` 时按空数组读取。

#### 商店定义与商品 Offer

`ShopDef` 描述商店本身的显示名称、NPC、营业时间和天气/季节/星期条件；`ShopOfferDef` 描述某个商店内可售商品。阶段 3 的迁移先把官方六个既有商店和它们当前商品池投影进 `taoyuan:shop` 与 `taoyuan:shop_offer`，不改变购买动作、价格、解锁条件、音效、日志或数量上限。

`ShopOfferDef` 至少包含 `shopId`、`itemId` 和 `price`；可选字段用于保留现有 UI 语义：

- `name`、`description`：商品在当前商店中的显示覆盖文案，不替代物品定义本身。
- `groupId`、`groupName`：稳定显示分组，例如当季种子、武器、鱼饵、肥料、帽子和鞋子。
- `purchaseKind`：购买处理入口提示，当前限定为 `item`、`seed`、`weapon`、`hat` 或 `shoe`；实际扣钱、加物品、装备弹窗和日志流程仍由既有 Store/View 处理。
- `sortOrder`：同一商店内的稳定排序键；相同排序值再按内容 ID 排序。
- `availableSeasons`：只表达季节性商品可见性，不替代 `ShopDef` 的营业条件。

迁移期查询门面可以按 `shopId` 和当前季节返回 Offer 或分组后的 Offer，但不得让第三方数据包绕过公开 Schema 与语义引用校验。无效 `itemId` 应在注册表校验阶段报告，不得等玩家点击购买时才失败。

旅行商人商品池复用同一个 `taoyuan:shop_offer` 契约，以 `shopId: "taoyuan:traveling_merchant"` 和 `groupId: "traveling"` 表示稀有商品候选池。该 Offer 只描述候选物品、基础价格、显示名称和排序；出摊日、每日库存随机、价格浮动、反季种子追加、隐藏 NPC 额外商品、购买扣款和剩余库存存档仍由商店框架规则处理，不新增独立旅行商人注册表。

瀚海驿站固定商品和每周轮换商品同样复用 `taoyuan:shop_offer` 契约，以 `shopId: "taoyuan:hanhai"`、`groupId: "fixed"` 表示常驻商品，以 `shopId: "taoyuan:hanhai_rotating"`、`groupId: "rotating"` 表示轮换候选池。该 Offer 只描述候选物品、显示名称、描述、价格、周限购和排序；固定的四件常驻商品顺序、十件轮换候选顺序和引用校验进入注册表，按年份/季节/周数确定性洗牌并抽取 4 件商品、购买扣钱、背包回滚、周购买计数、藏宝图使用流程和旧瀚海存档结构仍由 Hanhai Store/框架规则处理，不新增独立瀚海商店注册表。

#### 市场行情分类定义

`taoyuan:market_category` 描述每日行情系统可波动的品类定义，只包含玩家可见名称、四季价格系数和 7 天累计出货量供需阈值。公开 TypeBox 契约的等价形状为：

```ts
interface MarketCategoryDef {
  id: ContentId
  name: LocalizedTextRef
  seasonCoefficients: [number, number, number, number]
  supplyThresholds: {
    low: number
    mid: number
    high: number
  }
}
```

- 当前官方条目继续使用 `crop`、`fish`、`animal_product`、`processed`、`fruit`、`ore` 和 `gem` 的 `taoyuan:*` 内容 ID；运行时出货统计和旧存档仍保存这些本地分类键。
- `seasonCoefficients` 必须恰好包含春、夏、秋、冬四个非负数；`supplyThresholds.low/mid/high` 必须是非负整数。
- 每日行情的随机种子、±5% 波动、供需插值、趋势分类、价格系数 clamp、缓存和隔夜日志顺序仍属于框架算法，不进入本注册表。
- 外部市场分类 JSON 在校验前保持为 `unknown`，通过 `MarketCategoryDefSchema` / `market-category.schema.json` 后才允许注册。

#### 瀚海通商兑换定义

`taoyuan:hanhai_trade_exchange` 描述瀚海通商积分可以兑换的静态商品定义，只包含兑换目标、玩家可见文案、积分成本、周限/总限和用于旧运行时分支的最小标记。公开 TypeBox 契约的等价形状为：

```ts
interface HanhaiTradeExchangeDef {
  id: ContentId
  itemId: ContentId
  name: LocalizedTextRef
  pointsCost: number
  description: LocalizedTextRef
  weeklyLimit?: number
  totalLimit?: number
  isWalletItem?: boolean
  equipType?: 'weapon' | 'ring' | 'hat' | 'shoe'
}
```

- 官方条目使用 `taoyuan:hanhai_trade_exchange/{legacyItemId}` 作为兑换定义 ID；运行时购买记录和旧存档继续保存兑换目标的本地 `itemId`。
- `isWalletItem: true` 的 `itemId` 必须引用 `taoyuan:wallet_item`；其他兑换目标必须引用 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- `pointsCost`、`weeklyLimit` 和 `totalLimit` 必须是非负整数；`name` 与 `description` 必须使用 `LocalizedTextRef`。
- 通商积分余额、周限/总限购买计数、香料礼包发放 5 个西域香料、钱袋物品解锁、背包容量回滚和日志副作用仍属于 Hanhai Store/框架规则，不进入本注册表。
- 外部瀚海兑换 JSON 在校验前保持为 `unknown`，通过 `HanhaiTradeExchangeDefSchema` / `hanhai-trade-exchange.schema.json` 后才允许注册。

#### 瀚海通商店铺升级定义

`taoyuan:hanhai_trade_shop_upgrade` 描述瀚海通商店铺的静态升级定义，只包含等级、玩家可见名称、售货槽数量、售卖周期、金币费用和材料费用。公开 TypeBox 契约的等价形状为：

```ts
interface HanhaiTradeShopUpgradeMaterial {
  itemId: ContentId
  quantity: number
}

interface HanhaiTradeShopUpgradeDef {
  id: ContentId
  level: number
  name: LocalizedTextRef
  maxSlots: number
  sellDays: number
  cost: number
  materialCost: HanhaiTradeShopUpgradeMaterial[]
}
```

- 官方条目使用 `taoyuan:hanhai_trade_shop_upgrade/{level}` 作为升级定义 ID；运行时和旧存档继续保存本地数字 `tradeShopLevel`。
- `materialCost[].itemId` 必须引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- `level`、`maxSlots`、`sellDays` 和材料 `quantity` 必须是正整数；`cost` 必须是非负整数；`name` 必须使用 `LocalizedTextRef`。
- 升级扣款、扣料、槽位状态、售卖倒计时、通商积分结算、藏宝图使用流程、每周轮换洗牌和瀚海小游戏仍属于 Hanhai Store/框架规则，不进入本注册表。
- 外部瀚海店铺升级 JSON 在校验前保持为 `unknown`，通过 `HanhaiTradeShopUpgradeDefSchema` / `hanhai-trade-shop-upgrade.schema.json` 后才允许注册。

#### 瀚海藏宝图奖励定义

`taoyuan:hanhai_treasure_reward` 描述使用瀚海藏宝图时可命中的静态奖励档位，只包含随机阈值、金钱奖励和奖励物品清单。公开 TypeBox 契约的等价形状为：

```ts
interface HanhaiTreasureRewardItem {
  itemId: ContentId
  name: LocalizedTextRef
  quantity: number
}

interface HanhaiTreasureRewardDef {
  id: ContentId
  rollMaxExclusive: number
  money: number
  items: HanhaiTreasureRewardItem[]
}
```

- 官方条目使用 `taoyuan:hanhai_treasure_reward/{legacyRewardId}` 作为奖励档位 ID；运行时和旧存档继续只保存藏宝图物品、背包和日志状态，不新增奖励档位持久化字段。
- `rollMaxExclusive` 表示 `Math.random()` 小于该值时命中该档，必须大于 0 且不超过 1；官方档位顺序继续保留旧大奖、中奖、小奖、安慰奖顺序，最后一档作为兜底。
- `items[].itemId` 必须引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`；`items[].name` 只保留当前旧奖励文本中的显示名，不替代物品定义本身。
- 藏宝图扣除、单次随机调用、金钱发放、奖励物品发放失败忽略、奖励文本、日志和旧瀚海存档结构仍属于 Hanhai Store/框架规则，不进入本注册表。
- 外部瀚海藏宝图奖励 JSON 在校验前保持为 `unknown`，通过 `HanhaiTreasureRewardDefSchema` / `hanhai-treasure-reward.schema.json` 后才允许注册。

### 6.7 自定义注册表的开放边界

第一阶段的数据包只允许向现有注册表添加条目，不允许定义新的全局注册表。单独建立一个注册表不会自动产生玩法价值；游戏还必须知道如何校验、读取、执行、展示、保存以及在插件缺失时恢复其中的数据。

自定义数据按消费者能力分为三个等级：

| 需求 | 所属阶段 | 注册表权限 |
| --- | --- | --- |
| 向物品、标签、配方等现有类型添加条目 | 第一阶段：数据插件 | 只能注册条目 |
| 自定义数据只由插件自己的事件处理器读取 | 第二阶段：事件插件 | 可拥有受限的插件私有注册表 |
| 自定义数据对应新副本、设施、UI 或系统规则 | 第三阶段：机制插件 | 可申请定义公开注册表 |

例如，新增“魔法苹果”只需要注册到 `taoyuan:item`；事件插件内部使用的“法术定义”可以放入 `example_mod:spell` 私有注册表；若法术需要独立页面、法力值、战斗规则和跨插件引用，则属于机制插件。

未来开放自定义公开注册表时，应要求插件显式申请 `registry.define` 能力，并提供注册表所有者、Schema、版本和缺失插件策略：

```json
{
  "id": "example_mod",
  "capabilities": ["registry.define"],
  "registries": [
    {
      "id": "example_mod:spell",
      "schema": "schemas/spell.json",
      "schemaVersion": "1"
    }
  ]
}
```

数据插件不得通过清单声明、未知 `entrypoints` 键或内嵌 Schema 绕过该能力检查。自定义注册表必须先于内容条目冻结，且其跨插件引用、存档兼容、禁用行为和卸载恢复规则必须纳入机制插件验收。

### 6.8 官方注册表全量盘点

`TagRegistry`、`ItemRegistry`、`CropRegistry`、`MonsterRegistry`、`EnchantmentRegistry`、`DropTableRegistry`、`RecipeRegistry` 和 `ShopOfferRegistry` 只是首个迁移批次，不代表官方内容包的最终范围。阶段 0 必须逐文件盘点 `src/data/`，为所有静态内容确定目标注册表、派生编译器或保留在框架中的算法归属。

最低盘点范围包括：

| 内容组 | 当前内容示例 | 目标 |
| --- | --- | --- |
| 农业与自然资源 | 作物、鱼类、采集、果树、野树、动物、鱼塘、育种 | 建立对应定义注册表及引用校验 |
| 角色与叙事 | NPC、隐藏 NPC、好感事件、剧情任务、普通任务、提示、秘密纸条 | 建立角色、事件和任务注册表 |
| 成长与收藏 | 成就、技能相关内容、博物馆、行会、教程 | 建立进度与收藏内容注册表 |
| 生产与装备 | 加工机、加工配方、建筑、升级、武器、戒指、帽子、鞋、套装 | 建立生产、设施和装备注册表 |
| 世界与活动 | 农场地图、节日事件、市场、瀚海、旅行商人、钱包条目 | 建立地图、活动和交易内容注册表 |
| 纯规则或常量 | 时间推进、伤害、掉落倍率、结算顺序 | 保留在 engine/domain，不伪装成内容数据 |

盘点结果应形成机器可读清单，记录当前文件、导出符号、目标注册表、持久化 ID、引用方、迁移阶段和完成状态。只有清单中所有内容均已迁移或被明确判定为框架规则，才能宣告“官方内容包完全接管”。

### 6.9 Schema 单一事实来源

公开内容契约采用 TypeBox Schema 作为唯一事实来源。项目使用 `Static<typeof Schema>` 推导内部 TypeScript 类型，使用 Ajv 编译同一份 Schema 并校验外部 JSON，再从同一份 TypeBox 定义生成供模组作者和编辑器使用的公开 JSON Schema。建议依赖为 `@sinclair/typebox`、`ajv`，需要日期、URI 等标准格式时再引入 `ajv-formats`。

```text
TypeBox Schema
├─ Static<> 推导 TypeScript 类型
├─ Ajv 执行运行时结构校验
└─ 生成公开 JSON Schema
```

示例：

```ts
import { Static, Type } from '@sinclair/typebox'

export const LocalizedTextRefSchema = Type.Object({
  key: Type.String({ minLength: 1 }),
  fallback: Type.String({ minLength: 1 })
}, { additionalProperties: false })

export const ItemDefSchema = Type.Object({
  id: Type.String({ pattern: '^[a-z0-9_.-]+:[a-z0-9_./-]+$' }),
  name: LocalizedTextRefSchema,
  sellPrice: Type.Integer({ minimum: 0 })
}, { additionalProperties: false })

export type ItemDef = Static<typeof ItemDefSchema>
```

必须遵守以下约束：

- 不得为公开内容契约另行手写一套 TypeScript `interface` 或 `type`；类型必须从对应 TypeBox Schema 推导。
- 不得手工维护与 TypeBox 重复的公开 JSON Schema；导出的 `.schema.json` 文件只能由生成命令更新。
- 不得对外部数据使用 `parsedJson as ItemDef` 等断言绕过校验。外部值在 Ajv 校验成功前始终按 `unknown` 处理。
- TypeBox/Ajv 只负责字段、联合类型、格式和局部数值边界等结构校验；跨注册表引用、ID 所有权、依赖关系、标签传播和业务不变量由独立语义校验阶段负责。

完整挂载校验顺序为：

```text
签名与文件哈希校验
        → manifest Schema 校验
        → 旧 Schema 版本识别
        → 严格无损迁移
        → 当前版本 Ajv 结构校验
        → 包级暂存注册表
        → 跨注册表语义校验
        → 冻结 RegistrySnapshot
```

迁移函数的输入和输出都必须经过相应版本的 Schema 校验。结构校验失败不得进入语义校验；语义校验失败不得污染全局注册表。未来插件自定义注册表时，也必须提交可由挂载器编译的运行时 Schema，但是否允许定义公开注册表仍由后续机制插件的 `registry.define` 能力控制。

### 6.10 依赖与版本解析

拓扑排序只决定已经选定的包按什么顺序加载，版本解析必须先确定每个 `packageId` 唯一生效的版本。版本字段和依赖约束采用 SemVer 2.0；实现可使用成熟的 `node-semver`，公开契约首版支持确定版本、比较器范围、`^` 和 `~`，不支持 `latest`、Git URL、文件路径或脚本表达式。预发布版本只有在约束显式包含预发布标识时才参与匹配。

每个包文件的 `version` 必须是一个确定版本，一个包文件不得代表多个版本。同一个程序目录的单次启动环境中，同一 `packageId` 只允许一个生效版本；虽然依赖允许声明 SemVer 范围，但推荐模组作者依赖一个已经实际验证的确定版本。`mods/` 中存在同一包的多个版本且 lockfile 未明确选择时，挂载器必须停止解析并要求玩家处理，不得自动选择最新版。

所有必需依赖的版本约束必须同时满足。例如 A 要求 `library ^1.0.0`、B 要求 `library ^2.0.0` 时，不存在合法候选环境，挂载器必须在注册内容前阻止启动并展示完整依赖链。不得按目录扫描顺序、文件修改时间、最高版本或模糊优先级擅自消解冲突。

首版只把 `dependencies` 作为正式依赖类型：目标必须存在、版本必须满足，且目标包先加载。后续增加其他依赖类型时遵守以下语义：

- `optionalDependencies`：目标缺失时允许继续；目标已启用但版本不满足时仍视为冲突；存在且兼容时建立加载先后边。
- `loadBefore`、`loadAfter`：只提供确定性顺序提示，不代表可以引用目标内容，不参与版本选择，目标缺失时忽略，且不得覆盖必需依赖顺序。
- `peerDependencies`：数据插件阶段不提供。后续事件插件或机制插件确有共享运行库需求时再定义，不复用 npm 语义制造第二套嵌套依赖树。

游戏发行版本、引擎 API 版本和内容 Schema 版本是三个独立维度。较高版本游戏发现 `gameVersion` 较低的模组时，可以进入兼容尝试，但这不构成兼容保证，也不得绕过签名、权限、Schema、API 和语义校验：

```text
识别较低 gameVersion 的模组
        → 校验签名与文件完整性
        → 识别旧 contentSchemaVersion 并执行严格无损迁移
        → Ajv 结构校验
        → 包级暂存注册
        → API 与跨注册表语义校验
        → 成功后纳入候选 RegistrySnapshot
```

兼容尝试遇到任一错误时，应立即停止处理该模组，丢弃其全部暂存条目，并撤销依赖它的直接和传递依赖包。整个候选快照随之作废，`GameApp` 不得启动；挂载器继续运行并展示被放弃的模组、连带受影响包、失败阶段和错误日志。只有玩家明确禁用、移除或修复相应模组并重新解析后，才能产生新的候选环境。不得静默跳过失败模组后直接打开可能依赖其内容的存档。较低版本游戏不得尝试加载面向更高 `gameVersion`、更高引擎 API 或更高内容 Schema 的模组。

解析结果必须与文件扫描顺序无关。挂载器先扫描全部候选包，再按 `packageId` 分组并根据全局配置和 lockfile 选择唯一版本，检查版本约束后构建依赖图。稳定拓扑排序存在多个合法节点时，以规范化 `packageId` 字典序作为决胜规则。

每次完整挂载成功后，挂载器把最终包版本、内容哈希、依赖解析结果和稳定加载顺序原子写入程序目录 `userdata/mod-lock.json`。失败时不得覆盖上一次成功的 lockfile。lockfile 用于重现当前安装环境；存档仍须独立记录保存时实际使用的解析结果，用于环境比较和存档保护，不能反向控制全局模组启停。

### 6.11 生态规范与一致性契约

正式公开数据包 API 前，项目必须把生态约定拆成独立、版本化的规范，而不是只散落在实现代码和示例包中。至少发布清单、配置、本地化、生命周期和兼容性规范；规范版本独立于游戏版本、引擎 API 和内容 Schema 版本。

规范文本采用 RFC 风格关键词：

| 关键词 | 含义 |
| --- | --- |
| 必须（MUST） | 不满足即不兼容，校验器必须拒绝或给出规定错误 |
| 禁止（MUST NOT） | 出现即违反规范 |
| 应该（SHOULD） | 默认必须遵守，例外必须有可说明的理由 |
| 可以（MAY） | 可选能力，不得成为隐式依赖 |

每条可执行的强制规则都应具有稳定规则编号，例如 `PKG-LIFECYCLE-014`，并对应 Schema 或语义校验、稳定错误码以及至少一个一致性测试包。规范和实现发生分歧时，以已发布的稳定规范和测试结果为兼容承诺；修正文档不得静默改变既有稳定行为。

#### 6.11.1 配置作用域

模组全局启用状态与模组设置作用域分开。首个公开契约定义两种设置：

```ts
type PackageSettingScope = 'installation' | 'save'

interface PackageSettingDefinition {
  id: NamespacedId
  scope: PackageSettingScope
  default: unknown
  schema: unknown
}
```

| 作用域 | 保存位置 | 允许影响 |
| --- | --- | --- |
| `installation` | Electron 的 `userdata/mod-config/` 或平台等价存储 | 内容编译、注册表值、依赖解析和安装实例全局规则 |
| `save` | 根存档通用 `packageSettings` 区 | `RegistrySnapshot` 冻结后的当前存档运行时行为 |

会改变注册表条目、持久化 ID、配方、依赖关系、加载顺序或内容编译结果的设置必须使用 `installation`。这些内容相关配置的规范化摘要写入 `mod-lock.json` 和存档内容环境，防止相同包版本在不同配置下被误判为相同环境。`save` 设置不得参与包发现、版本选择、注册表定义或冻结前编译。

配置定义使用公开 Schema，挂载器根据布尔、枚举、数值、字符串等受支持类型生成标准控件；数据包不能借配置功能注入任意 Vue、HTML 或脚本。升级时保留玩家显式值，只为新增且缺失的字段补 Schema 默认值；配置迁移失败时保留旧值并拒绝候选提交，不得清空或静默重置。首阶段数据插件只实现 `installation` 配置，`save` 配置在事件或机制插件阶段启用，但根存档通用容器应提前定型。

主程序的纯显示偏好继续属于玩家设置，不伪装成模组配置。配置值不得包含密钥或依赖本方案提供秘密存储；未来确需敏感凭据时应另设平台安全存储能力。

#### 6.11.2 本地化

所有玩家可见的内容名称和描述使用稳定翻译引用，不把单一语言字符串作为唯一数据：

```ts
interface LocalizedTextRef {
  key: string
  fallback: string
}
```

语言标识采用 BCP 47，例如 `zh-CN`、`zh-TW`、`en-US`。翻译正文存放在清单声明的独立语言包中，翻译键必须位于数据包自己的命名空间。固定回退顺序为：

```text
当前地区语言
        → 同语言的默认地区
        → 数据包 defaultLocale
        → LocalizedTextRef.fallback
        → 显示翻译键并记录缺失诊断
```

默认语言和 `fallback` 必须存在。动态文本采用公开的 ICU MessageFormat 受限子集，并校验参数名称和复数分支；文本默认按纯文本渲染，禁止 HTML、脚本和事件属性。重复翻译键、非法语言标签、缺失默认语言和占位参数不一致必须进入文档或数据校验。官方内容开始迁移时即使用该结构，避免全量剥离后再次改写所有名称和描述。

#### 6.11.3 作者、许可与资源署名

公开清单支持 `authors`、`contributors`、`license`、`homepage`、`source`、`issues` 和 `attributions`。作者条目至少包含名称，可选角色和 HTTPS 地址；许可证优先使用 SPDX 标识。公共分发包必须声明许可证，本地开发包缺少许可证时可以加载但必须显示明确警告。

第三方图片、音乐、视频、字体和其他资源通过 `attributions.json` 逐项记录包内路径、作者、来源、许可证和是否修改。签名文件清单必须覆盖署名文件，构建和发布校验应检查资源路径存在且没有未署名的受管资源。挂载器只把外部链接交给系统浏览器，不在应用内直接执行或嵌入未知网页内容。

#### 6.11.4 原子生命周期

安装、升级、禁用和卸载使用统一事务状态机：

```text
discovered
        → staged
        → verified
        → resolved
        → mounted
        → committed
```

只有候选包、依赖图、配置迁移、资源和 `RegistrySnapshot` 全部通过后，才可以原子提交包文件、全局启用状态和 lockfile。Electron 在 `userdata/mod-transactions/` 保存崩溃恢复日志，Web 和 Android 使用平台等价事务存储；启动时必须先恢复或回滚未完成事务，再扫描模组。

- 安装失败不得在正式 `mods/` 中留下半个包。
- 升级先保留旧版本并暂存新版本，失败时继续使用旧版本和旧 lockfile。
- 禁用先验证依赖它的包和候选环境，成功后才更新全局配置。
- 卸载必须先完成禁用事务，默认只移除包文件，保留配置、`pluginData`、存档未知内容和可恢复备份。
- “同时清除插件数据”是独立危险操作，必须明确确认并先备份受影响存档。
- 安装和升级不得批量改写现有存档；存档迁移只在玩家打开副本时发生。
- 已运行的 `GameApp` 不热切换模组，生命周期变更在重新挂载后生效。

文件系统原子替换、IndexedDB 事务和 Android 存储操作由平台适配器实现，但共享同一状态机、错误码和回滚保证。

#### 6.11.5 弃用与兼容测试包

公开契约按 `experimental → stable → deprecated → removed` 管理。`experimental` 能力不作长期兼容保证；`stable` 能力只能按已发布版本策略演进。普通弃用至少保留两个稳定发布周期，并提供 `deprecatedSince`、替代接口、迁移示例、计划移除版本、挂载器警告和稳定错误码。移除稳定 API 原则上只能发生在新的 `engineApiVersion` 主版本；安全漏洞可以紧急禁用，但必须发布安全公告和明确诊断。

弃用清单同时以人类可读变更日志和机器可读索引发布。兼容承诺只覆盖正式公开的 Schema、注册表、事件和能力 API，不覆盖项目内部 TypeScript 模块。

项目维护公开的一致性测试包集合：

```text
compatibility-packs/
  valid/       # 最小合法包、全部注册表、配置、本地化和资源署名
  invalid/     # 缺失依赖、版本冲突、重复 ID、非法配置和越权资源
  legacy/      # 旧 Schema、弃用 API、旧本地化和迁移链
```

每个测试包包含预期结果，至少校验成功或失败、错误码、修复报告、最终加载顺序、lockfile、注册表快照哈希、配置摘要、缺失插件数据保留和生命周期失败后的目录状态。项目发布 CI 和模组作者的独立校验命令必须复用同一组夹具；只有通过对应一致性测试的能力才能标记为 `stable`。
