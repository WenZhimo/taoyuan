# 内置内容迁移盘点

> [返回统筹说明](./README.md)

## 当前基线

截至 2026-07-16，`src/data/` 包含 82 个 TypeScript 文件，约 31,550 行和 610 个导出符号；另有 148 个迁移期查询门面、契约和适配器审计产物。对应机器可读清单见 [content-inventory.json](./content-inventory.json)。该清单已经逐文件记录职责分类、候选目标注册表和迁移阶段，但混合文件在实际迁移前仍必须完成导出符号级审计。

盘点不是“把整个文件变成 JSON”。每个导出符号必须归入以下类别之一：

| 类别 | 处理方式 |
| --- | --- |
| `content` | 进入官方内容包和公开注册表 |
| `derived` | 由内容编译器从基础定义稳定生成 |
| `algorithm` | 移入或保留在 `engine/domain` |
| `adapter` | 迁移期保留原函数签名，内部改查注册表 |
| `ui` | 留在主题、组件或玩家设置体系 |
| `barrel` | 仅负责重导出，不形成内容注册表 |

## 高风险混合文件

| 文件 | 规模 | 主要风险 | 首要动作 |
| --- | ---: | --- | --- |
| `crops.ts` | 约 4,962 行 | 大量定义与索引查询混合 | 先建立 Crop Schema 和规范化快照，再改查询适配器 |
| `breeding.ts` | 约 226 行 | 升级配置、随机和遗传算法仍与兼容查询共存 | 杂交定义已拆到 `breedingDefinitions.ts`；继续按导出符号保留框架算法 |
| `recipes.ts` | 约 2,208 行 | 配方量大且将引入标签材料 | 固定现有配方快照，再扩展材料联合类型 |
| `processing.ts` | 约 2,102 行 | 机器、加工配方、肥料、鱼饵等多个域混合 | 拆为多个注册表，保留统一查询门面 |
| `items.ts` | 约 1,938 行 | 手写、映射生成和跨域汇总并存 | 建立内容编译器，避免复制派生物品 |
| `heartEvents.ts` | 约 1,964 行 | 叙事定义和条件行为紧密关联 | 先区分声明式条件与引擎动作 |
| `achievements.ts` | 约 1,281 行 | 成就和社区收集包共存 | 拆成独立注册表并固定进度 ID |
| `mine.ts` | 约 490 行 | 奖励、楼层和随机算法仍混合 | 怪物与池已提取到叶子模块并接入注册表；继续拆分框架算法 |
| `weapons.ts` | 约 698 行 | 武器、附魔、掉落和计算函数混合 | 分 Weapon、Enchantment、DropTable，计算留在 domain |
| `hanhai.ts` | 约 518 行 | 商店内容和多个小游戏算法混合 | 静态投注/奖励配置与牌局算法分离 |

## 符号级审计要求

阶段 0 必须把机器清单扩展到每个公开导出符号，至少记录：

```ts
interface ContentInventorySymbol {
  file: string
  exportName: string
  classification: 'content' | 'derived' | 'algorithm' | 'adapter' | 'ui' | 'barrel'
  targetRegistry?: RegistryTypeId
  persistentIds: boolean
  consumers: string[]
  migrationPhase: number[]
  snapshotFixture?: string
  status: 'inventoried' | 'baselined' | 'migrated' | 'verified' | 'framework-retained'
  rationale?: string
}
```

机器盘点可以在顶层使用可选的 `reviewedArtifacts` 数组登记 `src/data/` 之外、但与当前迁移切片直接相关的查询门面或适配器符号。条目复用上述 `file`、`exportName`、`classification`、`targetRegistry`、`persistentIds`、`migrationPhase`、`status` 和 `rationale` 字段；`summary.reviewedArtifactCount` 必须与实际条目数一致。该数组不计入 `sourceRoot` 文件数或 `symbolCount`，避免混淆静态内容基线与迁移期代码产物。

不得只依据变量名分类。包含 `Math.random()`、日期、Store 访问或状态修改的函数默认按算法处理；数组和对象也可能是框架常量，需要根据消费者和存档语义判断。

## 阶段 5 审计结果

- `src/data/monsters.ts` 是怪物数值和旧池映射的唯一静态内容源，状态为 `verified`；注册表适配器不再依赖 `src/data/mine.ts`。
- `src/data/mine.ts` 中的 `MONSTERS`、`BOSS_MONSTERS`、`SKULL_CAVERN_MONSTERS` 和 `ZONE_MONSTERS` 是原名重导出的兼容适配器，不形成第二份可独立修改的数据。
- `ZONE_MONSTERS` 已投影为六个 `taoyuan:monster_pool` 区域池；BOSS 楼层映射和骷髅矿穴候选投影为另外九个池。
- `MINE_FLOORS`、`getFloor()` 的楼层规则、感染层、随机选择、弱化和缩放继续标记为 `framework-retained` 或适配器，不误归类为内容。
- `reviewedArtifacts` 已登记首切片的三个怪物查询门面，以及 `MonsterPoolDefSchema`、官方池适配器和五组怪物池查询门面，共 10 个阶段 5 审计产物。

## 阶段 6 树木首切片审计结果

- `src/data/treeDefinitions.ts` 新增为 8 种果树、3 种野树旧格式数值的唯一静态叶子来源，两个定义数组均标记为 `verified` 并指向 `taoyuan:tree`。
- `FRUIT_TREE_DEFS`、`WILD_TREE_DEFS` 标记为兼容重导出，`getFruitTreeDef()`、`getWildTreeDef()` 标记为注册表兼容查询；四个旧符号保留原模块路径与名称。
- `items.ts` 只为编译树苗和果实物品读取叶子定义，`staticAdapters.ts` 只为官方注册表投影读取，等价测试只用于迁移前后比较；这些路径不属于业务层绕过。
- `useFarmStore.ts`、`useTreeActions.ts` 和 `ShopView.vue` 已移除旧树木数组直连，新增符合现有模型的官方树种时无需修改这三个消费者。
- `reviewedArtifacts` 新增 `TreeDefSchema`、官方树木适配器和三组查询门面，共 5 个阶段 6 审计产物；清单当前覆盖 46 个 `src/data` 文件、30,844 行、429 个导出符号和 15 个审计产物。
- 鱼类、采集、动物、鱼塘、育种、生产、叙事和世界活动仍保持原盘点状态，未因树木切片误标为完成。

## 阶段 6 鱼类切片审计结果

- `src/data/fishDefinitions.ts` 新增为 60 种旧格式鱼类定义和钓鱼地点显示列表的唯一静态叶子来源；其中 `FISH` 标记为 `verified` 并指向 `taoyuan:fish`，`FISHING_LOCATIONS` 暂按 UI/框架列表保留。
- `src/data/fish.ts` 保留原模块路径、`FISH` / `FISHING_LOCATIONS` 重导出以及 `getFishById()`、`getAvailableFish()` 旧签名；查询函数内部已改为读取官方鱼类注册表兼容门面。
- `items.ts` 只为派生鱼类物品读取叶子定义，`staticAdapters.ts` 只为官方注册表投影读取；普通钓鱼 Store、溪流田庄隔夜鱼获和钓翁令牌解锁不再直接读取旧 `FISH` 数组。
- 定向测试覆盖鱼类公开契约、旧入口等价、普通抛竿随机调用次数、魔法鱼饵忽略季节行为和钱包鱼类 ID 统计；旧鱼类 ID、名称、售价、季节、天气、地点、难度和小游戏参数保持不变。
- 本切片未迁移鱼塘、鱼苗、育种或钓鱼地点注册表；采集、动物、生产、叙事和世界活动仍保持原盘点状态，不能据此标记阶段 6 完成。

## 阶段 6 可养殖鱼切片审计结果

- `src/data/fishPondDefinitions.ts` 新增为 13 种旧格式可养殖鱼定义的唯一静态叶子来源；`PONDABLE_FISH` 标记为 `verified` 并指向 `taoyuan:pondable_fish`。
- `src/data/fishPond.ts` 保留原模块路径、`PONDABLE_FISH` 原名重导出、建造/升级/容量/水质/疾病/繁殖常量，以及 `getPondableFishDefs()`、`getPondableFish()`、`isPondableFish()` 旧签名；三个查询入口内部已改为读取官方可养殖鱼注册表兼容门面。
- `FishPondView.vue` 和 `useFishPondStore.ts` 继续通过旧查询门面读取可养殖鱼定义，因此放鱼、默认基因、成熟天数、产物 ID、产出概率和品质计算保持不变。
- `POND_CAPACITY`、育苗/自然繁衍、水质、疾病、死亡、喂食、清理和鱼塘存档结构未在本切片迁移；这些仍按设施或框架规则保留。
- 定向测试覆盖可养殖鱼公开契约、旧入口等价、冻结只读结果、缺失鱼类/产物引用诊断，以及鱼塘放鱼和每日产物闭环；本切片未迁移育种、加工、叙事或世界活动。

## 阶段 6 鱼塘品种切片审计结果

- `src/data/pondBreedDefinitions.ts` 新增为 400 条旧格式鱼塘品种定义和 `BREED_COUNTS` 的唯一静态叶子来源；`POND_BREEDS` 标记为 `verified` 并指向 `taoyuan:pond_breed`。
- `src/data/pondBreeds.ts` 保留原模块路径、`POND_BREEDS` / `BREED_COUNTS` 原名重导出，以及 `getBreedById()`、`getBreedsByGeneration()`、`getBreedsBySpecies()`、`getGen1BreedsForFish()`、`findBreedByParents()` 旧签名；五个查询入口内部已改为读取官方鱼塘品种注册表兼容门面。
- `useFishPondStore.ts` 继续通过旧查询门面读取品种定义，因此放鱼时的 Gen1 随机候选、育苗塘亲本配对查找、同代回退候选、发现品种 ID 和显示名称保持不变。
- 鱼塘容量、水质、喂食、疾病、死亡、育苗塘任务队列、自然繁衍进度、遗传随机、基因上下限、产物品质和鱼塘存档结构未在本切片迁移；这些仍按设施或框架规则保留。
- 定向测试覆盖鱼塘品种公开契约、旧入口等价、每代数量、顺序无关亲本查找、冻结只读结果、缺失可养殖鱼/亲本引用诊断，以及育苗塘产出品种闭环；本切片未迁移加工、叙事或世界活动。

## 阶段 6 鱼塘设施切片审计结果

- `src/data/fishPondFacilityDefinitions.ts` 新增为鱼塘建造成本、升级成本、等级容量和满级无限容量语义的唯一静态叶子来源；`FISH_POND_FACILITY` 标记为 `verified` 并指向 `taoyuan:fish_pond_facility`。
- `src/data/fishPond.ts` 保留原模块路径、`FISH_POND_FACILITY` 原名重导出、旧 `POND_BUILD_COST` / `POND_UPGRADE_COSTS` / `POND_CAPACITY` 常量，以及 `getPondBuildCost()`、`getPondUpgradeCost()`、`getPondCapacity()`、`getPondRuntimeCapacity()` 兼容查询；建造、升级和运行时容量读取已改为官方鱼塘设施注册表门面。
- `useFishPondStore.ts` 和 `FishPondView.vue` 的鱼塘建造、升级、弹窗金额、材料、静态容量和满级无限容量展示已改为通过兼容门面读取；扣款、扣料、建造/升级等级、旧 `POND_CAPACITY[3] = 20` 静态值和运行时满级无限容量保持不变。
- 水质衰减、喂食、水质改良剂、疾病、死亡、育苗塘任务、自然繁衍进度、遗传随机、产物品质和鱼塘存档结构未在本切片迁移；这些仍按 Store/框架规则保留。
- 定向测试覆盖鱼塘设施公开契约、外部 JSON `unknown` 校验、旧常量和新查询入口等价、冻结只读结果、缺失建造/升级材料引用诊断，以及 Store 建造/升级/满级容量闭环；本切片未迁移加工、叙事或世界活动。

## 阶段 6 建筑升级切片审计结果

- `src/data/buildingUpgradeDefinitions.ts` 新增为农舍、山洞和酒窖升级定义的唯一静态叶子来源；`FARMHOUSE_UPGRADES`、`CAVE_UPGRADES`、`CELLAR_UPGRADES` 均标记为 `verified` 并指向 `taoyuan:building_upgrade`。
- `src/data/buildings.ts` 保留原模块路径、旧数组原名重导出、旧类型重导出和 `getFarmhouseUpgrade()`、`getCaveUpgrade()`、`getCellarUpgrade()` 兼容查询；三个查询入口和新增列表查询均优先读取官方建筑升级注册表并返回本地 ID 形状。
- `useHomeStore.ts` 的农舍升级、山洞升级/每日产物参数和酒窖升级/容量/增值读取已改为通过兼容门面读取；扣款、扣料、等级字段、山洞随机调用、酒窖满级无限容量和旧存档结构保持不变。
- 温室升级空数组、温室解锁、仓库解锁、山洞品质阈值、山洞解锁收入、酒窖增值周期、山洞/酒窖日更算法未在本切片迁移；这些仍按 Store/框架规则保留。
- 定向测试覆盖建筑升级公开契约、外部 JSON `unknown` 校验、旧入口等价、冻结只读结果、缺失材料/山洞产物引用诊断，以及 HomeStore 农舍/山洞/酒窖升级和山洞固定随机产物闭环；本切片未迁移加工、叙事或世界活动。

## 阶段 6 钱袋物品切片审计结果

- `src/data/walletDefinitions.ts` 新增为 7 个旧格式钱袋物品定义的唯一静态叶子来源；`WALLET_ITEMS` 标记为 `verified` 并指向 `taoyuan:wallet_item`。
- `src/data/wallet.ts` 保留原模块路径、`WALLET_ITEMS` 原名重导出和 `getWalletItemById()` 兼容查询；新增 `getWalletItems()`，两个查询入口均优先读取官方钱袋物品注册表并返回本地 ID 形状。
- `useWalletStore.ts`、`WalletView.vue` 和 `CharInfoView.vue` 的钱袋列表、解锁校验和被动展示已改为通过兼容门面读取；解锁 ID、被动数值、自动解锁条件、瀚海发放和旧存档 `unlockedItems` 结构保持不变。
- 钱袋被动效果算法、商店/戒指折扣叠加、采集品质提升、挖矿体力减免、钓鱼 calm、烹饪恢复量、作物生长和通商积分加成未在本切片迁移；这些仍按 Store/框架规则保留。
- 定向测试覆盖钱袋物品公开契约、外部 JSON `unknown` 校验、旧入口等价、重复 ID 拒绝、冻结只读结果、无语义引用诊断、Store 自动解锁/手动解锁和旧存档恢复闭环；本切片未迁移加工、叙事或世界活动。

## 阶段 6 农场地图切片审计结果

- `src/data/farmMapDefinitions.ts` 新增为 6 个旧格式开局农场地图定义的唯一静态叶子来源；`FARM_MAP_DEFS` 标记为 `verified` 并指向 `taoyuan:farm_map`。
- `src/data/farmMaps.ts` 保留原模块路径、`FarmMapDef` 类型重导出和 `FARM_MAP_DEFS` 原名重导出；新增 `getFarmMapDefs()` 和 `getFarmMapByType()` 兼容查询，两个查询入口均优先读取官方农场地图注册表并返回本地 ID 形状。
- `MainMenu.vue` 的开局田庄列表、确认弹窗和开始游戏传入的 `FarmMapType` 已改为通过兼容门面读取；显示名称、描述、bonus 文案、选中类型和旧存档 `farmMapType` 字符串保持不变。
- 溪流/竹林/山丘/荒野/草甸/标准田庄的经验、售价、产物、遭遇、鸡舍赠送、动物加成、初始地块大小和日更算法未在本切片迁移；这些仍按 Store/框架规则保留。
- 定向测试覆盖农场地图公开契约、外部 JSON `unknown` 校验、旧入口等价、重复 ID 拒绝、冻结只读结果、无语义引用诊断、开局地图选择和旧存档恢复闭环；本切片未迁移生产、NPC、叙事或世界活动。

## 阶段 6 采集物切片审计结果

- `src/data/forageDefinitions.ts` 新增为 17 种旧格式竹林采集物定义的唯一静态叶子来源；`FORAGE_ITEMS` 标记为 `verified` 并指向 `taoyuan:forage`。
- `src/data/forage.ts` 保留原模块路径、`ForageItemDef` / `FORAGE_ITEMS` 重导出以及 `getForageItems()` 旧签名；季节查询函数内部已改为读取官方采集物注册表兼容门面。
- `WEATHER_FORAGE_MODIFIER`、`FOREST_ENCOUNTER_CHANCE`、`FRIENDLY_ANIMALS`、`HOSTILE_ANIMALS`、战败惩罚和 `rollForestEncounter()` 未在本切片迁移；天气倍率、动物遭遇和战斗流程继续按框架规则处理。
- `ForageView.vue` 和溪流/竹林田庄隔夜结算继续通过旧 `getForageItems()` 入口读取，因此获得物品、品质、经验、随机调用和玩家可见文案保持不变。
- 定向测试覆盖采集物公开契约、旧入口季节筛选等价、冻结只读结果和缺失产物物品引用诊断；本切片未迁移动物、鱼塘、育种、生产、叙事或世界活动。

## 阶段 6 动物定义切片审计结果

- `src/data/animalDefinitions.ts` 新增为 20 种旧格式可饲养动物物种定义的唯一静态叶子来源；`ANIMAL_DEFS` 标记为 `verified` 并指向 `taoyuan:animal`。
- `src/data/animals.ts` 保留原模块路径和 `ANIMAL_DEFS` 原名重导出；`getAnimalDef()`、`getAnimalDefsByBuilding()` 保留本地 ID 兼容形状并改为优先读取官方动物注册表。
- `useAnimalStore.ts` 和 `AnimalView.vue` 的动物物种查询已改为通过兼容门面读取，覆盖购买、建筑归属筛选、孵化完成名称、每日产出、自动抚摸机建筑归属、出售退款和牧场页面购买列表。
- `INCUBATION_MAP` 未在本动物定义切片迁移，已由后续动物孵化切片独立处理；饲料效果、放牧、疾病/死亡、宠物拾取和每日结算算法仍按设施或框架规则保留。
- 定向测试覆盖动物公开契约、旧入口和建筑筛选等价、冻结只读结果、缺失产物物品引用诊断，以及 Store 购买/产出/出售闭环；本切片未迁移鱼塘、育种、生产、叙事或世界活动。

## 阶段 6 动物饲料切片审计结果

- `src/data/animalFeedDefinitions.ts` 新增为 4 种旧格式动物饲料定义的唯一静态叶子来源；`FEED_DEFS` 标记为 `verified` 并指向 `taoyuan:animal_feed`。
- `src/data/animals.ts` 保留原模块路径和 `FEED_DEFS` 原名重导出；新增 `getFeedDef()`、`getFeedDefs()` 兼容查询，优先读取官方动物饲料注册表并返回本地 ID 形状。
- `AnimalView.vue` 的饲料选择、库存、名称、价格和购买入口已改为通过兼容门面读取；`useAnimalStore.ts` 中按 `fedWith` 判断的精饲料、滋补饲料、活力饲料效果仍是框架算法，不在本切片迁移。
- 定向测试覆盖动物饲料公开契约、外部 JSON `unknown` 校验、旧入口等价、冻结只读结果、缺失物品引用诊断和旧 `FEED_DEFS` 顺序；本切片未迁移畜舍升级、放牧、疾病/死亡或每日结算算法，孵化映射已由后续动物孵化切片独立处理。

## 阶段 6 动物建筑切片审计结果

- `src/data/animalBuildingDefinitions.ts` 新增为 3 种旧格式动物建筑定义和 4 条升级定义的唯一静态叶子来源；`ANIMAL_BUILDINGS` 与 `BUILDING_UPGRADES` 标记为 `verified` 并指向 `taoyuan:animal_building`。
- `src/data/animals.ts` 保留原模块路径和 `ANIMAL_BUILDINGS` / `BUILDING_UPGRADES` 原名重导出；新增 `getBuildingDefs()` 兼容查询，`getBuildingDef()` 和 `getBuildingUpgrade()` 优先读取官方动物建筑注册表并返回本地 ID 形状。
- `useAnimalStore.ts` 的畜舍建造/升级需求、`AnimalView.vue` 的建筑列表、马厩状态和建造日志已改为通过兼容门面读取；扣款、扣料、容量、存档建筑状态和旧 UI 文案保持不变。
- 定向测试覆盖动物建筑公开契约、外部 JSON `unknown` 校验、旧建筑/升级全量顺序和字段等价、冻结只读结果、缺失建造/升级材料引用诊断，以及 Store 建造/升级闭环；本切片未迁移 `INCUBATION_MAP`、饲料效果、放牧、疾病/死亡或每日结算算法，其中 `INCUBATION_MAP` 已由后续动物孵化切片独立处理。

## 阶段 6 动物孵化切片审计结果

- `src/data/animalIncubationDefinitions.ts` 新增为 7 条旧格式蛋类孵化映射的唯一静态叶子来源；`ANIMAL_INCUBATIONS` 标记为 `verified` 并指向 `taoyuan:animal_incubation`。
- `src/data/animals.ts` 保留原模块路径和 `INCUBATION_MAP` 原名重导出；新增 `getIncubationDef()`、`getIncubationDefs()` 和 `getIncubationMap()` 兼容查询，优先读取官方动物孵化注册表并返回本地 ID 形状。
- `useAnimalStore.ts` 与 `AnimalView.vue` 的鸡舍/牲口棚孵化入口已改为通过兼容门面读取，蛋物品 ID、孵化动物、建筑归属、孵化天数、扣料和日志文案保持不变。
- 定向测试覆盖动物孵化公开契约、外部 JSON `unknown` 校验、旧入口/旧 `INCUBATION_MAP` 全量顺序和字段等价、冻结只读结果、缺失蛋物品/动物引用诊断，以及鸡舍鸭蛋和牲口棚鸵鸟蛋孵化闭环。
- 本切片未迁移饲料效果、放牧、疾病/死亡、宠物拾取或每日结算算法；孵化器状态和旧存档结构保持框架原状。

## 阶段 6 工具升级切片审计结果

- `src/data/toolUpgradeDefinitions.ts` 新增为旧格式工具升级费用、工具中文名和等级中文名的唯一静态叶子来源；`TOOL_UPGRADE_COSTS`、`TOOL_NAMES` 和 `TIER_NAMES` 标记为 `verified` 并指向 `taoyuan:tool_upgrade`。
- `src/data/upgrades.ts` 保留原模块路径、旧类型/常量原名重导出和 `getUpgradeCost(type, currentTier)` 兼容查询，优先读取官方工具升级注册表并返回本地 ID 形状；注册表异常或未命中时仍回退旧静态定义。
- `src/domain/mods/schemas.ts` 新增 `ToolUpgradeDefSchema` 和公开 `tool-upgrade.schema.json`，继续以 TypeBox 作为工具升级费用契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 将 21 条旧工具升级费用只读投影为 `taoyuan:tool_upgrade`；注册表 ID 使用 snake case 工具路径，`toolType` 字段和运行时查询继续使用旧 `ToolType` 枚举。
- `src/domain/mods/semanticValidation.ts` 校验 `materials[].itemId` 引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖工具升级公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧费用/名称全量顺序和字段等价、冻结只读结果、重复 ID 拒绝、缺失材料引用诊断，以及 `getUpgradeCost()` iridium 满级返回 `undefined`。
- 本切片未迁移工具耐力、操作范围、升级等待天数、铁匠铺流程、钓鱼判定、批量操作或存档结构；工具状态和旧存档字段保持框架原状。

## 阶段 6 装备套装切片审计结果

- `src/data/equipmentSetDefinitions.ts` 新增为旧格式装备套装定义的唯一静态叶子来源；`EQUIPMENT_SET_DEFINITIONS` 标记为 `verified` 并指向 `taoyuan:equipment_set`。
- `src/data/equipmentSets.ts` 保留原模块路径、`EQUIPMENT_SETS` 原名导出、`EquipmentSetDef` / `SetBonusLevel` 类型重导出和 `getSetByPieceId(defId)` 兼容查询；新增 `getEquipmentSets()`，查询入口优先读取官方装备套装注册表并保留旧本地 ID 形状。
- `useInventoryStore.ts` 的 `activeSetBonuses` 和 `activeSets` 改为通过兼容门面读取装备套装定义；装备基础属性、装备方案、附魔、掉落、合成、装备实例和旧存档结构保持不变。
- `src/domain/mods/schemas.ts` 新增 `EquipmentSetDefSchema` 和公开 `equipment-set.schema.json`，继续以 TypeBox 作为装备套装定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 将旧装备套装只读投影为 `taoyuan:equipment_set`；`src/domain/mods/semanticValidation.ts` 校验武器、戒指、帽子和鞋子件位引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖装备套装公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧顺序和字段全量等价、旧件位查询语义、冻结只读结果、重复 ID 拒绝、缺失件位引用诊断，以及背包 Store 套装激活加成闭环。
- 本切片未迁移单件武器/戒指/帽子/鞋定义、装备获取、合成、怪物/宝箱掉落、附魔生成、装备方案或装备存档结构；这些仍按既有数据与 Store/框架规则保留。

## 阶段 6 戒指定义切片审计结果

- `src/data/ringDefinitions.ts` 新增为旧格式戒指定义和戒指掉落派生表的静态叶子来源；`RINGS` 标记为 `verified` 并指向 `taoyuan:equipment` 的首个 `kind: "ring"` 切片。
- `src/data/rings.ts` 保留原模块路径、`RINGS` / `CRAFTABLE_RINGS` / 戒指掉落表原名重导出，以及 `getRingById(id)` 兼容查询；查询入口优先读取官方单件装备注册表并返回本地 ID `RingDef` 形状。
- `src/domain/mods/schemas.ts` 新增 `EquipmentDefSchema` 和公开 `equipment.schema.json`，继续以 TypeBox 作为单件装备定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 将旧戒指定义只读投影为 `taoyuan:equipment`；`src/domain/mods/semanticValidation.ts` 校验戒指本体 ID 和合成材料引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖单件装备公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧戒指顺序和字段全量等价、冻结只读结果、重复 ID 拒绝、缺失本体/材料引用诊断，以及旧 `getRingById()` 本地和命名空间 ID 兼容。
- 本切片未迁移武器、鞋子定义、装备合成流程、装备掉落结算、附魔生成、装备方案或装备存档结构；这些仍按既有数据与 Store/框架规则保留。

## 阶段 6 帽子定义切片审计结果

- `src/data/hatDefinitions.ts` 新增为旧格式帽子定义和帽子掉落派生表的静态叶子来源；`HATS` 标记为 `verified` 并指向 `taoyuan:equipment` 的 `kind: "hat"` 切片。
- `src/data/hats.ts` 保留原模块路径、`HATS` / `SHOP_HATS` / `CRAFTABLE_HATS` / 帽子掉落表原名重导出，以及 `getHatById(id)` 兼容查询；查询入口优先读取官方单件装备注册表并返回本地 ID `HatDef` 形状。
- `src/domain/mods/schemas.ts` 将 `EquipmentDefSchema.kind` 扩展为 `ring | hat`，并新增可选 `shopPrice` 字段；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 将旧帽子定义只读投影为 `taoyuan:equipment`；`src/domain/mods/semanticValidation.ts` 校验帽子本体 ID 和合成材料引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖单件装备公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧帽子顺序和字段全量等价、商店/合成帽子派生表等价、冻结只读结果、重复 ID 拒绝、缺失本体/材料引用诊断，以及旧 `getHatById()` 本地和命名空间 ID 兼容。
- 本切片未迁移武器、鞋子定义、装备合成流程、装备掉落结算、附魔生成、装备方案或装备存档结构；这些仍按既有数据与 Store/框架规则保留。

## 阶段 6 鞋子定义切片审计结果

- `src/data/shoeDefinitions.ts` 新增为旧格式鞋子定义、商店/合成派生表、怪物/宝箱/BOSS 鞋子分组的唯一静态叶子来源；`SHOES` 标记为 `verified` 并指向 `taoyuan:equipment` 的 `kind: "shoe"` 切片。
- `src/data/shoes.ts` 保留原模块路径、`SHOES` / `SHOP_SHOES` / `CRAFTABLE_SHOES` / 鞋子掉落表原名重导出，以及 `getShoeById(id)` 兼容查询；查询入口优先读取官方单件装备注册表并返回本地 ID `ShoeDef` 形状。
- `src/domain/mods/schemas.ts` 将 `EquipmentDefSchema.kind` 扩展为 `ring | hat | shoe`，继续以 TypeBox 作为单件装备定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 将旧鞋子定义只读投影为 `taoyuan:equipment`；`src/domain/mods/semanticValidation.ts` 校验鞋子本体 ID 和合成材料引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖单件装备公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧鞋子顺序和字段全量等价、商店/合成鞋子派生表等价、冻结只读结果、重复 ID 拒绝、缺失本体/材料引用诊断，以及旧 `getShoeById()` 本地和命名空间 ID 兼容。
- 本切片未迁移武器定义、装备合成流程、装备掉落结算、附魔生成、装备方案或装备存档结构；这些仍按既有数据与 Store/框架规则保留。

## 阶段 6 武器定义切片审计结果

- `src/data/weaponDefinitions.ts` 新增为旧格式武器定义、商店武器派生表、怪物/宝箱/BOSS 武器分组的唯一静态叶子来源；`WEAPONS` 标记为 `verified` 并指向 `taoyuan:equipment` 的 `kind: "weapon"` 分支。
- `src/data/enchantmentDefinitions.ts` 新增为旧附魔定义、稀有度、随机池和效果映射的唯一静态叶子来源；`src/data/weapons.ts` 保留原模块路径、附魔原名重导出、武器原名重导出、掉落表原名重导出以及 `getWeaponById(id)` / `getWeaponSellPrice()` / `getWeaponDisplayName()` 兼容入口。
- `src/domain/mods/schemas.ts` 将 `EquipmentDefSchema` 扩展为可穿戴装备和武器判别联合；武器分支保留旧 `weaponType`、`attack`、`critRate`、`shopPrice`、`shopMaterials`、`fixedEnchantment` 和 `sellPrice` 字段，继续以 TypeBox 作为单件装备定义契约唯一事实来源。
- `src/domain/mods/staticAdapters.ts` 将旧武器定义只读投影为 `taoyuan:equipment`；`src/domain/mods/contentAccess.ts` 新增武器注册表定义、旧对象形状、列表和单项读取入口，旧 `getWeaponById()` 优先读取注册表并返回本地 ID `WeaponDef` 形状。
- `src/domain/mods/semanticValidation.ts` 校验武器本体 ID、商店材料引用现有 `taoyuan:item`，并校验固定附魔引用现有 `taoyuan:enchantment`；缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖单件装备公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧武器顺序和字段全量等价、商店武器派生表等价、冻结只读结果、重复 ID 拒绝、缺失本体/材料/固定附魔引用诊断，以及旧 `getWeaponById()` 本地和命名空间 ID 兼容。
- 本切片未迁移武器实例、随机附魔、定制/祛除附魔、装备方案、怪物/宝箱掉落执行、BOSS 首杀奖励结算、战斗算法或装备存档结构；这些仍按既有 domain/store 规则保留。

## 阶段 6 加工机器制造定义切片审计结果

- `src/data/processingMachineDefinitions.ts` 新增为旧格式加工机器制造定义的唯一静态叶子来源；`PROCESSING_MACHINES` 标记为 `verified` 并指向 `taoyuan:processing_machine`。
- `src/data/processing.ts` 保留原模块路径、`PROCESSING_MACHINES` 原名重导出、`getMachineById(id)` 兼容查询，并新增 `getProcessingMachines()` 列表查询；查询入口优先读取官方加工机器注册表并返回本地 ID 形状。
- `src/data/processingCraftDefinitions.ts` 新增为洒水器、肥料、鱼饵、浮漂和炸弹的无注册表依赖叶子来源，用于避免 `contentAccess -> staticAdapters -> items -> processing -> contentAccess` 循环；洒水器和炸弹制造定义已由后续 `taoyuan:sprinkler` 与 `taoyuan:bomb` 切片迁移，肥料、鱼饵和浮漂的店铺展示字段已由既有 shop offer 验证，完整运行时效果仍保留在框架。
- `useProcessingStore.ts` 和 `ProcessingView.vue` 的加工机器名称、制造、退款和展示顺序读取已改为通过兼容门面读取；机器 ID、显示名、描述、制造材料、制造金币、自动收取标记、放置数量和旧存档 `machineType` 字段保持不变。
- 加工配方定义当时未在机器制造切片迁移，已由后续 `taoyuan:processing_recipe` 切片单独处理；酒坊多任务、种子制造机多任务、加工队列、自动收取时机、虚空箱回退、配方解锁、制造消耗顺序、退款和加工存档结构仍按 Store/框架规则保留。
- 定向测试覆盖加工机器公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧机器顺序和字段全量等价、冻结只读结果、重复 ID 拒绝、缺失制造材料引用诊断，以及熔炉制造/自动收取/退款闭环；本切片未迁移 NPC、叙事或世界活动。

## 阶段 6 加工配方定义切片审计结果

- `src/data/processingRecipeDefinitions.ts` 新增为 133 条旧格式加工配方定义的唯一静态叶子来源；`PROCESSING_RECIPES` 标记为 `verified` 并指向 `taoyuan:processing_recipe`。
- `src/data/processing.ts` 保留原模块路径和 `PROCESSING_RECIPES` 原名重导出；`getProcessingRecipeById(id)` 与 `getRecipesForMachine(machineType)` 兼容查询优先读取官方加工配方注册表并返回本地 ID 形状。
- `src/domain/mods/schemas.ts` 新增 `ProcessingRecipeDefSchema` 和公开 `processing-recipe.schema.json`，继续以 TypeBox 作为加工配方定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧加工配方只读投影为 `taoyuan:processing_recipe`，并提供注册表定义、列表、单项读取和按机器筛选入口；配方 ID、机器 ID、输入/输出物品、数量、加工天数、名称和描述保持不变。
- `src/domain/mods/semanticValidation.ts` 校验配方 `machineId` 引用现有 `taoyuan:processing_machine`，`inputItemId` 和 `outputItemId` 引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖加工配方公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧配方顺序和字段全量等价、按机器筛选等价、冻结只读结果、重复 ID 拒绝、缺失机器/物品引用诊断，以及熔炉有输入自动收取和蜂箱无输入自动重启闭环。
- 本切片未迁移加工运行队列、自动收取算法、虚空箱回退、多任务酒坊/种子制造机算法、配方解锁、制造消耗顺序、退款、加工存档结构、NPC、叙事或世界活动；这些仍按既有 Store/框架规则保留。

## 阶段 6 炸弹制造定义切片审计结果

- `src/data/processingCraftDefinitions.ts:BOMBS` 继续作为 3 条旧格式炸弹制造定义的唯一无注册表依赖叶子来源，状态从 `baselined` 更新为 `verified` 并指向 `taoyuan:bomb`。
- `src/domain/mods/schemas.ts` 新增 `BombDefSchema` 和公开 `bomb.schema.json`，继续以 TypeBox 作为炸弹定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧炸弹只读投影为 `taoyuan:bomb`，按原顺序提供注册表定义、列表、单项读取和旧本地 `BombDef` 兼容形状；材料引用缺失继续输出 `REG-REFERENCE-001`。
- `src/data/processing.ts` 保留 `BOMBS` 原名重导出；`getBombById()` 保留旧签名、旧本地返回形状、未知 ID 返回 `undefined` 和静态回退，并优先读取官方炸弹注册表。
- 定向测试逐项覆盖 ID、名称、描述、矿石倍率、清怪标记、材料、金币、售价、顺序、冻结结果、重复 ID、缺失材料引用、物品派生、工坊制造、矿洞使用、矿洞操作列表和本地背包 ID；未修改 Store、Vue、Electron、打包配置或存档路径。
- 本切片未迁移制造消耗顺序、巨型炸弹配方门槛、爆炸范围算法、矿石/怪物结算、随机调用、日志或存档结构；这些仍按既有 Store/框架规则保留。

## 阶段 6 秘密纸条定义切片审计结果

- `src/data/secretNotes.ts` 保留为 28 条旧格式秘密纸条定义的唯一静态叶子来源；`SECRET_NOTES` 标记为 `verified` 并指向 `taoyuan:secret_note`。
- `src/domain/mods/schemas.ts` 新增 `SecretNoteDefSchema` 和公开 `secret-note.schema.json`，继续以 TypeBox 作为秘密纸条定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧秘密纸条只读投影为 `taoyuan:secret_note`，并提供注册表定义、列表和旧数字 ID 兼容读取入口；纸条数字 ID、类型、标题、正文、可使用状态和奖励保持不变。
- `src/domain/mods/semanticValidation.ts` 校验宝藏奖励物品引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- `useSecretNoteStore.ts` 和 `AchievementView.vue` 的秘密纸条总数、随机收集池、使用奖励和列表展示读取已改为通过官方注册表兼容门面读取；旧 `SECRET_NOTES` 导出保留为回滚路径。
- 定向测试覆盖秘密纸条公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧纸条顺序和字段全量等价、数字 ID 与内容 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失奖励物品引用诊断，以及收集、使用奖励和旧存档恢复闭环。
- 本切片未迁移纸条获得概率、钓鱼/挖矿触发入口、奖励发放 UI、收集/使用状态存档结构、NPC、剧情任务、成就、博物馆、行会或世界活动；这些仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 教程提示定义切片审计结果

- `src/data/tutorials.ts` 保留为 18 条旧格式晨间教程提示定义的唯一静态叶子来源；`MORNING_TIPS` 标记为 `verified` 并指向 `taoyuan:tutorial`。
- `src/domain/mods/schemas.ts` 新增 `TutorialDefSchema` 和公开 `tutorial.schema.json`，继续以 TypeBox 作为教程提示定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧教程提示只读投影为 `taoyuan:tutorial`，并提供注册表定义、列表、单项读取和旧 `MorningTipDef` 兼容形状。
- `useEndDay.ts` 的晨间教程提示读取已改为通过官方注册表兼容门面；条件判定、优先级排序、临时标记清理、日志和 `tutorial.shownTipIds` 本地字符串存档结构保持不变。
- 定向测试覆盖教程公开契约、外部 JSON `unknown` 校验、非法字段和未知条件键拒绝、旧提示顺序/字段等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、无语义诊断，以及隔夜晨间提示选择和低体力标记清理闭环。
- 本切片未迁移教程开关设置、已展示提示存档、访问面板状态、条件求值函数、NPC、剧情任务、成就、博物馆、行会或世界活动；这些仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 博物馆定义切片审计结果

- `src/data/museumDefinitions.ts` 新增为 6 个博物馆分类、40 个可捐赠条目和 8 个里程碑奖励的唯一静态叶子来源；`MUSEUM_CATEGORIES`、`MUSEUM_ITEMS` 和 `MUSEUM_MILESTONES` 标记为 `verified`，分别指向 `taoyuan:museum_category`、`taoyuan:museum_item` 和 `taoyuan:museum_milestone`。
- `src/data/museum.ts` 保留原模块路径、三个旧常量原名重导出和 `getMuseumItemById()` 兼容查询；新增 `getMuseumCategories()`、`getMuseumItems()`、`getMuseumMilestones()` 和 `getMuseumMilestoneByCount()`，查询入口优先读取官方注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `MuseumCategoryDefSchema`、`MuseumItemDefSchema`、`MuseumMilestoneDefSchema` 和三个公开 Schema，继续以 TypeBox 作为博物馆定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧博物馆定义只读投影为官方注册表，并提供分类、条目、里程碑的注册表定义、列表、单项读取和旧对象形状适配。
- `src/domain/mods/semanticValidation.ts` 校验博物馆条目物品、分类和里程碑奖励物品引用，缺失引用继续输出 `REG-REFERENCE-001`。
- `useMuseumStore.ts` 和 `MuseumView.vue` 的总数、分类展示、捐赠校验、单项查找和里程碑奖励读取已改为通过官方注册表兼容门面读取；`donatedItems` / `claimedMilestones` 旧存档结构、捐赠扣料、奖励发放顺序和 UI 文案保持不变。
- 定向测试覆盖博物馆公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧分类/条目/里程碑顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失引用诊断，以及捐赠、里程碑奖励和旧存档恢复闭环。
- 本切片未迁移成就、NPC、行会、世界活动、捐赠状态存档结构、奖励 UI、Electron、打包配置或存档路径；这些仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 行会定义切片审计结果

- `src/data/guildDefinitions.ts` 新增为旧格式行会讨伐目标、捐献点数、等级阈值、公会商店条目和等级被动增益的唯一静态叶子来源；`MONSTER_GOALS`、`GUILD_DONATIONS` 和 `GUILD_LEVELS` 标记为 `verified`，分别指向 `taoyuan:guild_goal`、`taoyuan:guild_donation` 和 `taoyuan:guild_level`。
- `src/data/guild.ts` 保留原模块路径、五个旧常量原名重导出和 `getMonsterGoalByMonsterId()` / `getGuildDonationByItemId()` 等兼容查询；查询入口优先读取官方注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `GuildGoalDefSchema`、`GuildDonationDefSchema`、`GuildLevelDefSchema` 和三个公开 Schema，继续以 TypeBox 作为行会定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧行会目标、捐献表和等级表只读投影为官方注册表，并提供注册表定义、列表、单项读取和旧对象形状适配。
- `src/domain/mods/semanticValidation.ts` 校验行会目标怪物引用、目标奖励物品引用和捐献物品引用，缺失引用继续输出 `REG-REFERENCE-001`。
- `useGuildStore.ts` 和 `GuildView.vue` 的讨伐目标、捐献列表和等级阈值读取已改为通过官方注册表兼容门面；`monsterKills` / `claimedGoals` / 贡献点 / 经验 / 等级旧存档结构、奖励发放、捐献扣料、升级循环和 UI 文案保持不变。
- `GUILD_SHOP_ITEMS` 继续由既有 `taoyuan:shop_offer` 投影负责，`GUILD_BONUS_PER_LEVEL` 仍标记为框架规则；本切片未迁移公会商店购买流程、限购状态、贡献点被动加成算法、Electron、打包配置或存档路径。
- 定向测试覆盖行会公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧目标/捐献/等级顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失引用诊断，以及讨伐领取、捐献升级和旧存档补点迁移闭环。

## 阶段 6 成就与祠堂任务切片审计结果

- `src/data/achievementDefinitions.ts` 新增为旧格式成就定义和祠堂任务定义的唯一静态叶子来源；`ACHIEVEMENTS` 和 `COMMUNITY_BUNDLES` 标记为 `verified`，分别指向 `taoyuan:achievement` 和 `taoyuan:community_bundle`。
- `src/data/achievements.ts` 保留原模块路径、两个旧常量原名重导出和 `getAchievementById()` / `getBundleById()` 兼容查询；新增 `getAchievements()` 和 `getCommunityBundles()`，查询入口优先读取官方注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `AchievementDefSchema`、`CommunityBundleDefSchema` 和公开 `achievement.schema.json` / `community-bundle.schema.json`，继续以 TypeBox 作为成就与祠堂任务定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧成就和祠堂任务只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧对象形状适配。
- `src/domain/mods/semanticValidation.ts` 校验成就 `itemDiscovered` 条件、成就奖励物品、祠堂任务提交物品和祠堂奖励物品引用，缺失引用继续输出 `REG-REFERENCE-001`。
- `useAchievementStore.ts` 和 `AchievementView.vue` 的成就列表、完成计数、奖励领取、祠堂任务列表和提交读取已改为通过官方注册表兼容门面读取；`completedAchievements`、`completedBundles`、`bundleSubmissions` 和 `stats` 旧存档结构保持不变。
- 本切片未迁移成就进度统计函数、任务/NPC/世界活动条件源、成就弹窗、奖励发放 UI、祠堂提交状态存档结构、Electron、打包配置或存档路径；这些仍按既有 Store/框架规则保留。
- 定向测试覆盖成就/祠堂任务公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧成就/祠堂任务顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失引用诊断，以及成就奖励、祠堂提交和旧存档补字段迁移闭环。

## 阶段 6 普通 NPC 定义切片审计结果

- `src/data/npcDefinitions.ts` 新增为旧格式普通村民 NPC 定义的唯一静态叶子来源；`NPCS` 标记为 `verified` 并指向 `taoyuan:npc`。
- `src/data/npcs.ts` 保留原模块路径、`NPCS` 原名重导出、`getNpcById(id)` 旧签名，并新增 `getNpcs()` 注册表兼容列表；查询入口优先读取官方 NPC 注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `NpcDefSchema` 和公开 `npc.schema.json`，继续以 TypeBox 作为普通 NPC 定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧 `NPCS` 只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧对象形状适配；NPC ID、显示文本、性别、角色、性格、喜好、对话、生日和兼容心事件 ID 保持不变。
- `src/domain/mods/semanticValidation.ts` 校验 NPC 喜爱、喜欢和讨厌物品引用现有 `taoyuan:item`，并由后续 `heart_event` 切片校验 NPC 心事件 ID 引用现有 `taoyuan:heart_event`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useNpcStore.ts`、`NpcView.vue` 和 `CottageView.vue` 的 NPC 初始状态、旧存档补新增 NPC、生日读取、普通村民列表、一键聊天和一键送礼已改为通过注册表兼容门面读取；`npcStates`、配偶、知己、雇工、子女和好感相关旧存档字段仍保存本地 NPC ID。
- 定向测试覆盖 NPC 公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧 NPC 顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失礼物引用诊断，以及 Store 初始化、聊天、送礼和旧存档补字段迁移闭环。
- 本切片未迁移好感事件场景、剧情任务、普通任务、每日事件、NPC 好感状态、求婚、知己、雇工、孕育、子女、Electron、打包配置或存档路径；隐藏 NPC 定义已由后续 `taoyuan:hidden_npc` 切片单独迁移，好感事件定义已由后续 `taoyuan:heart_event` 切片单独迁移，其余仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 隐藏 NPC 定义切片审计结果

- `src/data/hiddenNpcDefinitions.ts` 新增为旧格式隐藏仙灵 NPC 定义的唯一静态叶子来源；`HIDDEN_NPCS` 标记为 `verified` 并指向 `taoyuan:hidden_npc`。
- `src/data/hiddenNpcs.ts` 保留原模块路径、`HIDDEN_NPCS` 原名重导出和 `getHiddenNpcById(id)` 旧签名，并新增 `getHiddenNpcs()` 注册表兼容列表；查询入口优先读取官方隐藏 NPC 注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `HiddenNpcDefSchema` 和公开 `hidden-npc.schema.json`，继续以 TypeBox 作为隐藏 NPC 定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧 `HIDDEN_NPCS` 只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧对象形状适配；隐藏 NPC ID、显示文本、发现条件、供奉喜好、亲密对话、求缘/结缘材料、结缘奖励、能力和显现生日保持不变。
- `src/domain/mods/semanticValidation.ts` 校验供奉物品、求缘/结缘物品、制造材料、物品发现条件、普通 NPC 好感条件、剧情任务完成条件和鱼类捕获条件引用，并由后续 `heart_event` 切片校验隐藏 NPC 心事件 ID 引用现有 `taoyuan:heart_event`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useHiddenNpcStore.ts` 的初始状态、旧存档补新增隐藏 NPC、发现条件、供奉、能力解锁和每日结缘被动读取已改为通过注册表兼容门面；`useDialogs.ts` 的隐藏 NPC 心事件 ID 判断也改为通过兼容查询。
- 定向测试覆盖隐藏 NPC 公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧隐藏 NPC 顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失物品/NPC/剧情任务/鱼类引用诊断，以及 Store 初始化、发现、供奉、能力解锁、每日结缘被动和旧存档补字段迁移闭环。
- 本切片未迁移好感事件场景、普通任务、每日事件、隐藏 NPC 亲密状态存档结构、求缘/结缘流程副作用、被动效果执行、Electron、打包配置或存档路径；好感事件定义已由后续 `taoyuan:heart_event` 切片单独迁移，其余仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 剧情任务定义切片审计结果

- `src/data/storyQuestDefinitions.ts` 新增为旧格式主线剧情任务定义的唯一静态叶子来源；`STORY_QUESTS` 标记为 `verified` 并指向 `taoyuan:story_quest`，`CHAPTER_TITLES` 标记为 UI 章节标签。
- `src/data/storyQuests.ts` 保留原模块路径、`STORY_QUESTS` / `CHAPTER_TITLES` 原名重导出和全部旧查询函数；查询入口优先读取官方剧情任务注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `StoryQuestDefSchema` 和公开 `story-quest.schema.json`，继续以 TypeBox 作为剧情任务定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧 `STORY_QUESTS` 只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取、章节/顺序/下一任务读取和旧对象形状适配；任务 ID、章节顺序、显示文本、目标和奖励保持不变。
- `skillLevel` 目标缺省 `skillType` 时保持旧“任意技能达到目标等级”语义；注册表往返不会静默补成农耕或其他单一技能。
- `src/domain/mods/semanticValidation.ts` 校验发布 NPC、交付物品目标、NPC 好感目标、好感奖励和物品奖励引用，缺失引用继续输出 `REG-REFERENCE-001`；`npcFriendship.npcId: "_any"` 保留旧任意 NPC 语义。
- `useQuestStore.ts` 的主线任务初始化、接取、进度检查、提交、下一任务推进和完成总数读取已改为通过注册表兼容门面；`mainQuest`、`completedMainQuests` 和目标进度旧存档字段仍保存本地任务 ID。
- 定向测试覆盖剧情任务公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧剧情任务顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失 NPC/物品引用诊断，以及 Quest Store 初始化、接取、提交和旧存档恢复闭环。
- 本切片未迁移普通任务、好感事件场景、每日事件、NPC 好感状态、奖励发放 UI、Electron、打包配置或存档路径；隐藏 NPC 定义已由 `taoyuan:hidden_npc` 切片单独迁移，好感事件定义已由 `taoyuan:heart_event` 切片单独迁移，其余仍按既有 Store/框架规则或后续内容域保留。

## 阶段 6 好感事件定义切片审计结果

- `src/data/heartEventDefinitions.ts` 和 `src/data/hiddenNpcHeartEventDefinitions.ts` 新增为旧格式普通 NPC 与隐藏 NPC 好感事件定义的唯一静态叶子来源；`HEART_EVENTS` 与 `HIDDEN_NPC_HEART_EVENTS` 标记为 `verified` 并指向 `taoyuan:heart_event`。
- `src/data/heartEvents.ts` 与 `src/data/hiddenNpcHeartEvents.ts` 保留原模块路径、旧导出和 `getHeartEventsForNpc()` / `getHeartEventById()` / `getHiddenNpcHeartEvents()` / `getHiddenNpcHeartEventById()` 旧签名；查询入口优先读取官方好感事件注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `HeartEventDefSchema` 和公开 `heart-event.schema.json`，继续以 TypeBox 作为好感事件定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧普通和隐藏 NPC 好感事件只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧对象形状适配；事件 ID、所属 NPC、好感阈值、知己要求、标题、场景、选项和好感变化保持不变。
- `src/domain/mods/semanticValidation.ts` 校验好感事件所属对象引用现有 `taoyuan:npc` 或 `taoyuan:hidden_npc`，并校验普通/隐藏 NPC 定义中声明的好感事件引用现有 `taoyuan:heart_event`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useNpcStore.ts` 与 `useHiddenNpcStore.ts` 的好感事件触发检查已改为通过注册表兼容门面读取；`triggeredHeartEvents`、好感数值、选项响应结算、求婚/结缘流程和旧存档字段仍保存旧本地事件 ID。
- `wedding_ceremony` 仍作为框架婚礼事件保留在旧 `getHeartEventById()` 兼容路径，不进入 `taoyuan:heart_event` 快照；定向测试覆盖这一框架保留结论。
- 定向测试覆盖好感事件公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧普通/隐藏 NPC 好感事件顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失事件归属/声明引用诊断，以及普通/隐藏 NPC Store 触发检查和旧本地 ID 记录闭环。

## 阶段 6 季节节日事件定义切片审计结果

- `src/data/seasonEventDefinitions.ts` 新增为旧格式季节节日事件定义的唯一静态叶子来源；`SEASON_EVENTS` 标记为 `verified` 并指向 `taoyuan:season_event`。
- `src/data/events.ts` 保留原模块路径、`SeasonEventDef` 类型重导出、`SEASON_EVENTS` 和 `getTodayEvent()` 旧签名；查询入口优先读取官方季节节日事件注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `SeasonEventDefSchema` 和公开 `season-event.schema.json`，继续以 TypeBox 作为季节节日事件定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧季节节日事件只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧 `SeasonEventDef` 对象形状适配；事件 ID、季节、日期、奖励、旁白顺序和互动节日类型保持不变。
- `src/domain/mods/semanticValidation.ts` 校验节日奖励物品引用现有 `taoyuan:item`；缺失引用继续输出 `REG-REFERENCE-001`。
- `getTodayEvent()`、隔夜节日事件处理入口和农舍日历节日展示已改为通过注册表兼容门面读取；节日奖励结算、食谱解锁、BGM/弹窗副作用、`{year}` 文案替换和旧存档结构仍由既有框架处理。
- 定向测试覆盖季节节日事件公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧事件顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失奖励物品引用诊断，以及隔夜节日效果和互动节日展示闭环。

## 阶段 6 普通任务模板切片审计结果

- `src/data/questDefinitions.ts` 新增为旧格式公告板普通委托模板和特殊订单模板的唯一静态叶子来源；`QUEST_TEMPLATES` 和 `SPECIAL_ORDER_TEMPLATES` 标记为 `verified` 并指向 `taoyuan:quest_template`。
- `src/data/quests.ts` 保留原模块路径、`QUEST_TEMPLATES`、`QUEST_TYPE_LABELS`、`SpecialOrderTemplate`、`generateQuest()`、`generateSpecialOrder()`、`getQuestTemplates()` 和 `getSpecialOrderTemplates()` 旧签名；查询和生成入口优先读取官方任务模板注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `QuestTemplateDefSchema` 和公开 `quest-template.schema.json`，继续以 TypeBox 作为任务模板定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧普通委托模板和特殊订单模板只读投影为官方注册表，并提供注册表定义、列表、内容 ID / 本地 ID 兼容读取和旧对象形状适配；任务类型、目标物品、数量范围、季节、NPC 池、奖励倍率、好感奖励、特殊订单阶梯和奖励保持不变。
- `src/domain/mods/semanticValidation.ts` 校验任务目标物品、特殊订单奖励物品和 NPC 引用现有 `taoyuan:item` / `taoyuan:npc`；缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖任务模板公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧公告板/特殊订单模板顺序和字段全量等价、内容 ID 与旧类型查询兼容、冻结只读结果、重复 ID 拒绝、缺失目标/NPC/奖励引用诊断，以及公告板任务生成、接取、提交、扣料、奖励和特殊订单生成闭环。

## 阶段 6 晨间随机事件定义切片审计结果

- `src/data/farmEventDefinitions.ts` 新增为旧格式晨间随机事件定义的唯一静态叶子来源；`MORNING_NARRATIONS`、`MORNING_CHOICE_EVENTS` 和 `MORNING_EASTER_EGGS` 标记为 `verified` 并指向 `taoyuan:morning_event`。
- `src/data/farmEvents.ts` 保留原模块路径、旧类型重导出、旧数组原名导出以及 `getMorningNarrations()` / `getMorningChoiceEvents()` / `getMorningEasterEggs()` / `getNoLossMorningNarrations()` 兼容查询；查询入口优先读取官方晨间事件注册表兼容门面。
- `src/domain/mods/schemas.ts` 新增 `MorningEventDefSchema` 和公开 `morning-event.schema.json`，继续以 TypeBox 作为晨间事件定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧旁白、带选项事件和彩蛋只读投影为 `taoyuan:morning_event`，并提供内容 ID / 本地 ID 兼容读取和旧对象形状适配；文本、选择项、效果类型、数量和顺序保持不变。
- `src/domain/mods/semanticValidation.ts` 校验晨间事件 `gainItem` 效果引用现有 `taoyuan:item`，缺失引用继续输出 `REG-REFERENCE-001`。
- `useEndDay.ts` 的晨间随机事件输入已改为通过注册表兼容 getter 读取；触发概率、随机调用顺序、作物破坏、物品/金钱/好感效果、选择弹窗副作用和旧存档结构保持不变。
- 定向测试覆盖晨间事件公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧旁白/选择事件/彩蛋顺序和字段全量等价、内容 ID 与本地 ID 查询兼容、冻结只读结果、重复 ID 拒绝、缺失物品引用诊断，以及固定随机下的选择事件和彩蛋效果闭环。

## 阶段 6 育种杂交定义切片审计结果

- `src/data/breedingDefinitions.ts` 新增为 400 条旧格式杂交品种定义的唯一静态叶子来源；`HYBRID_DEFINITIONS` 标记为 `verified` 并指向 `taoyuan:breeding_hybrid`，`HYBRID_TIER_COUNTS` 作为阶层边界派生数据保留并验证。
- `src/data/breeding.ts` 保留原模块路径、旧类型导入、`HYBRID_DEFS` 原名导出和 `getHybridTier()` / `findPossibleHybrid()` / `findPossibleHybridById()` 兼容查询；旧入口优先读取官方育种杂交注册表并返回本地 ID 兼容对象。
- `src/domain/mods/schemas.ts` 新增 `BreedingHybridDefSchema` 和公开 `breeding-hybrid.schema.json`，继续以 TypeBox 作为育种杂交定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧杂交定义只读投影为 `taoyuan:breeding_hybrid`，并提供内容 ID / 本地 ID 兼容读取、父本匹配、ID 查询、旧对象形状适配和阶层计算；父本、门槛、结果作物、基础基因、发现文本和顺序保持不变。
- `src/domain/mods/semanticValidation.ts` 校验 `parentCropA`、`parentCropB` 和 `resultCropId` 引用现有 `taoyuan:crop`，缺失引用继续输出 `REG-REFERENCE-001`。
- 定向测试覆盖育种杂交公开契约、外部 JSON `unknown` 校验、非法字段拒绝、旧定义顺序和字段全量等价、本地/前缀/命名空间 ID 查询兼容、父本匹配、阶层计算、冻结只读结果、重复 ID 拒绝、缺失作物引用诊断，以及固定随机下的育种完成、图鉴记录和日志闭环。


## 阶段 6 旅行商人商品池切片审计结果

- `src/data/travelingMerchant.ts` 保留为旅行商人稀有商品池和库存生成框架算法的兼容入口；`TRAVELING_MERCHANT_POOL` 标记为 `verified` 并指向既有 `taoyuan:shop_offer`。
- `src/domain/mods/staticAdapters.ts` 继续把旅行商人池投影为 `shopId: "traveling_merchant"`、`groupId: "traveling"` 的官方商品 Offer，商品 ID、名称、基础价格和排序保持不变。
- `src/domain/mods/contentAccess.ts` 新增 `getOfficialTravelingMerchantOffers()` 和 `getOfficialTravelingMerchantPoolAsLegacy()`，为库存生成和隐藏 NPC 额外商品恢复旧 `TravelingMerchantItem` 形状。
- `generateMerchantStock()` 和 `useShopStore.refreshMerchantStock()` 已通过注册表兼容门面读取稀有商品池；每日库存随机、反季种子、价格下限、购买流程、`travelingStockKey` / `travelingStock` 存档结构保持不变。
- 本切片未迁移出摊日算法、确定性随机数生成器、反季作物选择、狐仙额外商品随机选择、市场、瀚海或行会商店；这些仍按既有 Store/框架规则或后续内容域保留。
- 定向测试覆盖 `taoyuan:shop_offer` 与旧旅行商人池的顺序/字段等价，以及四季固定日期下 `generateMerchantStock()` 与旧池算法输出完全一致。

## 阶段 6 市场行情分类切片审计结果

- `src/data/marketDefinitions.ts` 新增为 7 个旧格式市场行情分类定义的唯一静态叶子来源；`MARKET_CATEGORY_DEFINITIONS` 标记为 `verified` 并指向 `taoyuan:market_category`。
- `src/data/market.ts` 保留原模块路径、`MarketCategory`、`MARKET_CATEGORY_NAMES`、`getMarketMultiplier()` 和 `getDailyMarketInfo()` 旧签名；行情分类、名称、季节系数和供需阈值优先通过官方注册表兼容门面读取。
- `src/domain/mods/schemas.ts` 新增 `MarketCategoryDefSchema` 和公开 `market-category.schema.json`，继续以 TypeBox 作为市场分类定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `useEndDay.ts` 的隔夜市场日志分类名称已改为通过 `getMarketCategoryName()` 读取；天气、行情日志顺序、随机种子、随机调用顺序、趋势分类、价格系数 clamp、未知分类返回 `1.0` 和旧存档结构保持不变。
- 本切片未迁移出货历史统计、商店售价结算、瀚海、行会商店、Electron、打包配置或存档路径；这些仍按既有 Store/框架规则或后续内容域保留。
- 定向测试覆盖 `taoyuan:market_category` 与旧分类定义的字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、防御性拷贝、每日行情和单分类倍率与旧算法副本完全一致。

## 阶段 6 瀚海通商兑换定义切片审计结果

- `src/data/hanhaiDefinitions.ts` 新增为 6 条旧格式瀚海通商积分兑换定义的唯一静态叶子来源；`TRADE_EXCHANGE_ITEMS` 标记为 `verified` 并指向 `taoyuan:hanhai_trade_exchange`。
- `src/data/hanhai.ts` 保留原模块路径和 `TRADE_EXCHANGE_ITEMS` 原名重导出；固定/轮换商店商品、店铺升级读取、售货槽、通商积分结算和所有瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/schemas.ts` 新增 `HanhaiTradeExchangeDefSchema` 和公开 `hanhai-trade-exchange.schema.json`，继续以 TypeBox 作为瀚海兑换定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧兑换定义只读投影为官方注册表，并提供内容 ID / 本地 itemId 兼容读取和旧对象形状适配；兑换目标、名称、描述、积分成本、周限、总限、钱袋标记和装备标记保持不变。
- `src/domain/mods/semanticValidation.ts` 校验普通兑换目标引用 `taoyuan:item`，钱袋兑换目标引用 `taoyuan:wallet_item`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useHanhaiStore.exchangeItem()` 和 `HanhaiView.vue` 的积分兑换列表已改为通过注册表兼容门面读取；积分扣除、购买记录、香料礼包发放 5 个西域香料、钱袋解锁、背包失败回滚、日志和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:hanhai_trade_exchange` 与旧定义字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、缺失普通物品/钱袋物品引用诊断，以及香料礼包和通商金印兑换分支。

## 阶段 6 瀚海驿站商品池切片审计结果

- `src/data/hanhaiShopDefinitions.ts` 新增为 4 条旧格式瀚海常驻商品和 10 条旧格式每周轮换候选商品的唯一静态叶子来源；`HANHAI_FIXED_ITEMS` 和 `HANHAI_ROTATING_POOL` 标记为 `verified` 并指向既有 `taoyuan:shop_offer`。
- `src/data/hanhai.ts` 保留原模块路径和 `HANHAI_FIXED_ITEMS`、`HANHAI_ROTATING_POOL` 原名重导出，以及 `getWeeklyRotatingItems()` 旧签名；店铺升级读取、售货槽、通商积分结算、藏宝图随机奖励和所有瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧固定/轮换商品只读投影为官方商品 Offer，并提供旧 `HanhaiShopItemDef` 形状、常驻商品列表、轮换候选池和每周确定性轮换结果的兼容读取；商品 ID、名称、描述、价格、周限购和排序保持不变。
- `src/domain/mods/semanticValidation.ts` 继续通过统一 `taoyuan:shop_offer` 规则校验商品 `itemId` 引用，缺失引用继续输出 `REG-REFERENCE-001`。
- `useHanhaiStore.getWeeklyRemaining()`、`buyShopItem()`、`refreshRotatingStock()` 和 `HanhaiView.vue` 常驻商品列表已改为通过注册表兼容门面读取；购买扣款、背包失败回滚、周购买计数、每周重置和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:shop_offer` 与旧瀚海固定/轮换商品池字段/顺序等价、描述字段投影、本地旧入口重导出、确定性每周轮换算法、缺失商品引用诊断，以及 Store 刷新轮换商品和购买限购分支。

## 阶段 6 瀚海通商店铺升级定义切片审计结果

- `src/data/hanhaiTradeShopDefinitions.ts` 新增为 5 条旧格式瀚海通商店铺升级定义的唯一静态叶子来源；`TRADE_SHOP_UPGRADES` 标记为 `verified` 并指向 `taoyuan:hanhai_trade_shop_upgrade`。
- `src/data/hanhai.ts` 保留原模块路径和 `TRADE_SHOP_UPGRADES` 原名重导出；售货槽实例、通商积分结算、藏宝图随机奖励和所有瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/schemas.ts` 新增 `HanhaiTradeShopUpgradeDefSchema` 和公开 `hanhai-trade-shop-upgrade.schema.json`，继续以 TypeBox 作为瀚海店铺升级定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧升级定义只读投影为官方注册表，并提供 level / 内容 ID 兼容读取和旧 `TradeShopUpgradeDef` 对象形状适配；等级、名称、槽位、售卖周期、费用和材料保持不变。
- `src/domain/mods/semanticValidation.ts` 校验升级材料引用 `taoyuan:item`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useHanhaiStore.tradeShopConfig` 与 `nextTradeShopUpgrade` 已改为通过注册表兼容门面读取；升级扣款、扣料、等级字段、槽位上限、售卖周期、日志文案和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:hanhai_trade_shop_upgrade` 与旧定义字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、缺失升级材料引用诊断，以及 Store 升级扣钱扣料和满级分支。

## 阶段 6 瀚海藏宝图奖励定义切片审计结果

- `src/data/hanhaiTreasureDefinitions.ts` 新增为 4 条旧格式瀚海藏宝图奖励档位的唯一静态叶子来源；`HANHAI_TREASURE_REWARDS` 标记为 `verified` 并指向 `taoyuan:hanhai_treasure_reward`。
- `src/data/hanhai.ts` 保留原模块路径和 `HANHAI_TREASURE_REWARDS` 原名重导出；售货槽实例、通商积分结算、藏宝图使用流程和所有瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/schemas.ts` 新增 `HanhaiTreasureRewardDefSchema` 和公开 `hanhai-treasure-reward.schema.json`，继续以 TypeBox 作为瀚海藏宝图奖励定义契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧奖励档位只读投影为官方注册表，并提供本地档位 ID、内容 ID、完整命名空间 ID、旧 `HanhaiTreasureRewardDef` 对象形状和按随机值选择档位的兼容读取；阈值、金钱、物品 ID、显示名、数量和档位顺序保持不变。
- `src/domain/mods/semanticValidation.ts` 校验奖励物品引用 `taoyuan:item`；缺失引用继续输出 `REG-REFERENCE-001`。
- `useHanhaiStore.useTreasureMap()` 已改为通过注册表兼容门面读取；藏宝图扣除、单次 `Math.random()` 调用、奖励发放、物品发放失败忽略、奖励文本、日志和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:hanhai_treasure_reward` 与旧定义字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、缺失奖励物品引用诊断、三种 ID 查询兼容、随机阈值选择，以及大奖和安慰奖 Store 分支。

## 阶段 6 瀚海幸运轮盘固定配置切片审计结果

- `src/data/hanhaiRouletteDefinitions.ts` 新增为 1 条旧格式幸运轮盘固定配置的唯一静态叶子来源；`ROULETTE_OUTCOMES`、`ROULETTE_BET_TIERS` 和 `HANHAI_ROULETTE_CONFIG` 标记为 `verified` 并指向 `taoyuan:hanhai_roulette`。
- `src/data/hanhai.ts` 保留原模块路径和 `ROULETTE_OUTCOMES`、`ROULETTE_BET_TIERS`、`HANHAI_ROULETTE_CONFIG` 原名重导出；骰子、猜杯、斗蛐蛐、扑克、轮盘转动动画和其他瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/schemas.ts` 新增 `HanhaiRouletteDefSchema` 和公开 `hanhai-roulette.schema.json`，继续以 TypeBox 作为瀚海幸运轮盘固定配置契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧结果档位和投注档位只读投影为官方注册表，并提供本地 ID、内容 ID、完整命名空间 ID、旧 `RouletteOutcome[]` 和投注档位数组的兼容读取；标签、倍率、概率、档位顺序和投注金额保持不变。
- `src/domain/mods/semanticValidation.ts` 校验结果概率合计为 100 且投注档位不重复；异常继续输出 `SCHEMA-VALIDATE-001`。
- `spinRoulette()`、`useHanhaiStore.playRoulette()` 和 `HanhaiView.vue` 已改为通过注册表兼容门面读取；单次 `Math.random()` 调用、阈值选择、无效下注不掷随机、扣款、派奖、每日次数、结果弹窗、日志和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:hanhai_roulette` 与旧定义字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、概率合计和重复投注档位诊断、三种 ID 查询兼容、随机阈值选择，以及 Hanhai Store 无效/有效下注分支。

## 阶段 6 瀚海赌坊固定投注配置切片审计结果

- `src/data/hanhaiCasinoDefinitions.ts` 新增为 2 条旧格式瀚海赌坊固定投注配置的唯一静态叶子来源；`HANHAI_CASINO_WAGERS`、`DICE_BET_AMOUNT`、`DICE_WIN_MULTIPLIER`、`CUP_BET_AMOUNT` 和 `CUP_WIN_MULTIPLIER` 标记为 `verified` 并指向 `taoyuan:hanhai_casino_wager`。
- `src/data/hanhai.ts` 保留原模块路径和上述常量原名重导出；掷骰、猜杯藏球、斗蛐蛐、扑克、轮盘选择/动画和其他瀚海小游戏算法继续按框架或后续内容域保留。
- `src/domain/mods/schemas.ts` 新增 `HanhaiCasinoWagerDefSchema` 和公开 `hanhai-casino-wager.schema.json`，继续以 TypeBox 作为瀚海赌坊固定投注配置契约唯一事实来源；外部 JSON 在 Ajv 校验前仍按 `unknown` 处理。
- `src/domain/mods/staticAdapters.ts` 与 `src/domain/mods/contentAccess.ts` 将旧骰子/猜杯投注配置只读投影为官方注册表，并提供本地 ID、内容 ID、完整命名空间 ID、旧 `HanhaiCasinoWagerDef` 对象形状和投注金额/派奖倍率的兼容读取；金额、倍率和条目顺序保持不变。
- `useHanhaiStore.playDice()`、`useHanhaiStore.playCup()` 和 `HanhaiView.vue` 已改为通过注册表兼容门面读取；扣款、派奖、每日次数、随机调用次数、日志和旧存档结构保持不变。
- 定向测试覆盖 `taoyuan:hanhai_casino_wager` 与旧定义字段/顺序等价、外部 JSON `unknown` 校验、非法字段拒绝、三种 ID 查询兼容，以及骰子/猜杯 Store 分支的金额、赔率和随机调用次数。

## 阶段 6 瀚海小游戏机制边界审计结果

- 本轮未新增注册表、未迁移玩法代码，也未继续把瀚海小游戏拆成零散常量；`src/data/hanhai.ts` 的剩余小游戏符号改为明确归类。
- `MAX_DAILY_BETS`、`CARD_TOTAL`、`getTexasTier` 标记为 `framework-retained`，目标归属为 `engine/domain/hanhai-mechanism-plugin`；这些符号依赖每日次数、随机发牌/翻牌、下注和结算状态，不作为独立数据包内容迁移。
- `CRICKETS` 和 `TEXAS_TIERS` 虽然是静态列表，但分别与斗蛐蛐战斗随机、瀚海扑克场次、盲注、庄家 AI、UI 状态和派奖流程强耦合；本轮改为阶段 3 机制插件候选，而不是 `taoyuan:shop_offer` 或新的阶段 6 静态注册表。
- 已完成的 `taoyuan:hanhai_roulette` 与 `taoyuan:hanhai_casino_wager` 仍只表示既有纯静态配置边界；轮盘动画、掷骰/猜杯随机、斗蛐蛐、翻牌、扑克和恶魔轮盘完整玩法仍留在框架或后续机制插件设计中。
- `content-inventory.json` 由 `scripts/generate-content-inventory.mjs` 重新生成，后续执行 `pnpm run mod:inventory` 不会把这些符号恢复成待迁移的阶段 6 数据项。

## 阶段 6 收敛审计结果

- `taoyuan:bomb` 试点完成后，`content-inventory.json` 的 573 个阶段 6 符号中 330 个已验证，92 个 `framework-retained` 明确保留在框架/领域层，52 个 `inventoried` 为 barrel re-export，99 个 `baselined` 多数是兼容门面、类型形状或算法参数。
- 已迁移/已验证：阶段 6 主要官方内容已覆盖树木、鱼类、鱼塘、建筑、采集、动物、育种杂交、工具、装备、加工机器、加工配方、洒水器、炸弹、叙事/收藏/节日、NPC/任务、市场、旅行商人与瀚海通商/奖励/固定投注配置；旧查询入口和本地持久化 ID 保留。
- 可继续作为纯数据迁移：收敛审计提出的唯一候选 `src/data/processingCraftDefinitions.ts:BOMBS` 已完成 `taoyuan:bomb` 试点，当前机器盘点未发现新的低耦合纯数据候选。
- 不建议作为阶段 6 下一纯数据切片：肥料、鱼饵和浮漂的店铺展示字段已通过 `taoyuan:shop_offer` 验证，完整效果涉及农田施肥、钓鱼行为、浮漂耐久和工具等级门槛；Boss 首杀装备奖励映射仍属于矿洞奖励结算；`SHOP_SCHEDULES` 是营业/行程派生表；森林友好/敌对遭遇与竹林战斗和战败惩罚耦合。
- 应保留在框架：鱼塘水质/疾病/死亡、育种随机与种子实例、设施解锁与日更、动物疾病/放牧/每日结算、加工队列和自动收取、任务生成/提交、行情随机、旅行商人库存生成、时间/行程格式化、装备附魔生成与展示、存档结构和所有运行时状态。
- 第三阶段机制插件候选：`CRICKETS`、`TEXAS_TIERS`、`MAX_DAILY_BETS`、`CARD_TOTAL` 和 `getTexasTier` 已归到 `engine/domain/hanhai-mechanism-plugin`；瀚海斗蛐蛐、翻牌、扑克和恶魔轮盘应作为完整机制插件设计，而不是继续拆零散静态常量。
- 需要人工确认：机器盘点层面的低耦合纯数据候选已经清零，具备进入阶段 6 总验收和收尾确认的证据；是否正式宣布阶段 6 完成仍属于下一轮产品/路线图确认点，本轮不直接宣布。

## 阶段 6 总验收差距审计结果

- 新增只读 `pnpm run mod:audit-phase6`：使用 TypeScript AST 和 Vue SFC 编译器扫描 `src/` 非测试业务代码，并与机器盘点逐符号核对。510 条运行时数据导入已全部归类为 87 条官方适配器叶子、289 条兼容回退、82 条框架算法、19 条合法派生和 33 条待迁移违规；无无法解析的运行时导入和命名空间导入。
- 33 条读取集中在六组：配方/物品成就统计、育种派生计数、行会商品、装备与掉落表、炸弹列表、农场作物和商店列表。该结果证明“低耦合纯数据候选清零”不等于“所有消费者已切换查询门面”。
- 机器盘点总数仍为 628，阶段 6 仍为 330 `verified`、92 `framework-retained`、52 `inventoried`、99 `baselined`；后两类共 151 个暂定状态，尚不满足“每个符号都有最终状态”。
- 官方快照含 54 个注册表和 4242 条目。新增全量恢复测试证明完整构建、已提交夹具和恢复结果的 `snapshotHash` 及逐 ID 查询一致，但快照按 ID 排序后恢复，导致 50 个注册表的 `values()` 顺序与完整构建不同；等价性标准因此未通过。
- `src/main.ts` / `src/bootstrap.ts` 没有官方包挂载门禁；Web、Electron、Android 共享构建源码但没有产物级哈希探针；预编译签名、环境键、失败回退和完整人工回归仍未实现或执行。
- 结论：阶段 6 总验收为 3 项 `FAIL`、2 项 `PARTIAL`、1 项 `BLOCKED`，不能宣布完成。下一最小实现切片应先建立仅针对 `taoyuan-core` 的启动门禁，不扩展第三方加载器、管理 UI 或安装事务。

## 阶段 6 炸弹消费者组收口结果

- 选择 `taoyuan:bomb` 是因为三个剩余生产读取共享同一内容域、已有注册表和公开按 ID/有序列表查询，也已有制造与矿洞行为测试；该组可以完整收口而不触及存档、随机机制或第三方加载。
- `useProcessingStore.craftBomb()` 改用 `getOfficialBombById()`，`MiningView.vue` 与 `ProcessingView.vue` 改用 `getOfficialBombsAsLegacy()`；三个消费者不再直接导入 `src/data/processing.ts:BOMBS`。
- `src/data/processingCraftDefinitions.ts:BOMBS` 继续作为官方包唯一静态注册源，`src/data/processing.ts:BOMBS` 和 `getBombById()` 继续作为兼容、等价测试与回滚路径，不删除旧静态数据。
- 等价测试覆盖全部三个官方炸弹的 ID、顺序、名称、描述、爆炸效果、制造材料、金钱和售价，并覆盖未知 ID、制造扣料/发放、矿洞爆炸结算与动作列表；模拟已发布注册表不可用时，公开查询和制造消费者均明确抛错，不会静默回退。
- `pnpm run mod:audit-phase6` 当前统计从 510 条运行时数据导入、33 条待迁移违规降为 507 条和 30 条；87 条官方适配器叶子、289 条兼容回退、82 条框架算法与 19 条合法派生保持不变，业务静态读取门禁仍为 `FAIL`。
- 剩余 30 条分为装备/掉落 21 条、行会商品 2 条、成就物品 2 条，以及配方、作物、商店、育种阶层计数和鱼塘品种计数各 1 条。本切片没有修改字段、持久化 ID 或 Store 序列化结构，旧存档兼容策略不变。

## 阶段 6 行会商品消费者组收口结果

- `useGuildStore.ts` 与 `GuildView.vue` 不再直接读取 `GUILD_SHOP_ITEMS`，分别改用 `getOfficialGuildShopItemById()` 和 `getOfficialGuildShopItemsAsLegacy()`；旧静态叶子、原名重导出和本地商品 ID 继续保留。
- `ShopOfferDefSchema` 的官方行会投影覆盖 24 件商品的描述、`ring` 装备类型、铜钱、贡献点、解锁等级、日/周/总限购、发放数量和额外材料；材料引用继续在发布前校验。
- 定向测试覆盖全部商品的顺序、分组、等级可见性、两种货币、材料、装备发放、单次/批量购买、全部限购、未知/锁定/余额不足和发放失败回滚；注册表不可用时购买前明确失败且无扣款或发放副作用。
- 审计统计由 507 条运行时导入、30 条违规降为 505 条和 28 条；官方快照仍为 54 个注册表、4242 条目，因行会商品完整字段投影更新为 `sha256:6fa556c65520a948606b1e11bd5e67b7e2cb35fed47d2850477b0fd5803fcfc2`。

## 阶段 6 成就物品目录消费者组收口结果

- `contentAccess.ts` 新增 `getOfficialItemsAsLegacy()`，按注册表顺序重建完整旧 `ItemDef` 字段；`ITEMS` 和 `getItemById()` 保留为兼容、等价测试和回滚路径。
- `useAchievementStore.ts` 的完整出货和完成度分母、`AchievementView.vue` 的图鉴分类、筛选、虚拟滚动、出货列表及分组改用注册表列表；显示名称、价格、分类、本地 ID、发现/出货进度和旧存档字段均未改变。
- 定向测试逐项比较全部官方物品的 ID、顺序和旧字段，比较分类计数与可出货列表，并覆盖完整出货成就、完成度计算、旧查询入口、未知 ID 和注册表未发布时的无副作用失败。
- 审计统计由 505 条运行时导入、28 条违规降为 503 条和 26 条；快照内容和哈希不变。

## 阶段 6 矿洞宝箱装备消费者组收口结果

- `contentAccess.ts` 新增 `getOfficialEquipmentDropPoolsAsLegacy()`，从命名掉落表按来源、装备类型和区域重建本地 `gearId + chance` 池；旧 `TREASURE_DROP_*` 四张表继续作为官方适配器叶子、等价测试和回滚路径。
- `useMiningStore.ts` 初始化时读取戒指、帽子、鞋子和武器宝箱池，结算仍按原顺序执行；六个区域、空池、未知区域、概率、武器附魔、首次发放、重复装备售价和消息均保持不变。注册表不可用时 Store 在创建玩家/背包依赖和矿洞状态前失败。
- 定向测试覆盖四类全部区域池的键顺序、条目顺序和概率，既有手动开箱与连战自动清理测试继续证明普通物品、金钱、装备数量和自动售出结果不变。
- 审计统计由 503 条运行时导入、26 条违规降为 499 条和 22 条；剩余违规中矿洞怪物装备 4 条与本切片宝箱装备 4 条彼此独立，未顺带迁移。

## 阶段 6 矿洞怪物装备结算消费者组收口结果

- `useMiningStore.ts` 初始化时通过 `getOfficialEquipmentDropPoolsAsLegacy()` 读取 `source: "monster"` 的武器、戒指、帽子和鞋子池；旧 `MONSTER_DROP_*` 继续作为官方适配器叶子、等价测试和回滚路径。
- 六个区域的全部键、条目顺序、装备 ID 和概率与旧池逐项相等；Store 内武器、戒指、帽子和鞋子的结算顺序没有改变，未知区域仍按空池处理，注册表不可用时在 Store 创建阶段明确失败。
- 定向测试覆盖每个区域的概率边界和成功路径，精确断言随机调用次数与顺序，并验证随机武器附魔、四类首次发放、四类重复装备自动售出、售价和消息保持不变。
- 审计统计由 499 条运行时导入、22 条违规降为 495 条和 18 条；对应提交为 `89ac6ac refactor: route monster equipment drops through registry`。

## 阶段 6 行会图鉴怪物装备展示消费者组收口结果

- `GuildView.vue` 的四类普通怪物装备展示改读 `taoyuan:drop_table` 的本地 ID 投影；注册表筛选和兼容转换继续由 domain 查询入口负责，组件只保留现有区域/BOSS 关联、名称格式化和交互编排。
- 真实挂载测试逐一打开六个矿区的怪物详情，按旧武器、戒指、帽子、鞋子顺序比较全部名称和四舍五入概率；BOSS 固定奖励读取、首杀标记、缺失定义 ID 回显和骷髅矿穴无关联时的空状态未改变。
- 注册表不可用时页面挂载明确失败且行会状态无副作用，不会静默回退旧四张表。
- 审计统计由 495 条运行时导入、18 条违规降为 491 条和 14 条；对应提交为 `021e7ae refactor: route guild drop displays through registry`。

## 阶段 6 成就及背包装备展示消费者组收口结果

- `contentAccess.ts` 新增全部附魔的有序旧形状投影；`AchievementView.vue` 按 ID 读取武器和固定附魔，`InventoryView.vue` 通过投影读取附魔选项，不再直接导入旧 `WEAPONS` 或 `ENCHANTMENTS`。
- 全量测试逐项比较全部官方附魔的顺序和旧字段、全部武器与固定附魔关联，并覆盖未知 ID 与注册表不可用时的明确失败；名称、描述、属性、固定附魔显示和附魔选项顺序不变。
- 旧静态定义、`getWeaponById()`、`getEnchantmentById()`、持久化 ID 和装备存档结构均保留；审计统计由 491 条运行时导入、14 条违规降为 488 条和 11 条。对应提交为 `163ec4b refactor: route equipment displays through registry`。

## 阶段 6 商店装备池消费者组收口结果

- `contentAccess.ts` 新增武器/帽子/鞋子购买池和戒指/帽子/鞋子制造池六个注册表投影；`ShopView.vue` 在 Store 初始化前读取这些池，组件不自行重建注册表分组或兼容字段。
- 全量测试证明六个池的条目顺序、名称、说明、价格、材料和效果与旧静态池相等，并与 `taoyuan:shop_offer` 中镖局、绸缎庄商品一一对应；原购买、扣款、扣料、发放、失败回滚和制造函数未修改。
- 旧 `SHOP_*`、`CRAFTABLE_*` 导出与装备查询签名继续保留；审计统计由 488 条运行时导入、11 条违规降为 482 条和 5 条。对应提交为 `de4fd68 refactor: route shop equipment pools through registry`。

## 阶段 6 商店定义消费者组收口结果

- `contentAccess.ts` 新增 `getOfficialShopsAsLegacy()`，按冻结注册表顺序投影六大商铺；`ShopView.vue` 的导航列表和当前商店解析共享该投影，旧营业判断辅助函数继续负责日期、时间、天气和季节规则。
- 全量测试逐项比较商店顺序和全部字段，并覆盖营业/关闭原因、未知商店 ID 与注册表不可用时导航状态不变化；名称、NPC、描述、开放条件和空状态不变。
- 旧 `SHOPS`、`getShopById()`、持久化商店 ID 和 Store 序列化结构均保留；审计统计由 482 条运行时导入、5 条违规降为 481 条和 4 条。对应提交为 `3bbe541 refactor: route shop definitions through registry`。
- 最后四条精确为 `src/composables/useEndDay.ts:RECIPES`（内容，`taoyuan:recipe`）、`src/domain/mods/contentAccess.ts:HYBRID_TIER_COUNTS`（派生，`taoyuan:breeding_hybrid`）、`src/views/game/FarmView.vue:CROPS`（内容，`taoyuan:crop`）和 `src/views/game/FishPondView.vue:BREED_COUNTS`（适配器，`taoyuan:pond_breed`）。业务静态读取验收继续为 `FAIL`，未提前宣布阶段完成。

## 迁移顺序

1. `crops.ts`、`items.ts`、`recipes.ts`：建立第一条 Tag/Item/Crop/Recipe 闭环。
2. `shops.ts` 及各域商店池：统一 Shop 和 ShopOffer。
3. `weapons.ts`、`rings.ts`、`hats.ts`、`shoes.ts`：附魔、装备和掉落表。
4. `mine.ts`、`forage.ts`：怪物、区域池和战利品。
5. `processing.ts`、`animals.ts`、`fishPond.ts`、`breeding.ts`：生产和设施；育种杂交定义已迁移，种子实例、变异和育种队列算法继续保留在框架。
6. 普通任务运行时和其他世界活动；晨间随机事件定义已迁移，概率/随机/副作用算法继续保留在框架。
7. `timeConstants.ts`、`hanhai.ts` 等高混合文件最后处理；`processingCraftDefinitions.ts` 中洒水器和炸弹制造定义已完成 `taoyuan:sprinkler` 与 `taoyuan:bomb` 试点，当前不再建议继续拆肥料、鱼饵或浮漂的零散效果字段；`market.ts` 已完成行情分类定义试点，`hanhai.ts` 已完成通商兑换定义试点、瀚海驿站商品池试点、通商店铺升级定义试点、藏宝图奖励定义试点、幸运轮盘固定配置试点和骰子/猜杯固定投注配置试点；斗蛐蛐、翻牌、瀚海扑克和恶魔轮盘已归档为后续机制插件候选，不再作为阶段 6 零散静态配置继续拆分。

## 完成标准

“官方内容包完全接管”必须同时满足：

- 机器盘点中的每个文件和导出符号都有最终状态。
- 所有 `content` 和 `derived` 项已进入注册表或内容编译器。
- 所有 `algorithm` 和 `ui` 项有明确保留位置与理由。
- 所有持久化 ID 有别名或迁移策略。
- 全仓不存在业务层绕过查询门面直接导入旧内容数组的路径。
- 迁移前后规范化快照、固定随机测试和旧存档回归通过。
- 机器清单、文档、Schema 和官方包在 CI 中交叉校验。
