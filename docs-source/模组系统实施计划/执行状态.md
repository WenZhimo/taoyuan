# 模组系统执行状态

更新日期：2026-07-13

## 当前阶段

- 阶段 0：已完成。交付物包括导出符号级机器盘点、官方内容快照、哈希黄金向量、旧存档兼容夹具、最小合法/非法包夹具和可重复基线验证。
- 阶段 1：已完成。交付物包括 TypeBox 公开契约、Ajv 结构校验、语义校验、通用注册表、冻结生命周期、旧静态数据适配层、公开 JSON Schema 生成与一致性检查、快照 codec 和 10 万条注册/查找响应性验证。
- 阶段 2：进行中。本轮完成“物品、作物和配方试点”的第一个可回滚切片，范围限定为官方 Tag/Item/Crop/Recipe 查询门面与 Cooking 材料规划读取链路；尚未进入第三方包加载器、模组安装界面、安装/卸载事务、存档环境绑定或机制插件。

## 本次交付物

- 新增 `src/domain/mods/`：
  - 命名空间 ID 与包 ID 校验。
  - JCS 子集规范化、SHA-256 哈希、包路径规范化。
  - 模组错误码登记与诊断对象。
  - 首批公开 Schema：manifest、localized text、settings、attribution、tag、item、monster、enchantment、drop table、recipe、shop offer。
  - Registry / RegistrySet 定义期、条目期、冻结期。
  - 结构校验与语义引用校验。
  - 旧 `src/data` 静态内容到官方 RegistrySet 的只读适配。
  - 公开 JSON Schema 输出规范化，避免嵌套 `$id` 影响 Ajv 编译。
- 新增生成脚本：
  - `pnpm run mod:inventory`
  - `pnpm run mod:schemas`
  - `pnpm run mod:check-schemas`
  - `pnpm run mod:fixtures`
- 新增生成物与夹具：
  - `content-inventory.json`
  - `generated-schemas/*.schema.json`
  - `src/tests/fixtures/mods/*`
  - `src/tests/fixtures/saves/legacy-v1-baseline.json`
- 新增测试：
  - `src/tests/domain/modRegistryContracts.test.ts`
  - `src/tests/stores/legacySaveBaseline.test.ts`
- 阶段 2 首个切片新增：
  - `src/domain/mods/contentAccess.ts`：提供只读官方内容查询门面，覆盖 `getOfficialTagDef()`、`getOfficialItemDef()`、`getOfficialRecipeDef()`、`getOfficialCropById()` 和官方物品列表读取。
  - `src/domain/cooking/ingredientPlanner.ts`：统一烹饪材料规划，兼容旧 `{ itemId, quantity }`，并支持 `type: "item"`、`type: "tag"`、`type: "anyOfTags"`。
  - `src/stores/useCookingStore.ts`：`canCook`、最大可制作数量、品质预览和实际扣料改为复用同一材料规划结果，旧食谱入口和玩家可见文案保持不变。
  - 官方物品注册表适配器增加最小标签元数据：作物/水果为 `taoyuan:vegetarian`，鱼为 `taoyuan:meat` 和 `taoyuan:protein`，可食用动物产品为 `taoyuan:protein`。
  - 新增 `src/tests/domain/cookingIngredientPlanner.test.ts` 和 `src/tests/stores/cooking.test.ts`，覆盖旧材料兼容、面粉加任意素食或肉类的标签材料试点、重叠标签不重复计数、品质与扣料等价性。
  - 更新 `src/tests/fixtures/mods/official-content-snapshot.json` 和 `content-inventory.json`，记录本轮已验证的 `CROPS/getCropById`、`ITEMS/getItemById`、`RECIPES/getRecipeById` 状态。

## 验证记录

- `pnpm run mod:schemas`：通过。
- `pnpm run mod:check-schemas`：通过。
- `pnpm exec vitest run src/tests/domain/modRegistryContracts.test.ts --no-file-parallelism`：通过，覆盖哈希黄金向量、公开 Schema 编译与非法夹具、注册表生命周期、跨注册表同 ID、10 万条分块注册与查找、官方快照稳定性和快照恢复。
- `pnpm exec vitest run src/tests/stores/legacySaveBaseline.test.ts --no-file-parallelism`：通过，覆盖旧存档加载、迁移、保存和再次加载。
- `pnpm run type-check`：通过。
- `pnpm run lint`：通过。
- `pnpm run validate:data`：通过。
- `pnpm run validate:docs`：通过。
- `pnpm run mod:inventory`：通过，当前盘点覆盖 44 个 `src/data` 文件、423 个导出符号。
- `pnpm run mod:fixtures`：通过。
- `pnpm test`：通过，117 个测试文件、624 个测试；首次 6 分钟超时后以更长超时重跑通过。
- `pnpm build`：通过，仅有既有 Browserslist 与大 chunk 警告。
- `git diff --check`：通过，仅有 Windows CRLF 提示。
- 阶段 2 切片定向验证：
  - `pnpm exec vitest run src/tests/domain/cookingIngredientPlanner.test.ts src/tests/stores/cooking.test.ts --no-file-parallelism`：通过，2 个测试文件、7 项测试。
  - `pnpm exec vitest run src/tests/domain/modRegistryContracts.test.ts --no-file-parallelism`：通过，官方内容快照哈希已随标签元数据更新。
  - `pnpm run type-check`：通过。
  - `pnpm run lint`：通过。
  - `pnpm run validate:data`：通过，1 个测试文件、6 项测试。
  - `pnpm run validate:docs`：通过，1 个测试文件、3 项测试。
  - `pnpm test`：通过，119 个测试文件、631 项测试。
  - `pnpm build`：通过，仅有既有 Browserslist 数据过期和大 chunk 警告。
  - `git diff --check`：通过，仅有 Windows CRLF 提示。

## 当前阻塞

- 无。

## 下一步

- 阶段 2 下一步继续逐类迁移官方内容来源；优先在不改变旧数据路径的前提下，把更多业务调用方切到 `contentAccess` 或等价查询适配层。
- 设计运行时模组包发现、加载事务和失败回退流程，但不得在阶段 2 前跳到外载加载器。
- 在任何存档结构变化前同步更新 `../save-compatibility.md` 并提供旧档迁移测试。
- 烹饪标签材料目前只完成自动选料与扣料规划，尚未实现 UI 中展示标签槽选择结果或玩家手动换料。

## 对应提交

- `61892b7 feat: add mod registry contracts`
- `5e87025 test: complete mod registry phase gates`
- `3ab9a6f feat: add cooking content registry pilot`
