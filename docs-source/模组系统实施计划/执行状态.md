# 模组系统执行状态

更新日期：2026-07-13

## 当前阶段

- 阶段 0：已建立基线夹具、哈希黄金向量、官方内容快照和机器盘点生成脚本。
- 阶段 1：已建立 TypeBox 公开契约、Ajv 校验入口、注册表生命周期、语义校验和旧静态数据适配层。
- 尚未进入运行时加载、模组安装界面、存档环境绑定或内置玩法数据迁移。

## 本次交付物

- 新增 `src/domain/mods/`：
  - 命名空间 ID 与包 ID 校验。
  - JCS 子集规范化、SHA-256 哈希、包路径规范化。
  - 模组错误码登记与诊断对象。
  - 首批公开 Schema：manifest、localized text、settings、attribution、tag、item、monster、enchantment、drop table、recipe、shop offer。
  - Registry / RegistrySet 定义期、条目期、冻结期。
  - 结构校验与语义引用校验。
  - 旧 `src/data` 静态内容到官方 RegistrySet 的只读适配。
- 新增生成脚本：
  - `pnpm run mod:inventory`
  - `pnpm run mod:schemas`
  - `pnpm run mod:fixtures`
- 新增生成物与夹具：
  - `content-inventory.json`
  - `generated-schemas/*.schema.json`
  - `src/tests/fixtures/mods/*`
  - `src/tests/fixtures/saves/legacy-v1-baseline.json`
- 新增测试：
  - `src/tests/domain/modRegistryContracts.test.ts`

## 验证记录

- `pnpm run type-check`：通过。
- `pnpm run lint`：通过。
- 其余验证在提交前继续执行并以终端结果为准。

## 下一步

- 将阶段 1 的 Registry 只读适配接入更多只读查询夹具，继续证明游戏行为不变。
- 设计运行时模组包发现、加载事务和失败回退流程。
- 在迁移任何玩法数据来源前，补充对应旧存档兼容测试和行为等价测试。
