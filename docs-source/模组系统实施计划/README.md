# 桃源乡模组系统文档总览

> 状态：提案
>
> 适用版本：模组系统处于官方内容迁移试点阶段
>
> 本目录是模组系统设计和实施工作的唯一入口。Word、PDF 或其他导出文件仅供阅读，不是事实来源。

## 目标

本套文档指导《桃源乡》从编译期静态数据演进为“挂载器加载官方内容包和第三方模组”的架构。实施必须保持现有玩法、旧存档和 Web、Electron、Android 构建可用，并通过可回滚的小切片推进。

## 文档地图

| 文档 | 职责 |
| --- | --- |
| [愿景与原则](./01-愿景与原则.md) | 背景、目标、非目标和长期架构原则 |
| [目标架构](./02-目标架构.md) | 挂载器、注册表、游戏本体和目录结构 |
| [数据包契约](./03-数据包契约.md) | 清单、ID、注册表、Schema、依赖、配置和生态契约 |
| [内置数据迁移](./04-内置数据迁移.md) | 哪些内容迁移、哪些算法留在框架 |
| [实施路线图](./05-实施路线图.md) | 阶段 0 至阶段 9、验收标准和回滚点 |
| [平台与安全](./06-平台与安全.md) | 三端来源、版本目录、资源和签名信任 |
| [存档兼容](./07-存档兼容.md) | 内容环境、版本矩阵、未知内容、`pluginData` 和配置 |
| [质量性能与发布](./08-质量性能与发布.md) | 测试矩阵、性能、风险、交付和发布策略 |
| [后续插件](./09-后续插件.md) | 事件插件、机制插件和首个实现切片 |
| [规范化与哈希](./10-规范化与哈希规范.md) | 内容、配置、lockfile 和私有负载的确定性哈希 |
| [错误码](./11-错误码规范.md) | 诊断结构、错误码命名和首批登记表 |
| [资源限制](./12-资源限制默认值.md) | 包、JSON、媒体、配置和私有存档的默认配额 |
| [内容盘点](./13-内容迁移盘点.md) | 当前 `src/data/` 的真实文件级盘点和迁移顺序 |
| [机器盘点](./content-inventory.json) | 工具可读取的内容文件清单 |
| [重构协同](./14-重构协同与前置门槛.md) | 与现有重构计划的边界、前置门槛和禁止并行项 |
| [注册表快照缓存](./15-注册表快照缓存.md) | 缓存恢复、环境键、失效条件和官方预编译快照 |

## 事实来源优先级

模组系统尚未发布时：

```text
现有代码和回归测试所证明的玩法行为
        → 本目录已经确认的架构决策
        → 阶段夹具和机器盘点
        → 示例与说明性文本
```

首个稳定 API 发布后：

```text
已发布稳定规范及规则编号
        → 从 TypeBox 生成的公开 JSON Schema
        → 官方一致性测试包及预期错误码
        → 参考实现
        → 示例和非规范性说明
```

若稳定规范、生成 Schema 和一致性测试互相矛盾，必须阻止发布并修复事实来源，不能由挂载器临时猜测。当前游戏内部 TypeScript 类型不是第三方公开契约。

## 已确认决策

- 官方内容和第三方数据包使用同一套注册表与校验流程。
- 注册表条目身份是 `(registryId, contentId)`，内容 ID 使用命名空间。
- TypeBox Schema 是官方公开内容契约的单一事实来源；Ajv 执行外部 JSON 校验。
- 怪物定义与怪物池分别使用 `taoyuan:monster` 和 `taoyuan:monster_pool`；池条目顺序是公开行为，省略 `weight` 时按 `1` 处理。
- 单个怪物池的原始条目数、单条权重和有效权重总和均以 250,000 为上限；超限使用 `PKG-LIMIT-001` 拒绝，不静默截断。
- 果树和野树统一使用独立 `taoyuan:tree` 判别联合契约；树苗、果实和采脂产物引用 `taoyuan:item`。
- 同一安装实例中，同一 `PackageId` 只加载一个版本；版本冲突阻止启动。
- 模组在当前程序目录中全局启用，不按存档分别启停。
- 较高版本游戏可以暂存尝试旧模组，任一错误立即回滚相关依赖并停止游戏本体启动。
- 挂载器成功后才创建 `GameApp`；注册表在启动期构建，运行期冻结。
- 每次启动都经过挂载器；环境未变化时从已验证的持久化注册表快照恢复，缓存无效时自动完整挂载。
- Electron 的 `mods/` 和 `userdata/` 位于程序目录，重新构建必须保留二者。
- 原版旧存档只保证向原版新版本升级；模组存档跨环境不作保证。
- 插件私有状态进入 `pluginData`，通用设置进入 `packageSettings`，不得持续扩展根存档字段。
- 数据插件不执行未受信任代码；事件插件和机制插件属于后续阶段。

## 当前执行状态

阶段 0 至阶段 5 已完成当前范围的实现与自动化验收；阶段 6 的低耦合纯数据候选、业务静态读取违规、机器盘点暂定状态和快照恢复顺序差异均已清零，并已完成总验收差距审计、官方内容启动门禁、正式预编译产物、`environmentHash` 绑定和失败回退。六项验收当前为 4 项 `PASS`、1 项 `PARTIAL`、1 项 `BLOCKED`，因此阶段 6 仍为进行中，不能宣布“官方内容包完全接管”。各阶段切片、审计矩阵、验证证据和提交详见 [执行状态](./执行状态.md)：

1. 已固定首批规范编号、错误码、ID 规则、哈希算法和生成式公开 Schema 流程。
2. 已将 `content-inventory.json` 扩展到 `src/data/` 导出符号级，并建立官方内容快照、哈希黄金向量和旧存档夹具。
3. 已实现 `ContentId`、`PackageId`、`RegistryTypeId`、通用 `Registry<T>` / `RegistrySet`、冻结生命周期、结构校验和语义校验。
4. 已建立 54 个官方内容注册表、61 份公开 Schema、最小合法/非法包夹具、旧静态数组兼容适配器和可序列化注册表快照 codec；`registry-snapshot.schema.json` 公开 v2 保序快照契约。
5. 阶段 2 至阶段 5 已依次完成物品/作物/配方、商店、附魔/掉落表和怪物/怪物池迁移闭环；矿洞运行时已通过注册表解析 15 个官方怪物池。
6. 阶段 6 首切片建立怪物池资源预算和 `taoyuan:tree`，鱼类切片建立 `taoyuan:fish`，可养殖鱼切片建立 `taoyuan:pondable_fish`，鱼塘品种切片建立 `taoyuan:pond_breed`，鱼塘设施切片建立 `taoyuan:fish_pond_facility`，建筑升级切片建立 `taoyuan:building_upgrade`，采集物切片建立 `taoyuan:forage`，动物定义切片建立 `taoyuan:animal`，动物饲料切片建立 `taoyuan:animal_feed`，钱袋物品切片建立 `taoyuan:wallet_item`，农场地图切片建立 `taoyuan:farm_map`，动物建筑切片建立 `taoyuan:animal_building`，动物孵化映射切片建立 `taoyuan:animal_incubation`，育种杂交定义切片建立 `taoyuan:breeding_hybrid` 官方读取路径，工具升级切片建立 `taoyuan:tool_upgrade`，装备套装切片建立 `taoyuan:equipment_set`，戒指、帽子、鞋子和武器定义切片建立 `taoyuan:equipment` 的 `kind: "ring"` / `kind: "hat"` / `kind: "shoe"` / `kind: "weapon"` 官方读取路径，加工机器制造定义切片建立 `taoyuan:processing_machine` 官方读取路径，加工配方定义切片建立 `taoyuan:processing_recipe` 官方读取路径，洒水器与炸弹制造定义切片分别建立 `taoyuan:sprinkler` 和 `taoyuan:bomb` 官方读取路径，秘密纸条定义切片建立 `taoyuan:secret_note` 官方读取路径，教程提示定义切片建立 `taoyuan:tutorial` 官方读取路径，博物馆切片建立 `taoyuan:museum_category`、`taoyuan:museum_item` 和 `taoyuan:museum_milestone` 官方读取路径，行会切片建立 `taoyuan:guild_goal`、`taoyuan:guild_donation` 和 `taoyuan:guild_level` 官方读取路径，成就/祠堂任务切片建立 `taoyuan:achievement` 和 `taoyuan:community_bundle` 官方读取路径，普通 NPC 定义切片建立 `taoyuan:npc` 官方读取路径，隐藏 NPC 定义切片建立 `taoyuan:hidden_npc` 官方读取路径，剧情任务定义切片建立 `taoyuan:story_quest` 官方读取路径，好感事件切片建立 `taoyuan:heart_event` 官方读取路径，季节节日事件切片建立 `taoyuan:season_event` 官方读取路径，普通任务模板切片建立 `taoyuan:quest_template` 官方读取路径，晨间随机事件切片建立 `taoyuan:morning_event` 官方读取路径，市场行情分类切片建立 `taoyuan:market_category` 官方读取路径，瀚海通商兑换定义切片建立 `taoyuan:hanhai_trade_exchange` 官方读取路径，瀚海驿站商品池切片复用 `taoyuan:shop_offer` 官方读取路径，瀚海通商店铺升级切片建立 `taoyuan:hanhai_trade_shop_upgrade` 官方读取路径，瀚海藏宝图奖励切片建立 `taoyuan:hanhai_treasure_reward` 官方读取路径，瀚海幸运轮盘固定配置切片建立 `taoyuan:hanhai_roulette` 官方读取路径，瀚海赌坊固定投注配置切片建立 `taoyuan:hanhai_casino_wager` 官方读取路径；旧导出和存档结构保持不变。
7. 旅行商人商品池切片复用既有 `taoyuan:shop_offer`，`generateMerchantStock()` 和隐藏 NPC 额外商品通过注册表兼容门面读取旧形状商品池；出摊日、库存随机、价格浮动、反季种子、购买流程和旧存档结构保持不变。
8. 市场行情分类切片建立 `taoyuan:market_category`，`getDailyMarketInfo()`、`getMarketMultiplier()` 和隔夜行情日志通过注册表兼容门面读取旧分类名称、季节系数和供需阈值；随机种子、趋势分类、价格系数和旧存档结构保持不变。
9. 瀚海通商兑换定义切片建立 `taoyuan:hanhai_trade_exchange`，`useHanhaiStore.exchangeItem()` 和兑换列表通过注册表兼容门面读取旧兑换目标、积分成本、描述和限购字段；积分扣除、购买记录、香料礼包、钱袋解锁、日志和旧存档结构保持不变。
10. 瀚海驿站商品池切片复用既有 `taoyuan:shop_offer`，`useHanhaiStore.getWeeklyRemaining()`、`buyShopItem()`、`refreshRotatingStock()` 和 `HanhaiView.vue` 常驻商品列表通过注册表兼容门面读取旧固定/轮换商品形状；固定/轮换顺序、描述、价格、限购、每周确定性洗牌、购买回滚和旧存档结构保持不变。
11. 瀚海通商店铺升级切片建立 `taoyuan:hanhai_trade_shop_upgrade`，`useHanhaiStore.tradeShopConfig` 与 `nextTradeShopUpgrade` 通过注册表兼容门面读取旧等级、名称、槽位、售卖周期、费用和材料字段；升级扣款、扣料、日志、`tradeShopLevel` 和旧存档结构保持不变。
12. 瀚海藏宝图奖励切片建立 `taoyuan:hanhai_treasure_reward`，`useHanhaiStore.useTreasureMap()` 通过注册表兼容门面读取旧奖励档位、阈值、金钱和物品；藏宝图消耗、单次随机调用、奖励文本、日志、物品发放失败忽略语义和旧存档结构保持不变。
13. 瀚海幸运轮盘固定配置切片建立 `taoyuan:hanhai_roulette`，瀚海赌坊固定投注配置切片建立 `taoyuan:hanhai_casino_wager`；轮盘、骰子和猜杯的结果/投注配置通过注册表兼容门面读取，结果标签、倍率、概率、下注档位、固定投注、单次随机、派奖、日志和旧存档结构保持不变。
14. 鱼塘切片当前迁移可养殖鱼物种定义、品种图鉴定义和建造/升级/容量定义，水质、疾病、育苗塘任务、自然繁衍、遗传随机和鱼塘存档结构仍属框架/后续范围；育种切片当前迁移杂交品种定义，育种台数量、种子箱容量、加工天数、变异随机、属性波动、图鉴状态、种子实例和旧存档结构仍属框架范围；建筑升级切片当前迁移农舍、山洞和酒窖升级定义，温室/仓库解锁、山洞品质阈值、酒窖增值周期和日更算法仍属框架范围；动物切片当前迁移物种定义、饲料定义、畜舍建造/升级定义和孵化映射，饲料效果、放牧、疾病/死亡和每日结算仍属框架/后续范围；加工切片当前迁移机器制造定义、加工配方定义、洒水器制造定义和炸弹制造定义，运行队列、自动收取、多任务酒坊/种子制造机、配方解锁、爆炸范围算法、矿洞结算和存档结构仍属框架/后续范围；叙事/收藏/节日切片当前迁移秘密纸条定义、教程提示定义、博物馆分类/条目/里程碑定义、行会讨伐/捐献/等级定义、成就定义、祠堂任务定义、普通 NPC 定义、隐藏 NPC 定义、剧情任务定义、好感事件定义、季节节日事件定义、普通任务模板和晨间随机事件，任务生成、提交副作用、任务进度和其他世界活动仍未完成；NPC 好感状态、隐藏 NPC 亲密状态、行会商店、贡献点状态、被动加成、成就进度统计、主线任务进度状态、普通任务状态、节日奖励结算副作用和旧存档结构仍按既有 Store/框架规则保留；瀚海售货槽、通商积分结算、藏宝图使用流程、幸运轮盘转动动画、掷骰/猜杯随机算法和斗蛐蛐、翻牌、瀚海扑克、恶魔轮盘完整玩法仍属框架/后续机制插件范围；尚未进入外载数据包加载器、模组管理界面、安装/卸载事务、存档环境绑定或机制插件。炸弹试点处理完收敛审计建议的最后一个低耦合纯数据候选，但本轮不直接宣布阶段 6 完成。
15. 阶段 6 总验收差距审计新增 `pnpm run mod:audit-phase6` 和全注册表快照恢复测试；完成消费者、机器盘点和 v2 快照 codec 收口后，477 条业务运行时数据导入已逐项归为 87 条官方适配器叶子、150 条兼容回退、190 条框架算法和 50 条合法派生，待迁移违规为 0，`businessStaticReads` 为 `PASS`。629 个导出符号均已有最终状态，其中阶段 6 的 574 个符号为 384 个 `verified` 和 190 个 `framework-retained`，暂定状态为 0；完整构建/快照恢复顺序差异由 50 降为 0。Windows 本机预编译哈希探针已通过，Linux/macOS CI 和真实 Web/Electron/Android 运行时探针仍需记录，完整人工回归尚未闭合。
16. 阶段 6 首个收尾实现切片建立 `taoyuan-core` 启动门禁：54 个官方注册表、4242 条目完成 TypeBox 结构校验、跨注册表语义校验和冻结后才原子发布；Vue、Pinia、路由、存档读取和界面挂载均在门禁成功后发生。失败不发布候选注册表、不创建 Store、不读取存档，并通过 Web 最小错误状态和 Electron `userdata/startup.log` 提供诊断。
17. 阶段 6 第二个收尾切片选择完整的 `taoyuan:bomb` 消费者组：`useProcessingStore.ts`、`MiningView.vue` 和 `ProcessingView.vue` 已停止直接导入旧 `BOMBS`，统一通过已发布注册表的公开查询入口读取；旧导出、制造签名、顺序、名称、材料、金钱、矿洞效果和存档结构保持不变，业务静态读取违规由 33 条降为 30 条。
18. 行会商品消费者组完成 `taoyuan:shop_offer` 收口：Guild Store 与行会页面不再直接读取 `GUILD_SHOP_ITEMS`；24 件商品的顺序、分组、名称、货币、等级、限购、材料、装备发放、失败回滚和旧存档计数保持不变，违规由 30 条降为 28 条。
19. 成就物品目录消费者组改用 `taoyuan:item` 的旧形状适配列表：图鉴分类/顺序、虚拟滚动、出货分组、完整出货判定和完成度分母与旧 `ITEMS` 全量等价，违规由 28 条降为 26 条。
20. 矿洞宝箱装备消费者组改用 `taoyuan:drop_table` 的按来源/类型/区域读取入口：六个区域的戒指、帽子、鞋子和武器池顺序与概率不变，手动开箱、连战自动清理、首次发放和重复装备自动售出保持原行为，违规由 26 条降为 22 条。
21. 矿洞怪物装备结算消费者组复用同一读取入口：六区域四类装备池、概率边界、随机调用顺序、武器随机附魔、首次发放和重复自动售出保持不变，违规由 22 条降为 18 条。
22. 行会图鉴怪物装备展示消费者组改读同一注册表投影：区域关联、武器/戒指/帽子/鞋子顺序、名称、概率、BOSS 首杀展示和无关联怪物空状态保持不变，违规由 18 条降为 14 条。
23. 成就及背包装备展示消费者组改读 `taoyuan:equipment` 和 `taoyuan:enchantment`：全部武器及附魔的顺序、旧字段、固定附魔名称、附魔选项和未知 ID 行为保持不变，违规由 14 条降为 11 条。
24. 商店装备池消费者组通过注册表门面读取武器、帽子、鞋子购买池和戒指、帽子、鞋子制造池：商品顺序、价格、材料、效果、购买与制造流程保持不变，违规由 11 条降为 5 条。
25. 商店定义消费者组通过 `taoyuan:shop` 的有序旧形状投影渲染六大商铺并解析当前商店：名称、NPC、顺序、营业条件、导航和未知商店空状态保持不变，违规由 5 条降为 4 条。
26. 鱼塘品种计数消费者组从 `taoyuan:pond_breed` 分代查询结果计算图鉴分母：五代数量仍为 200 / 100 / 50 / 30 / 20，总数仍为 400，显示顺序、已发现计数、筛选和分页保持不变，违规由 4 条降为 3 条。
27. 温室作物目录消费者组改用 `taoyuan:crop` 的有序旧形状投影：作物 ID、名称、种子映射、生长数据、温室选项、肥料和育种种子行为保持不变，违规由 3 条降为 2 条。
28. 每日配方解锁消费者组改用完整官方配方旧形状投影：遍历顺序、NPC 好感、配偶、技能、特殊物品、重复解锁和日志行为保持不变；旧 `itemId + quantity` 材料继续兼容，违规由 2 条降为 1 条。
29. `taoyuan:breeding_hybrid` 现显式携带 1 至 10 阶 `tier`，业务阶层查询不再读取 `HYBRID_TIER_COUNTS`；400 条定义的旧阶层分布、旧 `HybridDef`、查询签名、未知 ID 返回值和存档结构保持不变。业务静态读取违规由 1 条降为 0，但这只使对应验收项通过，不代表阶段 6 整体完成。
30. 阶段 6 机器盘点结论已逐符号闭合：99 个 `baselined` 最终归为 7 个 `verified` 和 92 个 `framework-retained`，52 个 `inventoried` re-export 最终归为 46 个 `verified` 和 6 个 `framework-retained`。`pnpm run mod:audit-phase6 -- --strict` 现对暂定符号、未解析/命名空间运行时导入、业务静态读取和启动语义执行统一失败门禁，未来新增内容、适配器和 barrel 仍默认进入待审计状态。
31. 注册表快照升级为 `formatVersion: 2`：注册表数组保持稳定 ID 顺序，每个 `entries` 数组直接保存真实注册顺序，`snapshotHash` 覆盖格式、内容、来源和顺序语义。完整构建、启动门禁构建和恢复结果的 54 个注册表、4242 条目、`entries()`、`values()`、来源、查询和冻结状态逐项相等，新哈希为 `sha256:4e87e4bc1d6310d4467335da77603006bf769fdb5c4da45ad927f7ed85a5c4b3`。v1 只作为可失效重建的派生缓存处理，不涉及玩家存档。
32. 官方预编译注册表切片提交确定性 JSON 产物和元数据，使用 TypeBox 契约校验 `unknown` 输入，并将 v2 快照绑定到包内容、配置、Schema 集、编译器、加载顺序和信任策略构成的 `environmentHash`。启动时优先恢复预编译产物，缺失、非法 JSON、结构/哈希错误、旧格式或环境变化均完整回退静态构建；旧静态路径和所有公开查询签名保留。Windows 本机逐字节检查与哈希探针已通过，三系统 CI 使用同一已提交产物作为基准，不维护平台特例。

## 变更规则

- 架构决策修改必须同时更新受影响的主题文档、实施阶段、测试和风险。
- 公开字段变化必须先改 TypeBox Schema，再生成类型和公开 JSON Schema。
- 新的 MUST/MUST NOT 规则必须登记规则编号、错误码和一致性夹具。
- 更改默认资源限制必须记录原因、性能证据和兼容影响。
- 内容文件迁移后必须更新机器盘点状态，不能只修改说明文字。
- 重大变更同步更新 `../refactor-plan.md`；存档变化同步更新 `../save-compatibility.md`。
